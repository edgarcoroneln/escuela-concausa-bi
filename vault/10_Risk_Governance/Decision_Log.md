---
id: DOC-DECLOG
title: "Decision Log"
owner: "Edgar Edmundo Coronel Navarrete"
status: active
source_of_truth: true
tags: [governance, decisions]
---

# Decision Log — FARO

> Decisiones de proceso/producto (las técnicas van a `vault/03_Architecture/ADRs`).
> → [[vault/10_Risk_Governance/_index]]

| DEC | Fecha | Decisión | Contexto | Dueño |
|---|---|---|---|---|
| DEC-001 | 2026-08-05 | El tablero PM es una proyección generada, no una fuente de verdad | Evitar duplicar US, responsables, riesgos y estados dentro del HTML | Edgar Edmundo Coronel Navarrete |
| DEC-002 | 2026-08-06 | Reducir temporalmente a una aprobación la protección del PR y restaurar dos aprobaciones el 2026-08-07 | Excepción operativa para agilizar el PR del tablero; no modifica la política canónica de doble compuerta | Edgar Edmundo Coronel Navarrete |
| DEC-003 | 2026-08-09 | **Cambio de política canónica: de doble compuerta (2 aprobaciones) a compuerta única (1 aprobación, el PM).** Ruleset `main` → `required_approving_review_count: 1`, `require_code_owner_review: true`; CODEOWNERS reducido a `* @edgarcoroneln`; se agrega bypass de administrador para que el PM pueda mergear sus propios PR (no puede autoaprobarlos). | La doble compuerta demostró ser un cuello de botella operativo en Sprint 1 (PR bloqueados esperando 2ª aprobación). El PM asume la revisión de proceso y técnica; los Tech Leads revisan de forma no bloqueante. Deja sin efecto la política de doble compuerta de [[vault/05_Engineering/Branching_Strategy]]. | Edgar Edmundo Coronel Navarrete |
| DEC-004 | 2026-08-11 | **Auto-refresco del Tablero PM en cada push a `main`.** Workflow `refresh-dashboard.yml` regenera y commitea el tablero (HTML + snapshot + historial + actividad) al mergear a `main`. Para que el bot pueda escribir en `main` (protegida por el ruleset) se usa un **PAT fine-grained de admin** en el secreto `DASHBOARD_PAT`, ya que el admin está en el bypass del ruleset (DEC-003). Cambio de CI/CD y seguridad (regla 7): **revisión de Célula 5 (Luis)**. | El tablero solo se refrescaba cuando alguien corría el generador a mano; se busca que todos los reportes estén "vivos" sin intervención. Prevención de loop: el disparador excluye `vault/13_Reports/**` y el commit del bot lleva `[skip ci]`. | Edgar Edmundo Coronel Navarrete |
| DEC-005 | 2026-08-13 | **Contrato de schema de `gold.predicciones`: conserva `valor` (variación cruda, para métricas MAE/RMSE de ML-01) y agrega la columna derivada `indice_riesgo` (float[0,1], única fuente `src/modelos/riesgo.py`).** `driver_dominante` vive en `gold.recomendaciones`; ambas salidas de ML se leen por `JOIN (cct, id_ciclo)` y nunca se duplican en `fact_escuela_ciclo` (Data_Model §4.1/§4.5). | Cierra la ambigüedad señalada en [[vault/15_ML_Models/Indice_Riesgo_ML01]] §4 (¿la tabla guarda la variación cruda o el índice acotado? — guarda las dos). Ratificado el 13-ago por C2 (Manuel) con C1/C3/C4; cambio de schema (regla 7) revisado por Diana (C1), Andrés/Héctor (C3) y Christian (C4). Se **registra ahora** para cerrar el hueco (varios DevLogs pedían "que el PM registre DEC-005"). Es la mitad "schema" del atajo `DEC-005/006`. Toca US-201, US-313, TEST-006. | Edgar Edmundo Coronel Navarrete |
| DEC-006 | 2026-08-13 | **Umbral de negocio: "escuela en riesgo" = `indice_riesgo ≥ 0.6` ↔ pérdida de ~5% de matrícula.** Desbloquea que [[vault/15_ML_Models/Indice_Riesgo_ML01]] pase de `in_review` a `approved`. | El `0.6` lo fijó el agente en S1 como "revisable por negocio"; ratificado el 13-ago por Manuel (C2) leyendo el `>= 0.6` de [[vault/04_UX_Design/Screen_Specs]]. Es la mitad "umbral" del atajo `DEC-005/006` que circulaba sin registrar. Toca US-201 (KPI-04), DOC-INDICE-RIESGO. | Edgar Edmundo Coronel Navarrete |
| DEC-007 | 2026-08-19 | **Definición del target de ML como híbrido de dos niveles (mitigación de RISK-007).** El objetivo supervisado `target_variacion_matricula` se calcula a nivel **`municipio × nivel`** usando la **serie histórica agregada de la SEP (SNIEE / Sistema de Consulta de Estadística Educativa)** —multi-año y validable con partición temporal—; las **features y el driver dominante se mantienen a nivel escuela (CCT)** con el 911 2024-2025 + los 6 drivers. En paralelo se persigue el 2º ciclo crudo del 911 para, de llegar antes del gate S4, subir la granularidad del target a escuela. Fallback si nada llega el 2026-08-30: índice compuesto de riesgo desde los 6 drivers, marcado `SIN_DATO_REAL`. | El Formato 911 solo se descargó con el ciclo 2024-2025; sin ≥2 ciclos no hay etiqueta que predecir. La serie SNIEE es la **misma fuente DS-01** en otra distribución (agregada), así que no se agrega una 9ª fuente ni se altera la constante de 8 fuentes. Preserva el carácter prescriptivo (driver por escuela) con un target real hoy. Toca US-104, US-311, US-313. | Edgar Edmundo Coronel Navarrete |
| DEC-008 | 2026-08-14 | **Cambio de grano de `gold.cubo_comparador_municipio`: de `municipio × ciclo` a `municipio × nivel × ciclo`, con las métricas almacenadas como numerador y denominador por separado (no como razón/ratio precalculada).** Habilita el filtro global por nivel educativo de AC-002.2 (DB-04) y que cualquier filtro downstream reagregue correctamente. | Un cubo ya agregado no se puede desagregar después. Decisión de esquema (regla 7) tomada por Diana Alvarez Varela (TL Célula 1) el 14-ago a partir del hallazgo de Marina García en US-211a; [[vault/03_Architecture/Data_Model]] §4.3 la señalaba como "pendiente de registrar". Toca US-211a, US-212 (DB-04), REQ-002. | Edgar Edmundo Coronel Navarrete |
| DEC-009 | 2026-08-23 | **Extiende el patrón de DEC-008 a otros cuatro cubos: `gold.cubo_matricula`, `gold.cubo_riesgo_territorial`, `gold.cubo_driver` y `gold.cubo_completitud` agregan `nivel` a su grano** (`entidad × municipio × ciclo` → `municipio × nivel × ciclo` en `cubo_matricula`; `municipio × ciclo` → `municipio × nivel × ciclo` en `cubo_riesgo_territorial`; `driver × municipio × ciclo` → `driver × municipio × nivel × ciclo` en `cubo_driver`; `municipio × driver × ciclo` → `municipio × nivel × driver × ciclo` en `cubo_completitud`), con sus métricas como numerador/denominador por separado, nunca como razón o promedio precalculado. `gold.cubo_pivot` (DB-08) y `gold.cubo_escuela_360` (DB-03) no cambian: ya traen `nivel` gratis vía `dim_escuela`. | Mismo problema que resolvió DEC-008: un cubo ya agregado no se puede desagregar después, y AC-002.2 exige que el filtro global de nivel educativo aplique a *todos* los tableros (DB-01, DB-02, DB-05, DB-07 en este caso). Señalado por Deni Garrido Fragoso (US-113, al planear la materialización) y por Monserrat Xcaret Miranda Olivas (US-211b, `vault/04_UX_Design/Cube_Specs_DB05_DB08.md` §8.1) el 22-ago; decisión de esquema (regla 7) tomada por Diana Alvarez Varela (TL Célula 1) el 23-ago. Las SQL de referencia de Manuel Serranía para DB-01/DB-02 (`superset/semantic/db01_cubo_matricula.sql`/`db02_cubo_riesgo_territorial.sql`, PR #71 ya mergeado) y de Monserrat para DB-05 (`superset/semantic/db05_cubo_driver.sql`) ya estaban construidas asumiendo este grano nuevo — mantener el grano viejo habría roto ese trabajo ya entregado, no al revés. Toca US-113, US-211b, US-203, REQ-002. | Edgar Edmundo Coronel Navarrete |
| DEC-010 | 2026-08-23 | **`gold.predicciones` adopta un grano dual: agrega el discriminador `grano` (`escuela` \| `municipio_nivel`) junto con `cve_mun`+`nivel` como llaves alternativas a `cct`, en vez de repartir el valor predicho de ML-01 a cada escuela del grupo cuando predice a `municipio × nivel` (DEC-007).** `gold.recomendaciones` no cambia — se mantiene siempre a grano escuela. | Repartir un valor predicho a municipio×nivel hacia cada escuela le atribuiría un dato que no se midió ahí — mismo principio de `SIN_DATO` explícito que rige el resto del esquema. Señalado por Héctor Rafael Morales Marbán (PR #56, C3) junto con una pregunta a Imanol Ruiz Hurtado (C4, dueño de `PrediccionOut`); decisión de esquema (regla 7) tomada por Diana Alvarez Varela (TL Célula 1) el 23-ago. Forma exacta del contrato de la API pendiente de confirmar con Imanol. Toca US-311, US-313, REQ-003, DEC-007. | Edgar Edmundo Coronel Navarrete |
| DEC-011 | 2026-08-23 | **Convención de defaults para las vars de dbt (cierre de BUG-009): toda var del proyecto dbt debe tener valor por default en el repo, y su ubicación depende de qué es.** Los `identifier` de fuentes Bronze llevan el default **inline** en `dbt/models/sources.yml` (`{{ var('x', 'default') }}`), extendiendo a las 7 restantes el patrón que ya usaban `formato911`, `formato911_historico` y `cemabe`. Las vars que **no** son identifiers (`bronze_conapo_age_column`, `bronze_sesnsp_count_column`, `bronze_conagua_id_column`, `coneval_periodo_medicion`) viven en un bloque `vars:` nuevo en `dbt/dbt_project.yml`, porque `sources.yml` no tiene dónde alojarlas. Se agrega el job **`dbt parse`** a `ci.yml` como test de regresión permanente. El reparto **no** se divide entre los dueños de cada DS: lo ejecuta el PM en un solo PR y cada dueño revisa sus propios valores. | dbt renderiza el manifest completo del proyecto antes de ejecutar cualquier selección, así que **una** var sin default rompía `dbt build`/`dbt run`/`dbt test` para todo el equipo, aunque el modelo elegido no la usara — incluidos los comandos que `CLAUDE.md` documenta como estándar. Al validarlo aparecieron 11 vars sin default, no las 7 registradas en BUG-009: faltaba `bronze_conagua_id_column` (`agua_region.sql`), que nadie había topado porque `conagua` no tiene datos ingeridos. Se descartó repartir en 4 PRs (uno por dueño de DS) porque los cuatro tocaban el mismo YAML: conflicto garantizado y 4 ciclos de revisión para ~15 líneas, a dos semanas del freeze. Ubicación ratificada por Diana Alvarez Varela (TL Célula 1, regla 7) el 23-ago, que eligió mantener el patrón inline existente sobre centralizar las 11. Deja dos valores sin confirmar, documentados como tales: `coneval_periodo_medicion` (deuda técnica aceptada por el PM, pendiente de Deni, rastreada como **RISK-008** con dueña y fecha objetivo porque el tablero PM lee el registro de riesgos y no el de bugs) y `bronze_conagua_identifier` (default deliberadamente falso `conagua_no_ingerido`). Toca BUG-009, US-111, US-106, REQ-001. | Edgar Edmundo Coronel Navarrete |
| DEC-012 | 2026-08-28 | **Criterio de cierre por superficie desplegada: una historia cuyo entregable es una ruta HTTP no pasa a `done` mientras esa ruta no responda en el despliegue que se va a demostrar.** Una historia cuyo entregable es un **contrato, un esquema o una biblioteca** sí cierra con evidencia de código y pruebas, porque no tiene superficie desplegada que verificar. | Surgió de una asimetría real detectada por Héctor Rafael Morales Marbán al revisar la reconciliación del 28-ago (PR #120): US-411 se sostenía abierta por BUG-020 mientras US-412 —que entrega `/predicciones/*`, rutas que respondían 500 en el mismo despliegue— se cerraba difiriendo el E2E a US-422, que estaba `planned`. La misma incoherencia existía dentro de las tres historias de un solo dueño: US-412 cerraba y US-416 no, con idéntico diferimiento. El criterio evita que el tablero declare funcionando una API que la demo mostraría rota, sin castigar a las historias que entregan artefactos de código. Aplicación inmediata: US-412 volvió a `in_review`, US-415 se mantuvo `done`. Escrito también en las reglas de [[vault/12_Roadmap_Sprints/Execution_Status]]. Toca US-411, US-412, US-415, US-416, REQ-004. | Edgar Edmundo Coronel Navarrete |
| DEC-013 | 2026-08-29 | **Un identificador queda reservado únicamente cuando está escrito en su registro canónico en `main`.** Un número anunciado de palabra, en un mensaje o en una rama sin mergear no reserva nada. Ante una colisión gana quien ya está en `main`; el otro toma el siguiente libre. Un hueco en la numeración es aceptable —los IDs no se reciclan (regla 3)— y siempre preferible a un número duplicado. | Tres colisiones en una semana sobre `BUG-###`: BUG-016 y BUG-020 se asignaron dos veces por acuerdos verbales, y BUG-022 se le apartó de palabra a Monserrat Xcaret Miranda Olivas mientras Diana Alvarez Varela lo registraba de verdad. Resolver cada colisión costó renumerar filas y rehacer PRs. La regla aplica a todo prefijo con registro canónico: `BUG-` ([[vault/06_Quality_Testing/Bug_Register]]), `DEC-` (este documento), `RISK-`, `ADR-`, `INC-`. Toca REQ-007, regla 3 del vault. | Edgar Edmundo Coronel Navarrete |
| DEC-014 | 2026-09-03 | **US-413, US-414 y US-525a se recortan del alcance y se registran como `descoped`, no como `done`.** Una historia recortada no se entregó, así que no puede pesar como entrega en el porcentaje; pero tampoco es trabajo pendiente, así que sale del denominador en vez de arrastrar el avance. Se introduce el estado terminal `descoped` en [[vault/12_Roadmap_Sprints/Execution_Status]], que exige la decisión y la fecha del corte en la evidencia, y el generador del tablero las reporta aparte en un bloque propio. Alcance del proyecto: **91 → 88 historias**. | El 3-sep reasigné la semana de Karla Monter a cerrar US-411 (cortando US-413/US-414) y corté US-525a con Alejandro Velázquez. Las tres decisiones vivieron **sólo en los DevLog de quien las recibió** y ninguna llegó a `Execution_Status`, así que el tablero las siguió contando como pendientes por dos días. El riesgo de la alternativa —marcarlas `done`— es peor: infla el avance con trabajo que no existe, justo el modo de falla que ya nos costó reabrir US-206. Riesgos aceptados: la API queda sin colección importable publicada (`/docs` de FastAPI cubre la demostración) y sin runbook de rollback escrito para la ventana de demo. Toca REQ-004, REQ-005 y la regla 1 del vault. | Edgar Edmundo Coronel Navarrete |
| DEC-015 | 2026-09-05 | **`US-207` cierra con dos modelos servidos y el tercero declarado, no con los tres.** El panel de ML muestra ML-01 (índice de riesgo contra el umbral 0.60) y ML-02 (driver dominante y recomendación prescriptiva) contra la API real, y presenta **ML-03 como `SIN_DATO` explícito** con el aviso de qué falta, por qué y cuál historia lo cierra. **Lo que NO se ratifica:** que el par de demostración se pueda citar el 9-sep. Marina reporta `15DJN0049A` → riesgo 0.7423 / D1 y `09DSN0042A` → 0.6692 / D2; **eso es cierto en local y falso en producción**, que hoy devuelve para el primer CCT riesgo **0.129** y driver **D3**. Mientras `BUG-048` siga abierto, la narrativa "mismo riesgo, distinta recomendación" **no existe en la URL pública** y no puede sostenerse en la demo. | ML-03 **no tiene productor**: `US-321` (Estefany Hernández, C3) sigue `in_progress`, y el contrato de la API ya lo documentaba antes de este trabajo — `src/api/schemas.py:169` declara `cluster: StrictInt \| None` nombrando la historia faltante, y `repositorio_modelos.py:104` lo fija en `None`. Verificado: producción devuelve `"cluster": null`. El `PR #231` de Estefany **no lo destraba**: agrega `ejecutar_cierre_ml03.py` pero su propio DevLog deja la corrida real pendiente, así que `cluster` sigue nulo aunque se mergee. Declarar el hueco es la **regla de cobertura parcial** del proyecto —`SIN_DATO` explícito, nunca cero ni nulo silencioso— y es el arreglo que ya aplicamos en `BUG-017` y `BUG-031`: un número creíble que significa otra cosa cuesta más que un hueco visible. La alternativa —mantener `US-207` abierta— deja una historia de C2 rehén de una de C3 a un día del freeze, sin trabajo de C2 que hacer. Sin costo de retrabajo: el cliente ya propaga el valor y hay una prueba que lo verifica, así que cuando `US-321` publique, la página no se toca. Toca REQ-002, REQ-003, `US-321`, `BUG-048`. | Edgar Edmundo Coronel Navarrete |
