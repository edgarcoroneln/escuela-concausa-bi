---
id: DOC-DECLOG
title: "Decision Log"
owner: "Edgar Edmundo Coronel Navarrete"
status: active
source_of_truth: true
tags: [governance, decisions]
---

# Decision Log — FARO

> Decisiones de proceso/producto (las técnicas van a `vault/03_Architecture/ADRs`).
> → [[vault/10_Risk_Governance/_index]]

| DEC | Fecha | Decisión | Contexto | Dueño |
|---|---|---|---|---|
| DEC-001 | 2026-08-05 | El tablero PM es una proyección generada, no una fuente de verdad | Evitar duplicar US, responsables, riesgos y estados dentro del HTML | Edgar Edmundo Coronel Navarrete |
| DEC-002 | 2026-08-06 | Reducir temporalmente a una aprobación la protección del PR y restaurar dos aprobaciones el 2026-08-07 | Excepción operativa para agilizar el PR del tablero; no modifica la política canónica de doble compuerta | Edgar Edmundo Coronel Navarrete |
| DEC-003 | 2026-08-09 | **Cambio de política canónica: de doble compuerta (2 aprobaciones) a compuerta única (1 aprobación, el PM).** Ruleset `main` → `required_approving_review_count: 1`, `require_code_owner_review: true`; CODEOWNERS reducido a `* @edgarcoroneln`; se agrega bypass de administrador para que el PM pueda mergear sus propios PR (no puede autoaprobarlos). | La doble compuerta demostró ser un cuello de botella operativo en Sprint 1 (PR bloqueados esperando 2ª aprobación). El PM asume la revisión de proceso y técnica; los Tech Leads revisan de forma no bloqueante. Deja sin efecto la política de doble compuerta de [[vault/05_Engineering/Branching_Strategy]]. | Edgar Edmundo Coronel Navarrete |
| DEC-004 | 2026-08-11 | **Auto-refresco del Tablero PM en cada push a `main`.** Workflow `refresh-dashboard.yml` regenera y commitea el tablero (HTML + snapshot + historial + actividad) al mergear a `main`. Para que el bot pueda escribir en `main` (protegida por el ruleset) se usa un **PAT fine-grained de admin** en el secreto `DASHBOARD_PAT`, ya que el admin está en el bypass del ruleset (DEC-003). Cambio de CI/CD y seguridad (regla 7): **revisión de Célula 5 (Luis)**. | El tablero solo se refrescaba cuando alguien corría el generador a mano; se busca que todos los reportes estén "vivos" sin intervención. Prevención de loop: el disparador excluye `vault/13_Reports/**` y el commit del bot lleva `[skip ci]`. | Edgar Edmundo Coronel Navarrete |
| DEC-005 | 2026-08-13 | **Contrato de schema de `gold.predicciones`: conserva `valor` (variación cruda, para métricas MAE/RMSE de ML-01) y agrega la columna derivada `indice_riesgo` (float[0,1], única fuente `src/modelos/riesgo.py`).** `driver_dominante` vive en `gold.recomendaciones`; ambas salidas de ML se leen por `JOIN (cct, id_ciclo)` y nunca se duplican en `fact_escuela_ciclo` (Data_Model §4.1/§4.5). | Cierra la ambigüedad señalada en [[vault/15_ML_Models/Indice_Riesgo_ML01]] §4 (¿la tabla guarda la variación cruda o el índice acotado? — guarda las dos). Ratificado el 13-ago por C2 (Manuel) con C1/C3/C4; cambio de schema (regla 7) revisado por Diana (C1), Andrés/Héctor (C3) y Christian (C4). Se **registra ahora** para cerrar el hueco (varios DevLogs pedían "que el PM registre DEC-005"). Es la mitad "schema" del atajo `DEC-005/006`. Toca US-201, US-313, TEST-006. | Edgar Edmundo Coronel Navarrete |
| DEC-006 | 2026-08-13 | **Umbral de negocio: "escuela en riesgo" = `indice_riesgo ≥ 0.6` ↔ pérdida de ~5% de matrícula.** Desbloquea que [[vault/15_ML_Models/Indice_Riesgo_ML01]] pase de `in_review` a `approved`. | El `0.6` lo fijó el agente en S1 como "revisable por negocio"; ratificado el 13-ago por Manuel (C2) leyendo el `>= 0.6` de [[vault/04_UX_Design/Screen_Specs]]. Es la mitad "umbral" del atajo `DEC-005/006` que circulaba sin registrar. Toca US-201 (KPI-04), DOC-INDICE-RIESGO. | Edgar Edmundo Coronel Navarrete |
| DEC-007 | 2026-08-19 | **Definición del target de ML como híbrido de dos niveles (mitigación de RISK-007).** El objetivo supervisado `target_variacion_matricula` se calcula a nivel **`municipio × nivel`** usando la **serie histórica agregada de la SEP (SNIEE / Sistema de Consulta de Estadística Educativa)** —multi-año y validable con partición temporal—; las **features y el driver dominante se mantienen a nivel escuela (CCT)** con el 911 2024-2025 + los 6 drivers. En paralelo se persigue el 2º ciclo crudo del 911 para, de llegar antes del gate S4, subir la granularidad del target a escuela. Fallback si nada llega el 2026-08-30: índice compuesto de riesgo desde los 6 drivers, marcado `SIN_DATO_REAL`. | El Formato 911 solo se descargó con el ciclo 2024-2025; sin ≥2 ciclos no hay etiqueta que predecir. La serie SNIEE es la **misma fuente DS-01** en otra distribución (agregada), así que no se agrega una 9ª fuente ni se altera la constante de 8 fuentes. Preserva el carácter prescriptivo (driver por escuela) con un target real hoy. Toca US-104, US-311, US-313. | Edgar Edmundo Coronel Navarrete |
| DEC-008 | 2026-08-14 | **Cambio de grano de `gold.cubo_comparador_municipio`: de `municipio × ciclo` a `municipio × nivel × ciclo`, con las métricas almacenadas como numerador y denominador por separado (no como razón/ratio precalculada).** Habilita el filtro global por nivel educativo de AC-002.2 (DB-04) y que cualquier filtro downstream reagregue correctamente. | Un cubo ya agregado no se puede desagregar después. Decisión de esquema (regla 7) tomada por Diana Alvarez Varela (TL Célula 1) el 14-ago a partir del hallazgo de Marina García en US-211a; [[vault/03_Architecture/Data_Model]] §4.3 la señalaba como "pendiente de registrar". Toca US-211a, US-212 (DB-04), REQ-002. | Edgar Edmundo Coronel Navarrete |
| DEC-009 | 2026-08-23 | **Extiende el patrón de DEC-008 a otros cuatro cubos: `gold.cubo_matricula`, `gold.cubo_riesgo_territorial`, `gold.cubo_driver` y `gold.cubo_completitud` agregan `nivel` a su grano** (`entidad × municipio × ciclo` → `municipio × nivel × ciclo` en `cubo_matricula`; `municipio × ciclo` → `municipio × nivel × ciclo` en `cubo_riesgo_territorial`; `driver × municipio × ciclo` → `driver × municipio × nivel × ciclo` en `cubo_driver`; `municipio × driver × ciclo` → `municipio × nivel × driver × ciclo` en `cubo_completitud`), con sus métricas como numerador/denominador por separado, nunca como razón o promedio precalculado. `gold.cubo_pivot` (DB-08) y `gold.cubo_escuela_360` (DB-03) no cambian: ya traen `nivel` gratis vía `dim_escuela`. | Mismo problema que resolvió DEC-008: un cubo ya agregado no se puede desagregar después, y AC-002.2 exige que el filtro global de nivel educativo aplique a *todos* los tableros (DB-01, DB-02, DB-05, DB-07 en este caso). Señalado por Deni Garrido Fragoso (US-113, al planear la materialización) y por Monserrat Xcaret Miranda Olivas (US-211b, `vault/04_UX_Design/Cube_Specs_DB05_DB08.md` §8.1) el 22-ago; decisión de esquema (regla 7) tomada por Diana Alvarez Varela (TL Célula 1) el 23-ago. Las SQL de referencia de Manuel Serranía para DB-01/DB-02 (`superset/semantic/db01_cubo_matricula.sql`/`db02_cubo_riesgo_territorial.sql`, PR #71 ya mergeado) y de Monserrat para DB-05 (`superset/semantic/db05_cubo_driver.sql`) ya estaban construidas asumiendo este grano nuevo — mantener el grano viejo habría roto ese trabajo ya entregado, no al revés. Toca US-113, US-211b, US-203, REQ-002. | Edgar Edmundo Coronel Navarrete |
| DEC-010 | 2026-08-23 | **`gold.predicciones` adopta un grano dual: agrega el discriminador `grano` (`escuela` \| `municipio_nivel`) junto con `cve_mun`+`nivel` como llaves alternativas a `cct`, en vez de repartir el valor predicho de ML-01 a cada escuela del grupo cuando predice a `municipio × nivel` (DEC-007).** `gold.recomendaciones` no cambia — se mantiene siempre a grano escuela. | Repartir un valor predicho a municipio×nivel hacia cada escuela le atribuiría un dato que no se midió ahí — mismo principio de `SIN_DATO` explícito que rige el resto del esquema. Señalado por Héctor Rafael Morales Marbán (PR #56, C3) junto con una pregunta a Imanol Ruiz Hurtado (C4, dueño de `PrediccionOut`); decisión de esquema (regla 7) tomada por Diana Alvarez Varela (TL Célula 1) el 23-ago. Forma exacta del contrato de la API pendiente de confirmar con Imanol. Toca US-311, US-313, REQ-003, DEC-007. | Edgar Edmundo Coronel Navarrete |
| DEC-011 | 2026-08-23 | **Convención de defaults para las vars de dbt (cierre de BUG-009): toda var del proyecto dbt debe tener valor por default en el repo, y su ubicación depende de qué es.** Los `identifier` de fuentes Bronze llevan el default **inline** en `dbt/models/sources.yml` (`{{ var('x', 'default') }}`), extendiendo a las 7 restantes el patrón que ya usaban `formato911`, `formato911_historico` y `cemabe`. Las vars que **no** son identifiers (`bronze_conapo_age_column`, `bronze_sesnsp_count_column`, `bronze_conagua_id_column`, `coneval_periodo_medicion`) viven en un bloque `vars:` nuevo en `dbt/dbt_project.yml`, porque `sources.yml` no tiene dónde alojarlas. Se agrega el job **`dbt parse`** a `ci.yml` como test de regresión permanente. El reparto **no** se divide entre los dueños de cada DS: lo ejecuta el PM en un solo PR y cada dueño revisa sus propios valores. | dbt renderiza el manifest completo del proyecto antes de ejecutar cualquier selección, así que **una** var sin default rompía `dbt build`/`dbt run`/`dbt test` para todo el equipo, aunque el modelo elegido no la usara — incluidos los comandos que `CLAUDE.md` documenta como estándar. Al validarlo aparecieron 11 vars sin default, no las 7 registradas en BUG-009: faltaba `bronze_conagua_id_column` (`agua_region.sql`), que nadie había topado porque `conagua` no tiene datos ingeridos. Se descartó repartir en 4 PRs (uno por dueño de DS) porque los cuatro tocaban el mismo YAML: conflicto garantizado y 4 ciclos de revisión para ~15 líneas, a dos semanas del freeze. Ubicación ratificada por Diana Alvarez Varela (TL Célula 1, regla 7) el 23-ago, que eligió mantener el patrón inline existente sobre centralizar las 11. Deja dos valores sin confirmar, documentados como tales: `coneval_periodo_medicion` (deuda técnica aceptada por el PM, pendiente de Deni, rastreada como **RISK-008** con dueña y fecha objetivo porque el tablero PM lee el registro de riesgos y no el de bugs) y `bronze_conagua_identifier` (default deliberadamente falso `conagua_no_ingerido`). Toca BUG-009, US-111, US-106, REQ-001. | Edgar Edmundo Coronel Navarrete |
| DEC-012 | 2026-08-28 | **Criterio de cierre por superficie desplegada: una historia cuyo entregable es una ruta HTTP no pasa a `done` mientras esa ruta no responda en el despliegue que se va a demostrar.** Una historia cuyo entregable es un **contrato, un esquema o una biblioteca** sí cierra con evidencia de código y pruebas, porque no tiene superficie desplegada que verificar. | Surgió de una asimetría real detectada por Héctor Rafael Morales Marbán al revisar la reconciliación del 28-ago (PR #120): US-411 se sostenía abierta por BUG-020 mientras US-412 —que entrega `/predicciones/*`, rutas que respondían 500 en el mismo despliegue— se cerraba difiriendo el E2E a US-422, que estaba `planned`. La misma incoherencia existía dentro de las tres historias de un solo dueño: US-412 cerraba y US-416 no, con idéntico diferimiento. El criterio evita que el tablero declare funcionando una API que la demo mostraría rota, sin castigar a las historias que entregan artefactos de código. Aplicación inmediata: US-412 volvió a `in_review`, US-415 se mantuvo `done`. Escrito también en las reglas de [[vault/12_Roadmap_Sprints/Execution_Status]]. Toca US-411, US-412, US-415, US-416, REQ-004. | Edgar Edmundo Coronel Navarrete |
| DEC-013 | 2026-08-29 | **Un identificador queda reservado únicamente cuando está escrito en su registro canónico en `main`.** Un número anunciado de palabra, en un mensaje o en una rama sin mergear no reserva nada. Ante una colisión gana quien ya está en `main`; el otro toma el siguiente libre. Un hueco en la numeración es aceptable —los IDs no se reciclan (regla 3)— y siempre preferible a un número duplicado. | Tres colisiones en una semana sobre `BUG-###`: BUG-016 y BUG-020 se asignaron dos veces por acuerdos verbales, y BUG-022 se le apartó de palabra a Monserrat Xcaret Miranda Olivas mientras Diana Alvarez Varela lo registraba de verdad. Resolver cada colisión costó renumerar filas y rehacer PRs. La regla aplica a todo prefijo con registro canónico: `BUG-` ([[vault/06_Quality_Testing/Bug_Register]]), `DEC-` (este documento), `RISK-`, `ADR-`, `INC-`. Toca REQ-007, regla 3 del vault. | Edgar Edmundo Coronel Navarrete |
| DEC-014 | 2026-09-03 | **US-413, US-414 y US-525a se recortan del alcance y se registran como `descoped`, no como `done`.** Una historia recortada no se entregó, así que no puede pesar como entrega en el porcentaje; pero tampoco es trabajo pendiente, así que sale del denominador en vez de arrastrar el avance. Se introduce el estado terminal `descoped` en [[vault/12_Roadmap_Sprints/Execution_Status]], que exige la decisión y la fecha del corte en la evidencia, y el generador del tablero las reporta aparte en un bloque propio. Alcance del proyecto: **91 → 88 historias**. | El 3-sep reasigné la semana de Karla Monter a cerrar US-411 (cortando US-413/US-414) y corté US-525a con Alejandro Velázquez. Las tres decisiones vivieron **sólo en los DevLog de quien las recibió** y ninguna llegó a `Execution_Status`, así que el tablero las siguió contando como pendientes por dos días. El riesgo de la alternativa —marcarlas `done`— es peor: infla el avance con trabajo que no existe, justo el modo de falla que ya nos costó reabrir US-206. Riesgos aceptados: la API queda sin colección importable publicada (`/docs` de FastAPI cubre la demostración) y sin runbook de rollback escrito para la ventana de demo. Toca REQ-004, REQ-005 y la regla 1 del vault. | Edgar Edmundo Coronel Navarrete |
| DEC-015 | 2026-09-05 | **`US-207` cierra con dos modelos servidos y el tercero declarado, no con los tres.** El panel de ML muestra ML-01 (índice de riesgo contra el umbral 0.60) y ML-02 (driver dominante y recomendación prescriptiva) contra la API real, y presenta **ML-03 como `SIN_DATO` explícito** con el aviso de qué falta, por qué y cuál historia lo cierra. **Lo que NO se ratifica:** que el par de demostración se pueda citar el 9-sep: el par `15DJN0049A` → 0.7423 / D1 y `09DSN0042A` → 0.6692 / D2 está **verificado en local**, y **producción no lo reproduce** mientras `BUG-048` siga abierto: hoy devuelve para el primer CCT riesgo **0.129** y driver **D3**. (Redacción corregida el 2026-09-05 a petición de Marina García: la primera se la atribuía como afirmación sobre producción, y el §7 de `Panel_ML_US207.md` ya separaba las dos superficies — local para esos dos CCT, producción para `09DBN0007I`.) Mientras `BUG-048` siga abierto, la narrativa "mismo riesgo, distinta recomendación" **no existe en la URL pública** y no puede sostenerse en la demo. | ML-03 **no tiene productor**: `US-321` (Estefany Hernández, C3) sigue `in_progress`, y el contrato de la API ya lo documentaba antes de este trabajo — `src/api/schemas.py:169` declara `cluster: StrictInt \| None` nombrando la historia faltante, y `repositorio_modelos.py:104` lo fija en `None`. Verificado: producción devuelve `"cluster": null`. El `PR #231` de Estefany **no lo destraba**: agrega `ejecutar_cierre_ml03.py` pero su propio DevLog deja la corrida real pendiente, así que `cluster` sigue nulo aunque se mergee. Declarar el hueco es la **regla de cobertura parcial** del proyecto —`SIN_DATO` explícito, nunca cero ni nulo silencioso— y es el arreglo que ya aplicamos en `BUG-017` y `BUG-031`: un número creíble que significa otra cosa cuesta más que un hueco visible. La alternativa —mantener `US-207` abierta— deja una historia de C2 rehén de una de C3 a un día del freeze, sin trabajo de C2 que hacer. Sin costo de retrabajo: el cliente ya propaga el valor y hay una prueba que lo verifica, así que cuando `US-321` publique, la página no se toca. Toca REQ-002, REQ-003, `US-321`, `BUG-048`. | Edgar Edmundo Coronel Navarrete  **Actualización 2026-09-05 (tarde):** `BUG-048` **cerró** —Luis Téllez refrescó el Gold de Cloud SQL y producción pasa de `indice_completitud_drivers` 0.197 a **0.6194**, verificado en vivo—, pero **la salvedad de esta decisión sigue viva por otra razón**: el rerun con CONAPO real cambió el modelo (`absolute_error`) y la distribución de drivers, así que el par de demostración **ya no existe en ningún ambiente**. Medido en producción: `15DJN0049A` da **0.3466 · D4 · conectividad** (era 0.129·D3 con el Gold viejo, y 0.7423·D1·becas en el local de Marina) y `09DSN0042A` da **0.1339 · D2**. Riesgos 0.35 contra 0.13 — **2.6× de diferencia: ya no es "riesgo parecido"**. Hay que **elegir un par nuevo con los datos de producción**, no reciclar el viejo. Acción de C2 (Marina García) antes del 9. |
| DEC-016 | 2026-09-05 | **Un contraste insuficiente en un elemento que FARO no estiliza se registra como limitación conocida, con su medición, y NO bloquea el cierre de la historia.** Si el elemento sí lo escribe FARO —notas en markdown dentro de los tabs, subtítulos de KPI, textos del panel de ML— es defecto y bloquea. El criterio de aceptación es **WCAG 2.1 AA** (4.5:1 texto normal, 3:1 texto grande y componentes), medido sobre el color y el fondo **efectivos** y **en los dos temas**. Aplicado: `BUG-050` deja de bloquear `US-215a` y `BUG-051` deja de bloquear `US-215b`; los dos siguen `open`. | FARO **no declara paleta propia** —verificado: cero colores en `superset/**`, ningún `color_scheme` en los YAML de tablero, ningún tema de Streamlit—: los 10 tableros heredan el tema de Superset 6.1 y FARO Web el de Streamlit. Sin esa distinción no había criterio de aceptación y dos historias de C2 quedaban rehenes de un color que no elegimos: corregir el acento del tab activo de Superset es cambiar la imagen o un *upstream*, y eso vive en `REQ-005`, no en `REQ-002`. Adoptar identidad visual propia a tres días de la demo significaría re-teñir 103 charts sin ninguna prueba detrás. Se registra aparte a petición de **Marina García** al revisar el PR #234: la regla vivía sólo en [[vault/04_UX_Design/UX_Guidelines]] siendo la decisión más consecuente del PR para su célula. Toca REQ-002, `US-215a`, `US-215b`, `BUG-050`, `BUG-051`. | Edgar Edmundo Coronel Navarrete |
| DEC-017 | 2026-09-05 | **`/api/v1/predicciones/batch` y `/predicciones/{cct}/explicacion` se quedan en `require_lectura`; no pasan a solo-analista antes de la demo.** La corrección correcta era la que aplicó Christian Ruiz: **alinear la documentación al código**, no el código a la documentación. `API_Specification.md` prometía *"rol mínimo: analista — se forzará en US-403"* y el router se monta con `require_lectura` (`v1/__init__.py:25`); la promesa nunca fue cierta, ni siquiera después de cerrar US-403. Si algún día se restringen, el cambio va en `v1/__init__.py` con su propia dependencia, **post-demo**. | Verifiqué el argumento antes de decidir y **el que dio Christian no se sostiene hoy**: buscó en el repo y **ningún consumidor fuera de la propia API llama esas dos rutas** —los únicos hits son `explicar_driver` en `entrenar_ml02.py` y su prueba—, así que restringirlas no rompería a nadie **ahora**. La decisión igual es no hacerlo, por tres razones que sí se sostienen: **(1)** el RBAC ya está demostrado — US-403 validó en vivo analista→200 y ciudadano→403 con cuentas reales, así que el punto de rúbrica no depende de estas rutas; **(2)** `/explicacion` **sirve datos mock** (`BUG-053`), y poner un rol delante de un mock no demuestra nada; **(3)** FARO Web no está desplegado y el panel de ML y el chat son exactamente las superficies que las consumirían — el riesgo no es hoy, es el lunes. Cambiar enforcement a un día del freeze por un punto que ya está asegurado es riesgo sin retorno. Toca REQ-004, `US-403`, `US-412`, `US-416`. | Edgar Edmundo Coronel Navarrete |
| DEC-018 | 2026-09-05 | **La demo corre con OAuth de Google en las dos capas: Superset y API. `AUTH_LECTURA_PUBLICA=false` en producción.** Cierra la decisión que `src/api/config.py:72` marcaba como *"PROVISIONAL; la definitiva la decide Edgar/PO"* y que `SEC-006` dejó abierta como decisión de producto. El interruptor híbrido existía para no bloquear la URL viva mientras C5 no tuviera credenciales; ya las tiene, el login e2e está validado, y la postura pasa a **lectura con sesión**. Es configuración, no código: revertir a lectura anónima es poner `AUTH_LECTURA_PUBLICA=true` **sin rebuild**, y esa marcha atrás queda disponible durante la demo si algo falla. | **Se asienta con su asimetría, que es lo que importa saber el 9.** Las dos capas exigen sesión de Google, pero **no filtran igual**: Superset tiene **lista blanca estricta** (`SUPERSET_SSO_ALLOWED_EMAILS`, con guarda fail-loud si queda vacía, `docker/superset_config.py:160`), mientras que **la API no tiene lista blanca de acceso**: `resolve_role` (`src/api/security/roles.py:18-22`) devuelve `ciudadano` a **cualquier** correo que Google autentique, y sólo `analista` está restringido por `ANALISTA_EMAILS`. Es decir: cualquiera con cuenta de Google puede **leer** la API; nadie fuera de la lista puede abrir Superset. La asimetría se acepta a propósito —lo que protegemos es escritura y admin, y todo el dato es agregado a nivel escuela, nunca alumno— pero **no se descubre el miércoles**. **Modo de falla que hay que conocer:** si el correo del evaluador no está en `SUPERSET_SSO_ALLOWED_EMAILS`, entra correctamente con Google y **lo rechazamos**, sin que nada reviente y sin mensaje que lo explique. Dar de alta ese correo antes del 9 es pendiente del PO. Toca `SEC-006` (`resolved`), `BUG-057` (`closed`), `US-403`, `US-405`, `US-207`, `REQ-004`, `ADR-004`. | Edgar Edmundo Coronel Navarrete |
| DEC-019 | 2026-09-06 | **La línea de alerta del KPI-04 baja a `indice_riesgo >= 0.50`. El ancla de la sigmoide se queda en 0.60.** Hasta hoy eran **un solo número haciendo dos trabajos distintos**, y ahí estaba el defecto: 0.60 es la equivalencia de negocio *"perder 5 % de matrícula"* que calibra la sigmoide (`src/modelos/riesgo.py::RIESGO_UMBRAL`), y **también** era el corte con el que los tableros contaban escuelas. Como corte de alerta **es inalcanzable por construcción**: el máximo que ML-01 predice sobre el Gold real de 45 276 escuelas es **0.5717** (≈ perder 4.53 %), así que el KPI daba 0 porque el corte está **por encima del techo del fenómeno**, no por un defecto del modelo ni de los datos. **Revisa `DEC-006`, no la deroga**: cambia el criterio de alerta, no la calibración. **No se recalibra, no se re-entrena y no cambia un solo `indice_riesgo` publicado.** | **El número lo eligió C2 (Marina García del Buey) con un criterio de negocio, no de conveniencia**, y el PO lo asienta sin consultarlo de nuevo. **Por qué 0.50 y no otro:** 0.50 ≈ proyectar **−3.4 %**, **justo por debajo del 3.7 % de deserción real en secundaria** (0.6 % en primaria, ciclo 2023-2024, cifra aportada por C2). La alerta enciende **antes** de que la escuela alcance la norma nacional — eso es lo que significa *temprana*. **Por qué no más abajo:** medido sobre el mismo universo, 0.40 marcaría **11 775 escuelas (26 %)** y 0.35 marcaría **24 951 (55 %)**; a esa escala deja de ser alerta y pasa a ser un censo. Con **0.50 son 7 de 45 276**: una lista accionable, que es exactamente la narrativa prescriptiva del guion —**ordenar, no contar**—. **Medición doble e independiente**: las mismas cifras las produjeron C2 y C5 por separado contra el `gold` de producción. **Verificado por el PO antes de firmar**: `DEC-019` no existía en `main` y **cinco** ramas ya lo citaban (`marina-garcia`, `christian-ruiz`, `andres-gonzalez`, `estefany-hernandez`, `luis-tellez`) — una más de las cuatro reportadas—, contra `DEC-013`. **Estado de implementación al firmar:** las **tres capas están escritas** y las tres separan el ancla de la línea con el mismo nombre —`ANCLA_SIGMOIDE` / `LINEA_DE_ALERTA`—, señal de que C1, C2 y C4 se coordinaron sin que el PO lo pidiera: **dbt** (5 archivos, Diana Alvarez, `ad3d2fd`), **API** (`repositorio_gold.py` + `mock_data.py` + `v1/gold.py`, Christian Ruiz, `0f4b37c`) y **frontend** (`prediccion_client.py`, Marina García, PR #268). **Lo que falta no es código: son tres PRs y un orden.** **Riesgo del orden, no del contenido:** si entra sólo una capa, dos superficies del mismo sistema se contradicen —los tableros dirían 7 y `/api/v1/kpis` diría 0 sobre el mismo dato—. Y hay una **asimetría de costo**: la API y el frontend filtran en tiempo de consulta, así que cambian con un **redespliegue**; los cubos de dbt **materializan** `escuelas_en_riesgo`, así que exigen **regenerar Gold y reimportar a Cloud SQL** (C5). Ver `RISK-010`, que sigue abierto porque la **duplicación persiste**: `LINEA_DE_ALERTA` queda definida dos veces, en `repositorio_gold.py` y en `prediccion_client.py`. **Corrección del PO (2026-09-06):** la primera redacción de esta entrada decía que la API y el frontend *"no los tiene nadie"*. **Era falso y el error fue de método**: busqué por el nombre viejo de la constante (`UMBRAL_RIESGO`) y las dos células la habían **renombrado**. Es el mismo modo de falla que Marina García ya me señaló con `latitud` — buscar mal y escribirlo como verificado. Toca `BUG-058`, `DEC-006`, `KPI-04`, `US-113`, `REQ-001`, `REQ-002`, `REQ-003`. | Edgar Edmundo Coronel Navarrete |
| DEC-020 | 2026-09-06 | **Excepción acotada al CODE FREEZE del 6-sep para las correcciones P0 y P1 del recorrido de la URL pública.** Alcance cerrado: sesión y logout en las cuatro páginas de FARO Web (`US-405`), ficha de escuela y búsqueda de CCT en el panel de ML (`US-207`), y navegación de los botones de la portada (`US-206`). **Corte: 18:00 del 6-sep.** Todo lo marcado P2 se cae sin discusión. **Lo que la excepción NO autoriza:** tocar Gold, modelos o esquema; correr `superset/sync_semantic_layer.py`; ni mergear un cambio de frontend sin evidencia de navegador local — captura en el chat, no sólo CI en verde. | **Por qué se abre en vez de respetar el freeze.** El freeze protege contra el cambio no probado, no contra la corrección de lo que ya está roto en la superficie que se va a demostrar. El recorrido del 5-sep por la noche encontró tres defectos que **CI no podía ver porque no son de lógica**: los tres botones de *Acceso rápido* nunca tuvieron navegación (`st.button` sin `on_click`), la sesión vivía sólo en `st.session_state` —por pestaña, y muerta al recargar— con el `refresh_token` guardado y jamás usado contra un access token de 15 minutos, y el panel de ML mandaba `(user or {}).get("access_token")`, que **siempre** fue `None`. Ese último dejó de ser latente el mismo día: con `AUTH_LECTURA_PUBLICA=false` en producción (`SEC-006`, `DEC-018`) la API responde **401** a la petición sin cabecera, así que el panel del minuto 3:00 estaba **caído en producción**. Congelar eso no es proteger la entrega: es entregarla rota. **Por qué el P2 se cae.** Subir el `alto` de los tiles de DB-01 sólo surte efecto corriendo el sync, y el sync manda `"query_context": None` en el bundle de importación (`superset/sync_semantic_layer.py:1107`, dentro de `_export_chart()`), lo que **borra el parche de metadata con el que C5 levantó 20 charts *timeseries*** el 6-sep. `ensure_chart()` **sí** conserva el `query_context` por la vía de la API, pero el mismo run arma después el bundle de importación y **ese import corre al final y lo pisa** — verificado por Marina García al revisar la premisa. La tarea, ejecutada, rompe los tableros; y DB-01 no aparece en ningún minuto del guion. *(La primera redacción citaba la línea **1074**, que cae en el bucle de métricas de `_export_dataset`. Corregido a petición de Marina García: es la **tercera** cita de código en tres PRs míos que no aterriza donde dice, y el patrón ya vale más que cada error suelto.)* **Qué queda varado por no correr el sync, y no estaba escrito.** Lo señaló Marina García y es la parte que esta decisión debía a quien depende de ella: **(1)** la corrección de lectura horizontal de **DB-05** de **Monserrat Miranda** (PR #262, mergeado el 5-sep) **no llega a producción**, porque su propio DevLog dice que aterriza al correr el bootstrap de Superset y *"no hay paso manual"* — exactamente lo que aquí se prohíbe; **(2)** los **`subheader` visibles** que Manuel Serranía alineó a `0.50` siguen imprimiendo el texto viejo en producción, porque los empuja el sync (`sync_semantic_layer.py:592`). **Precisión que cambia la gravedad, verificada por el PO:** lo que **no** queda varado es **el conteo**. `escuelas_en_riesgo` se calcula en el cubo de dbt y la métrica sólo hace `SUM(escuelas_en_riesgo)`; la clave `umbral:` del YAML **no la lee nadie** —cero coincidencias de `umbral` en `sync_semantic_layer.py`—, es metadato documental. Así que **los tableros dirán 7 en cuanto Gold se reimporte**, sin sync. Lo que queda mal es **la etiqueta**, no la cifra: un `subheader` que dice *«Índice ≥ 0.6»* junto a un 7 calculado con 0.50. Es deuda **visible** y por eso se dice en el guion antes de que la pregunten, en vez de correr el sync. Se levanta el sync **después del 9**, junto con `BUG-058`, que sigue `open` precisamente por esto. **Nota sobre el identificador.** Esta excepción se anunció al equipo como *DEC-019*. Ese ID lo ocupó la línea de alerta del KPI-04, que entró antes a `main`. Se reasienta aquí como **DEC-020** en vez de reciclar el número (regla 3 del vault, mismo criterio de `DEC-013`); la ambigüedad ya se había propagado a un documento de C2, así que conviene decirlo y no corregirlo en silencio. Toca `US-206`, `US-207`, `US-405`, `US-526`, `DEC-012`, `DEC-018`, `DEC-019`. | Edgar Edmundo Coronel Navarrete |
| DEC-021 | 2026-09-08 | **Cierre administrativo para la entrega y code freeze definitivo.** Después del PR #294 y del smoke en producción, todas las historias distintas de `US-006` pasan a `done`; `US-006` permanece `in_progress` hasta ejecutar la demo y entregar el proyecto el 9-sep. Diana Alvarez presenta el guion completo; el resto del equipo queda como respaldo técnico. No entra código funcional nuevo antes de la demo. | Esta decisión cierra la fase de construcción, no reescribe la evidencia. Las historias que llegan con entrega parcial o sin artefacto dedicado conservan el residual explícito en [[vault/12_Roadmap_Sprints/Execution_Status]] y en [[vault/13_Reports/Cierre_Proyecto_2026-09-08]]. `done` significa aquí **cerrada administrativamente para esta entrega**, no cumplimiento técnico perfecto del PRD. `DEC-014` queda superada sólo en el estado administrativo de `US-413`, `US-414` y `US-525a`; el trabajo que fue recortado no se declara mágicamente implementado. El cumplimiento estricto del profesor sigue marcando como parcial la integración de ML-03 y registra las deudas operativas aceptadas. | Edgar Edmundo Coronel Navarrete |
| DEC-022 | 2026-09-09 | **Se reabre el desarrollo y se crea S7 de recuperación hasta el domingo 13; la nueva entrega es el lunes 14 a primera hora.** Se normalizan los frentes comunicados como equipos 3/5/6/8/9/10 a Equipos 1–6: componentes, chat, UX/storytelling, ML-03, frontend/deploy y QA. Hay revisión diaria a las 18:00 y *code freeze* de la candidata el domingo a las 20:00. | La revisión externa del profesor no aceptó satisfactoriamente frontend, gráficas, storytelling, documentación de componentes, naturalidad del chat, ML-03 ni explicación ML. Esta decisión **supera operativamente DEC-021 desde el 9-sep**, sin borrar el corte histórico. Todo trabajo conserva la rama personal `dev/{identidad}` y entra por PR; `componentes-back` es una rama remota no conforme y no recibe trabajo nuevo. **Edward lidera QA y Eloisa integra formalmente ese equipo**, confirmado por el PO el 10-sep. Evidencia y detalle: [[vault/13_Reports/Revision_Profesor_2026-09-09]] y [[vault/12_Roadmap_Sprints/Plan_Recuperacion_2026-09-09]]. | Edgar Edmundo Coronel Navarrete |
| DEC-023 | 2026-09-10 | **Se aprueba la propuesta del Equipo 3 como nueva dirección UX/UI de S7 y se elimina la obligación de conservar el frontend anterior o Superset como experiencia principal.** El Equipo 5 puede reconstruir pantallas, navegación, identidad y gráficas para implementar el storytelling; Superset permanece como evidencia y respaldo. | La experiencia presentada el 9-sep fue rechazada por frontend, visualizaciones y falta de storytelling. El PO aprobó y mergeó el PR #297; P-04/P-06 quedan resueltas mediante [[vault/03_Architecture/ADRs/ADR-011-rediseno-ux-graficas-nativas]]. La libertad de rediseño no elimina PRD, datos, seguridad, accesibilidad, veracidad ni QA. Toca `US-621`, `US-641`, `US-651`, `REQ-002`, `DEC-022`. | Edgar Edmundo Coronel Navarrete |
| DEC-024 | 2026-09-10 | **La remediación S7 se ejecuta en paralelo y ninguna dependencia entre equipos funciona como stopper de construcción o prueba.** Se cierran P-01…P-06: el frontend presenta nivel de atención desde `indice_riesgo` con bandas alta `>= 0.50`, media `>= 0.30 y < 0.50`, baja `< 0.30`; el chat se llama **Asistente FARO**; `escuelas_en_riesgo` se consume del contrato existente. | Los cortes reutilizan valores ya ratificados: `LINEA_DE_ALERTA=0.50` (`DEC-019`) y `RIESGO_ESTABLE=0.30`; no dependen de cambiar `gold.recomendaciones.prioridad`, que puede alinearse después sin bloquear UX. OpenAPI, `KpisOut`, repositorio Gold, mock y pruebas ya contienen `escuelas_en_riesgo`; su smoke se ejecuta continuamente. Cada frente usa contratos/fixtures y `SIN_DATO` explícito hasta integrar la pieza real. Se mantienen como únicas compuertas rama personal, PR, CI, una aprobación y QA de candidata. Toca `REQ-002`, `REQ-003`, `REQ-006`, `REQ-007`, `US-621`, `US-641`, `US-651`, `ADR-011`. | Edgar Edmundo Coronel Navarrete |
| DEC-025 | 2026-09-11 | **Mientras dure la ventana de remediación S7 (hasta el *code freeze* del domingo 20:00), la ausencia de Luis Téllez por viaje no bloquea ningún PR sobre sus rutas críticas (`docker/**`, `docker-compose.yml`, `vault/08_CICD_DevOps/**`, `.github/**`).** La compuerta única del PM (`DEC-003`) basta para mergear; su revisión técnica sigue siendo bienvenida y se le avisa, pero deja de ser condición. `docker-compose.yml` pasa a `comunes` en `ownership.yml` — ya no reprueba a quien no sea Luis por "fuera de alcance" — y se conserva en `criticos` para que el gate lo notifique cuando vuelva, mismo patrón que `Traceability_Matrix.md` con el PM. Cada vez que se use esta autorización debe quedar dicho en el PR o el DevLog correspondiente, no en silencio. | Disparado por el PR #322 (Diana Alvarez): el botón de login de `US-405` necesitaba agregar 4 variables de entorno a `docker-compose.yml` —sin ellas, `AUTH_LECTURA_PUBLICA`, `GOOGLE_CLIENT_ID/SECRET` y `FRONTEND_REDIRECT_URIS` nunca llegaban al contenedor de la API, y el login no funcionaba ni en local—, y `check_ownership.py` lo reprobó por no estar en el alcance de nadie más que Luis. Estamos en una ventana de reapertura (`DEC-022`) con un plan de remediación deliberadamente no estructurado por historias individuales —el frontend se está replanteando por completo y hay funcionalidad deficiente que corregir sin la cadencia de un sprint normal—, así que este tipo de choque va a repetirse mientras dure S7. Esperar a que Luis esté disponible detiene trabajo crítico (login, sesión) sin ganar nada que `DEC-003` no cubra ya: los Tech Leads revisan sin bloquear. Se prefiere una autorización explícita y acotada en el tiempo a repetir esta negociación PR por PR. Toca `US-405`, `ADR-012`, `DEC-003`, `DEC-022`, regla 7 del vault. | Edgar Edmundo Coronel Navarrete |
