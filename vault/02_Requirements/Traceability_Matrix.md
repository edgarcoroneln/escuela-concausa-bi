---
id: DOC-TRACE-MATRIX
title: "Matriz de Trazabilidad — FARO"
owner: "Edgar Edmundo Coronel Navarrete"
status: in_review
version: "1.0"
source_of_truth: true
traces_up: ["vault/02_Requirements/Requirements_Detailed", "vault/02_Requirements/User_Stories"]
last_reviewed: "2026-09-10"
tags: [requirements, traceability, matrix]
---

# ⭐ Matriz de Trazabilidad — FARO

> **Vista única del estado del proyecto** (historia **US-004**, PM). Cierra el ciclo:
> REQ → AC → US → Fuentes → Modelos → Arquitectura → Test → DevLog → Release.
> → [[vault/02_Requirements/_index]] · [[vault/02_Requirements/Requirements_Detailed]] · [[vault/02_Requirements/User_Stories]]

## Cómo se mantiene

- **El PM la actualiza en cada standup** (semanas 1–3 los jueves; semanas 4–6 L-Mi-V).
- Las columnas **Test**, **DevLog** y **Release** nacen **vacías (⬜)** y se llenan conforme avanza la
  ejecución.
- **Regla dura:** una fila con **Test o DevLog en ⬜ NO está Done.** La planeación puede estar completa,
  pero el REQ solo pasa a 🟢 cuando tiene prueba verde, DevLog y quedó en un release.
- Nuevo `REQ-###` → fila nueva. Cambios de alcance → PR con aviso al dueño del REQ.

---

## Matriz

| REQ | Criterios AC | User Stories | Fuentes DS | Modelos ML | Arquitectura | Test | DevLog | Release | Estado |
|---|---|---|---|---|---|---|---|---|---|
| [[vault/02_Requirements/Requirements_Detailed\|REQ-001]] · Data Engineering | AC-001.1…AC-001.7 (7) | [[vault/02_Requirements/User_Stories\|18 US]]: US-101–106, US-111–114, US-121a/b–124a/b | [[vault/14_Data_Sources/_index\|DS-01…DS-08]] (las 8) | — (produce `features_escuela`) | [[vault/03_Architecture/Data_Model\|Data_Model]] | ⬜ · US-111: `dbt compile` ✅ · US-103/US-104: estrella Gold completa (dim_tiempo, dim_driver, dim_escuela, dim_municipio, fact_escuela_ciclo + features_escuela), 88 data tests propios en PASS · US-105: interpolación IDW de D6 (aire) real hacia cada escuela (ADR-006), `dbt run`+`dbt test` 53/53 PASS, D5 (agua) sigue en SIN_DATO — bloqueado por DS-06 · 9 sources · US-112: estrella Gold materializada + tests nativos unique/not_null/relationships/accepted_values ✅ · BUG-009 `fixed`: 11 vars de dbt con default permanente (DEC-011), `dbt parse` verde como test de regresión en el job `dbt-contract` de `ci.yml` · US-106: linaje completo fuente→dashboard documentado (`Data_Lineage_US106.md`, **draft**, freeze objetivo 6-sep-2026) · US-122b/US-123b DS-04: fuente alterna a SharePoint encontrada y verificada (`repodatos.atdt.gob.mx`), extractor real agrega subtipo/modalidad, 12 553 440 filas en Bronze · [[vault/06_Quality_Testing/Automated/Great_Expectations_DS04_Sesnsp\|TEST-011 ✅]] (14/15, hallazgo real: 1 fila con conteo negativo) · US-124b: 28 pruebas `pytest` nuevas para extractores/suites GE de DS-04/DS-05, corren offline sin red (326 pruebas totales del repo en verde) · US-122b: cargadores reales Bronze→Postgres para DS-04 (`cargar_bronze_sesnsp_real.py`) y DS-05 (`cargar_bronze_sinaica_real.py`) preparados (idempotentes por snapshot `_source`+`_ingested_at`), pendientes de correr contra el corte real de datos y de actualizar `sources.yml` para dejar los fixtures `_test` · US-113: 9 cubos Gold / 10 dashboards · cobertura explícita · DEC-010 compatible · dbt/contratos ✅ · DS-01: 4 ciclos reales cargados (2021-2022…2024-2025) vía cargador real nuevo (`cargar_bronze_formato911_real.py`), estrella + 8 cubos Gold validados contra Postgres real, 149/149 tests dbt — evidencia del gate que US-113 tenía pendiente de Diana Alvarez (TL C1) | [[vault/_DevLog/2026-08-07-diana-alvarez-data-model-us101\|2026-08-07]] · [[vault/_DevLog/2026-08-14-luis-garcia-us121b-prueba-descarga\|2026-08-14]] · [[vault/_DevLog/2026-08-16-deni-garrido-us111-bronze-silver-cierre|2026-08-16 · US-111]] · [[vault/_DevLog/2026-08-18-luis-garcia-us122b-extractor-sinaica\|2026-08-18 · US-122b]] · [[vault/_DevLog/2026-08-18-diana-alvarez-us103-merge-silver-us111\|2026-08-18 · US-103]] · [[vault/_DevLog/2026-08-19-diana-alvarez-us104-features-escuela\|2026-08-19 · US-104]] · [[vault/_DevLog/2026-08-19-diana-alvarez-us103-gold-estrella\|2026-08-19 · US-103]] · [[vault/_DevLog/2026-08-19-diana-alvarez-us105-idw-calidad-aire\|2026-08-19 · US-105]] · [[vault/_DevLog/2026-08-22-deni-garrido-us112-silver-gold\|2026-08-22 · US-112]] [[vault/_DevLog/2026-08-23-diana-alvarez-us106-linaje-freeze\|2026-08-23 · US-106]] · [[vault/_DevLog/2026-08-23-edgar-bug009-defaults-dbt\|2026-08-23 · BUG-009]] · [[vault/_DevLog/2026-08-24-luis-garcia-us122b-us123b-sesnsp-fuente-alterna\|2026-08-24 · US-122b/US-123b]] · [[vault/_DevLog/2026-08-24-luis-garcia-us124b-fixtures-ds04-ds05\|2026-08-24 · US-124b]] · [[vault/_DevLog/2026-08-23-deni-garrido-us113-cierre-dec010-pipeline\|2026-08-23 · US-113 cierre]] · [[vault/_DevLog/2026-08-28-diana-alvarez-formato911-real-validacion-us113\|2026-08-28 · DS-01/US-113]] · [[vault/_DevLog/2026-09-02-luis-garcia-us122b-cargadores-bronze-real\|2026-09-02 · US-122b]] | US-111 · PR #37 · US-103/US-104 · US-105 · US-112 · PR #72 · US-106 · BUG-009/DEC-011 · US-122b/US-123b · US-124b · US-113 · PR #81 (Draft) · DS-01 · PR #105 (**Merged** 28-ago-2026) · DS-02 · PR #163 (**Merged** 2-sep-2026) | 🟡 En progreso |
| [[vault/02_Requirements/Requirements_Detailed\|REQ-002]] · Frontend BI | AC-002.1…AC-002.6 (6) | [[vault/02_Requirements/User_Stories\|19 US]]: US-201–207, US-211a/b, US-212, US-213, US-214a/b, US-215a/b, US-221–224 | — (vía Gold) | ML-01/02/03 (consumidos en DB-06/09) | [[vault/03_Architecture/Data_Model\|Data_Model]] (cubos) · [[vault/03_Architecture/Frontend_Architecture\|FARO Web]] · [[vault/04_UX_Design/Screen_Specs\|Screen_Specs]] (US-201) · [[vault/04_UX_Design/Cube_Specs_DB03_DB04\|Cube_Specs DB-03/04]] (US-211a) · [[vault/04_UX_Design/Superset_Setup_US202\|Superset Setup]] (US-202) · [[vault/04_UX_Design/Cube_Specs_DB05_DB08\|Cube_Specs DB-05/08]] (US-211b) · [[vault/04_UX_Design/US221_KPIs_Base|US221_KPIs_Base]] (US-221) · [[vault/04_UX_Design/Cube_Specs_DB07\|Cube_Specs DB-07]] (US-222) · [[vault/04_UX_Design/Cube_Specs_DB10\|Cube_Specs DB-10]] (US-223) | `test_semantic_db03_db04` ✅ (US-211a, 27 casos: SIN_DATO≠0, grano, repunteo a cubos US-205) · `test_semantic_db01_db02` ✅ (US-203, 47 casos: filtros AC-002.2 en todos los datasets, componentes aditivos DEC-008, SIN_DATO≠0) · E2E Playwright ✅ (16/16 charts con datos; filtros ciclo/entidad/nivel aplican a todo el tablero; nombres oficiales INEGI de municipio vía `gold.geo_municipio`) · `test_semantic_db05_db08` ✅ (US-211b/US-205/US-213, 53 casos: repunteo a gold.cubo_driver/cubo_pivot, re-escala DB-05 a KPI-07, dashboards DB-05/08 alineados a los nombres reescalados, SIN_DATO≠0, formato largo) · `test_semantic_repunteo_cubos` ✅ (US-205, 34 casos: los 15 datasets virtuales —13 US-205 + 2 DB-07/US-222— no leen gold.fact_*, fuentes en allowlist C1, toda expresión de métrica usa solo columnas expuestas por el dataset) · **BUG-011** ✅ fixed (`sync_semantic_layer.py` lee con `encoding="utf-8"` explícito — ya no requiere `PYTHONUTF8=1` en Windows; `ensure_chart()` compara `datasource_id` y ya no repunta charts homónimos de otro tablero) · `test_kpis_us221` ✅ (US-221, 6 casos: KPI-01/02/03/04/08 vs fixtures, SIN_DATO≠0, umbral 0.6, SCOPE_ENTIDADES) · `test_semantic_db06_db09` ✅ (US-204, 48 casos: grano escuela DEC-010, SIN_DATO≠0, ML por LEFT JOIN con llave completa + modelo='ML-01', umbral 0.6, componentes aditivos, repunteo a cubos US-205) · `test_db07_calidad_cobertura` ✅ (US-222, 7 casos: KPI-05/06 SUM/SUM, SIN_DATO≠0, grano sin duplicados en mapa, geometría, coherencia agregado vs detalle) · `test_semantic_db05_db08` ampliado ✅ (US-213, 51 casos totales: +21 nuevas — layout de tabs vía `_layout_tabs()`, dashboard db05 declarativo de 6 tabs D1-D6 con adhoc_filters por driver y nota MARKDOWN de fuente, dashboard db08 con guarda de doble conteo del pivote) · DB-05 (36 charts, 6 tabs) y DB-08 (5 charts, pivote libre) sincronizados y validados contra Gold real (dbt, no mock) ✅ · hallazgo de `gold.dim_driver` (Célula 1): condición de carrera en `dbt run` corregida como **BUG-021**; esquema desincronizado corregido por Diana como **BUG-022** (fix en `main`; fila del registro restaurada en este PR) · `test_db10_monitor_pipeline` ✅ (US-223, 5 casos: 8 fuentes del catálogo, SIN_DATO≠0, SUM(filas) excluye SIN_DATO, grano sin duplicados) | [[vault/_DevLog/2026-08-07-manuel-serrania-us-201\|2026-08-07]] · US-201 · [[vault/_DevLog/2026-08-13-manuel-serrania-screenspecs-cubos\|2026-08-13]] · [[vault/_DevLog/2026-08-14-marina-garcia-cubos-db03-db04\|2026-08-14]] · US-211a · [[vault/_DevLog/2026-08-15-manuel-serrania-kpis-db03-ratificacion-join\|2026-08-15]] · [[vault/_DevLog/2026-08-16-manuel-serrania-us202-superset\|2026-08-16]] · US-202 · [[vault/_DevLog/2026-08-21-manuel-serrania-us203-tableros-db01-db02\|2026-08-21]] · US-203 · [[vault/_DevLog/2026-08-21-marina-garcia-cierre-us211a\|2026-08-21]] · US-211a ✅ cerrada · [[vault/_DevLog/2026-08-24-marina-garcia-us212-db03-db04\|2026-08-24]] · US-212 (DB-03/DB-04 sobre mock; pendiente validar con Gold real) · [[vault/_DevLog/2026-08-27-marina-garcia-pipeline-local-us212\|2026-08-27]] · US-212 revalidada sobre Gold real (24/24 charts; bloques ML pendientes por BUG-013) · [[vault/_DevLog/2026-08-22-handoff-us203-tableros-superset\|2026-08-22]] · US-203 depuración E2E · [[vault/_DevLog/2026-08-22-handoff-us203-verificacion-e2e-playwright\|2026-08-22]] · verificación E2E + diagnóstico · [[vault/_DevLog/2026-08-22-manuel-serrania-us203-filtros-nombres-reales\|2026-08-22]] · US-203 filtros AC-002.2 completos + nombres reales · [[vault/_DevLog/2026-08-22-monserrat-miranda-us211b-cubos-db05-db08\|2026-08-22]] · US-211b · [[vault/_DevLog/2026-08-26-oscar-quiroz-us221-kpis-base\|2026-08-26]] · US-221 · [[vault/_DevLog/2026-08-27-manuel-serrania-us204-db06-db09\|2026-08-27]] · US-204 (DB-06/DB-09, validación en vivo 15/15 OK + AC-002.2; hallazgo preexistente `dim_driver` que bloquea DB-05/08 escalado a C1) · [[vault/_DevLog/2026-08-28-monserrat-miranda-us213-db05-db08-dashboards\|2026-08-28]] · US-213 (DB-05 6 tabs + DB-08 explorador, tabs/markdown en `sync_semantic_layer.py` aprobados por Manuel, validado contra Gold real; BUG-021/BUG-022 con Diana — threads ya corregido, fila de BUG-022 restaurada en Bug_Register.md) | ⬜ | 🟡 En progreso |
| [[vault/02_Requirements/Requirements_Detailed\|REQ-003]] · Modelos ML | AC-003.1…AC-003.6 (6) | [[vault/02_Requirements/User_Stories\|10 US]]: US-301–303, US-311–313, US-321, US-322, US-324, US-325 (+apoyo US-412/415/416) | DS-01…08 (vía `features_escuela`) | [[vault/15_ML_Models/_index\|ML-01, ML-02, ML-03]] | [[vault/03_Architecture/Data_Model\|Data_Model]] · [[vault/03_Architecture/API_Specification\|API_Spec]] | [[vault/06_Quality_Testing/Automated/Particion_Temporal_ML01\|TEST-003 ✅]] (US-311, AC-003.3) · [[vault/15_ML_Models/Indice_Riesgo_ML01\|TEST-004 ✅]] (US-311) · [[vault/15_ML_Models/ML01_Entrenamiento\|TEST-005 ✅]] (ML-01 entrenado) · [[vault/15_ML_Models/Publicacion_Gold\|TEST-006 ✅]] (US-313) · [[vault/06_Quality_Testing/Automated/Evaluacion_Modelos\|TEST-007 ✅]] (US-312) | [[vault/_DevLog/2026-08-08-hector-morales-fixture-particion-temporal\|2026-08-08]] · [[vault/_DevLog/2026-08-11-hector-morales-indice-riesgo-ml01\|2026-08-11]] · [[vault/_DevLog/2026-08-13-hector-morales-entrenamiento-ml01\|2026-08-13]] · [[vault/_DevLog/2026-08-14-hector-morales-publicacion-gold\|2026-08-14]] · [[vault/_DevLog/2026-08-18-hector-morales-evaluacion-us312\|2026-08-18]] · [[vault/_DevLog/2026-08-23-diana-alvarez-dec010-grano-dual-predicciones\|2026-08-23 · DEC-010]] · [[vault/_DevLog/2026-08-27-carlos-mayorga-us324\|2026-08-27 · US-324]] · [[vault/_DevLog/2026-08-29-diana-alvarez-us325-cve-mun-features-escuela\|2026-08-29 · US-325]] | DEC-010 | 🟡 En progreso |
| [[vault/02_Requirements/Requirements_Detailed\|REQ-004]] · Backend/API/Auth | AC-004.1…AC-004.6 (6) | [[vault/02_Requirements/User_Stories\|14 US]]: US-401–405, US-411–416, US-421–423 | — | ML-01/02/03 (expuestos vía API) | [[vault/03_Architecture/API_Specification\|API_Spec]] · [[vault/03_Architecture/Data_Model\|Data_Model]] · [[vault/03_Architecture/ADRs/ADR-004-autenticacion-oauth2-jwt\|ADR-004]] (auth) | `test_api_contract` ✅ (US-401/US-411/US-412/US-416, 27) · `test_auth_jwt` ✅ (US-402, 15) · `test_schemas_ml` ✅ (US-415, 11) · `test_cache_predicciones` ✅ (US-416, 8) · `test_oauth_google` ✅ (US-402 e2e + `state` CSRF, 18) · `test_rbac` ✅ (US-403, 10) · `test_hardening` ✅ (US-404, 7) | [[vault/_DevLog/2026-08-11-christian-ruiz-us401-contrato-api\|2026-08-11]] · US-401 · [[vault/_DevLog/2026-08-17-christian-ruiz-us402-oauth-jwt\|2026-08-17]] · US-402 · [[vault/_DevLog/2026-08-30-luis-tellez-oauth-creds-deploy\|2026-08-30]] · US-402 (deploy/C5: credenciales OAuth Google cableadas en Cloud Run, login listo para C4) · [[vault/_DevLog/2026-08-20-karla-monter-us411-endpoints-gold\|2026-08-20]] · US-411 · [[vault/_DevLog/2026-08-26-juan-macias-us415-contrato-modelos\|2026-08-26]] · US-415 · [[vault/_DevLog/2026-08-26-juan-macias-us412-repositorio-modelos-bug010\|2026-08-26]] · US-412/BUG-010 · [[vault/_DevLog/2026-08-27-juan-macias-us416-cache-timeouts\|2026-08-27]] · US-416 · [[vault/_DevLog/2026-09-02-christian-ruiz-us402-cierre-oauth-e2e\|2026-09-02]] · US-402/403/404 (cierre e2e de OAuth, `state` CSRF y revisión humana de seguridad) | PR #95 (US-412/415) | 🟡 En progreso |
| [[vault/02_Requirements/Requirements_Detailed\|REQ-005]] · Deploy GCP | AC-005.1…AC-005.5 (5) | [[vault/02_Requirements/User_Stories\|13 US]]: US-501, US-502, US-504, US-505, US-522a/b/c, US-524a/b/c, US-525a/b/c | — | — | [[vault/08_CICD_DevOps/Cloud_Run_Deploy\|Cloud Run Deploy ✅]] · [[vault/03_Architecture/ADRs/ADR-008-contenerizacion-airflow-sqlalchemy\|ADR-008]] | ⬜ | [[vault/_DevLog/2026-08-09-luis-tellez-us501-cloud-run-deploy\|2026-08-09]] · [[vault/_DevLog/2026-08-15-luis-tellez-us502-docker-compose-ml-services\|2026-08-15]] · US-502 · [[vault/_DevLog/2026-08-25-alejandro-velazquez-us522a\|2026-08-25]] · [[vault/_DevLog/2026-08-25-edgar-us522b-contenerizar-airflow\|2026-08-25]] · [[vault/_DevLog/2026-08-27-alejandro-velazquez-us524a\|2026-08-27]] · [[vault/_DevLog/2026-08-29-luis-tellez-us502-compose-sin-container-name\|2026-08-29 · US-502]] · [[vault/_DevLog/2026-08-29-luis-tellez-us504-provision-gcp-fase1\|2026-08-29 · US-504]] · [[vault/_DevLog/2026-08-29-luis-tellez-us505-fase2-gold-cloudsql-redeploy\|2026-08-29 · US-505 · BUG-020 prod ✅]] · [[vault/_DevLog/2026-08-29-luis-tellez-security-docs-cis-usids-genkeys\|2026-08-29 · docs seguridad CIS/US IDs]] · [[vault/_DevLog/2026-08-29-luis-tellez-bug025-redeploy-agente-prod\|2026-08-29 · BUG-025 deploy prod]] · [[vault/_DevLog/2026-08-30-luis-tellez-cierre-s4-devops\|2026-08-30 · cierre S4 · prod ✅]] · [[vault/_DevLog/2026-08-30-luis-tellez-oauth-creds-deploy\|2026-08-30 · credenciales OAuth Google en Cloud Run, desbloquea US-402]] | ⬜ | 🟡 En progreso |
| [[vault/02_Requirements/Requirements_Detailed\|REQ-006]] · Agente | AC-006.1…AC-006.4 (4) | [[vault/02_Requirements/User_Stories\|4 US]]: US-304a, US-304b, US-305, US-323 | — (vía Gold) | — (RAG sobre Gold) | [[vault/03_Architecture/API_Specification\|API_Spec]] (`/agente`) · [[vault/03_Architecture/Data_Model\|Data_Model]] | `test_agente_recuperacion` ✅ (US-304b, 3) · `test_agente_evaluacion` ✅ (US-323, 2) | [[vault/_DevLog/2026-08-26-andres-gonzalez-plan-registry-guardrails\|2026-08-26]] · [[vault/_DevLog/2026-08-26-andres-gonzalez-us305-apptest\|2026-08-26]] · [[vault/_DevLog/2026-08-27-andres-gonzalez-us305-jwt-client\|2026-08-27]] · [[vault/_DevLog/2026-08-27-carlos-mayorga-us304b-us323\|2026-08-27]] | ⬜ | 🟡 En progreso |
| [[vault/02_Requirements/Requirements_Detailed\|REQ-007]] · Equipo/Git/Docs | AC-007.1…AC-007.5 (5) | [[vault/02_Requirements/User_Stories\|13 US]]: US-001–006, US-503, US-521a/b/c, US-523a/b/c | — | — | [[AGENTS\|AGENTS.md]] · [[vault/_Meta/Vault_Rules\|vault]] · [[vault/13_Reports/PM_Dashboard_Spec\|Tablero PM]] | [[vault/06_Quality_Testing/Automated/_index\|TEST-002 ✅]] | [[vault/_DevLog/2026-08-05-edgar-tablero-control-pm-v2\|2026-08-05]] · [[vault/_DevLog/2026-08-06-edgar-directorio-github-codeowners\|2026-08-06]] · [[vault/_DevLog/2026-08-12-alejandro-velazquez-mendoza\|2026-08-12]] · [[vault/_DevLog/2026-08-15-luis-tellez-us503-ci-pipeline\|2026-08-15]] · US-503 · [[vault/_DevLog/2026-08-25-alejandro-velazquez-us522a\|2026-08-25]] · [[vault/_DevLog/2026-08-31-edgar-jimenez-us523b-ci-quality-gate|2026-08-31 · US-523b]] · [[vault/_DevLog/2026-09-01-edgar-coronel-rama-fija-consolidacion-vault|2026-09-01 · US-001]]| ⬜ | 🟡 En progreso |

## Reapertura S7 — evaluación del profesor del 9-sep

> `DEC-022` reabre los siete REQ. Las filas históricas anteriores conservan evidencia S1–S6; esta
> tabla registra el trabajo adicional y queda en progreso hasta que cada equipo agregue prueba,
> DevLog, PR y revisión desplegada. → [[vault/13_Reports/Revision_Profesor_2026-09-09]]

| REQ | Historias S7 | Criterio de recuperación | Test / DevLog / Release | Estado |
|---|---|---|---|---|
| `REQ-001` | `US-601` | Equipo 1: explicación verificable Bronze→Silver→Gold, filtros, cubos, ER y componentes | ⬜ / ⬜ / ⬜ | 🟡 En progreso |
| `REQ-002` | `US-621`, `US-641` | Equipos 3 y 5: UX, gráficas, storytelling y frontend aceptados e integrados | ⬜ / ⬜ / ⬜ | 🟡 En progreso |
| `REQ-003` | `US-631` | Equipo 4: ML-03 funcional y explicación completa de tres modelos | [[vault/15_ML_Models/ML03_Explicacion_US631]] / [[vault/_DevLog/2026-09-10-estefany-hernandez-explicacion-ml03-us631]] / ⬜ | 🟡 En progreso |
| `REQ-004` | `US-601`, `US-611`, `US-641` | Componentes e integraciones backend/chat/frontend revalidados | ⬜ / ⬜ / ⬜ | 🟡 En progreso |
| `REQ-005` | `US-641` | Equipo 5: candidata desplegada, observable y recuperable | ⬜ / [[vault/_DevLog/2026-09-10-alejandro-velazquez-observabilidad-redeploy-agente|2026-09-10]] / ⬜ | 🟡 En progreso |
| `REQ-006` | `US-611` | Equipo 2: chat natural, contextual, seguro y funcional en candidata | `test_agente_prompt` ✅ / [[vault/_DevLog/2026-09-10-andres-gonzalez-historial-en-prompt-us305]] / ⬜ | 🟡 En progreso |
| `REQ-007` | `US-651`, `US-654`, `US-006` | Equipo 6/PO: QA, trazabilidad, go/no-go y entrega | ⬜ / [[vault/_DevLog/2026-09-10-edgar-coronel-handoff-reapertura-s7]] · [[vault/_DevLog/2026-09-11-edgar-coronel-revision-prs-s7-gate-jueves]] / ⬜ | 🟡 En progreso |

## Evidencia incremental — 2026-08-26

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-302`, `US-303` | ML-02/MLflow/Gold/guardarraíles: 71 pruebas enfocadas ✅ · Registry local MLflow 3.15.1: `ML02_DriverClasificador` v1 ✅ | [[vault/_DevLog/2026-08-26-andres-gonzalez-plan-registry-guardrails]] | 🟡 En progreso |
| `REQ-006` | `US-304a`, `US-305` | Guardarraíles y prompt ✅ · cliente HTTP: 8 pruebas ✅ · flujo Streamlit persistente: historial, SQL y rechazo ✅ · Ruff ✅ | [[vault/_DevLog/2026-08-26-andres-gonzalez-plan-registry-guardrails]] · [[vault/_DevLog/2026-08-26-andres-gonzalez-us305-apptest]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-27

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-006` | `US-305` | Cliente y widget preparados para propagar `Authorization: Bearer` desde la sesión: 9 pruebas enfocadas ✅ · Ruff ✅ | [[vault/_DevLog/2026-08-27-andres-gonzalez-us305-jwt-client]] | 🟡 En progreso |
| `REQ-003` | `US-311`, `US-313` | BUG-015: la cobertura de drivers se evalúa **por ventana** de entrenamiento, no global — un driver con datos sólo en el ciclo reciente (D6, IDW de US-105) quedaba vacío en el tramo de entrenamiento y rompía el binning ✅ · `--ventanas` se calcula desde los ciclos disponibles ✅ · 3 pruebas de regresión nuevas · suite 481 ✅ · Ruff y `vault_lint` ✅ | [[vault/_DevLog/2026-08-27-hector-morales-bug015-segunda-vuelta]] | 🟡 En progreso |

## Evidencia incremental — 2026-09-03

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-006` | `US-305`, `US-611` | Frontend de Fase 3 listo: cliente SSE con `httpx.stream`, historial de hasta 10 turnos previos, campos imprimibles de máximo 500 caracteres y exclusión de la pregunta actual; 16 pruebas frontend en verde y Ruff limpio. Cierre end-to-end pendiente del endpoint `/consulta/stream`, OpenAPI y prueba real en el PR del dueño de API | [[vault/_DevLog/2026-09-10-andres-gonzalez-fase3-streaming-us305]] | 🟡 En progreso |
| `REQ-006` | `US-305`, `US-611` | Correcciones de revisión del PR #313: fallback explícito al endpoint síncrono `/api/v1/agente/consulta` ante `404` de `/consulta/stream`, reenvío de `historial` y corrección de `preparar_historial()` para conservar los 10 turnos más recientes y excluir la pregunta actual; 17 pruebas frontend en verde y `vault_lint` limpio | [[vault/_DevLog/2026-09-11-andres-gonzalez-fallback-streaming-us305]] | 🟡 En revisión; pendiente aprobación explícita de Diana Alvarez |
| `REQ-006` | `US-304a`, `US-304b`, `US-305` | Contrato estricto del agente confirmado: el cliente del chat envía solo `pregunta`; OAuth deja `access_token` en sesión para el Bearer; diagnósticos estáticos de `src/agente/**` y `src/modelos/**` sin errores. Sonda `src.agente.verificar_e2e` preparada para validar lectura RAG y rechazo de escritura autenticados; 3 pruebas offline en verde con Python 3.11. E2E Docker/Cloud Run pendiente de C5 porque la imagen API no instala dependencias RAG de C3. | [[vault/_DevLog/2026-09-03-andres-gonzalez-validacion-c3-e2e]] | 🟡 En progreso |
| `REQ-003` | `US-311`, `US-313` | Primera corrida real de ML-01 sobre `gold.features_escuela`: 45 249 predicciones ✅ · filas sin ningún driver apartadas de ML-02 conservando su predicción (BUG-016) ✅ · guarda de escala que detiene un `indice_riesgo` saturado (BUG-017) ✅ · BUG-018 reproducido en ML-02 y documentado ⬜ · 11 pruebas nuevas · suite 492 ✅ | [[vault/_DevLog/2026-08-27-hector-morales-bug016-018-gold-real]] | 🟡 En progreso |
| `REQ-003` | `US-104`, `US-311`, `US-313` | Unidad de `target_variacion_matricula` confirmada como diferencia absoluta de alumnos · **BUG-019**: la misma columna se produce en dos unidades según el grano y ambas llegan a `gold.predicciones.valor` ⬜ · **ADR-007** propuesto con evidencia cuantificada (correlación 0.70 entre variación absoluta y tamaño de escuela) ⬜ · guarda de escala vigente · suite 497 ✅ | [[vault/_DevLog/2026-08-28-hector-morales-adr007-unidad-target]] | 🟡 En progreso |
| `REQ-006` | `US-304b`, `US-323` | Capa RAG implementada e indexador ChromaDB creado ✅ · Set de evaluación de 20 preguntas con clasificación probado ✅ | [[vault/_DevLog/2026-08-27-carlos-mayorga-us304b-us323]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-27 · avance independiente C3

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-302`, `US-303` | Target ML-02 validado antes de entrenar: 13 pruebas ✅ · verificación y CLI del Registry: 14 pruebas ✅ | [[vault/_DevLog/2026-08-27-andres-gonzalez-avance-independiente-c3]] | 🟡 En progreso |
| `REQ-006` | `US-304a`, `US-305` | Orquestación segura inyectable: 19 pruebas con guardarraíles ✅ · cliente JWT distingue 401/403: 11 pruebas y 1 omitida ✅ | [[vault/_DevLog/2026-08-27-andres-gonzalez-avance-independiente-c3]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-28

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-001` | `US-121a`, `US-122a`, `US-123a`, `US-124a` | DS-06 (CONAGUA) destrabado: endpoint POST real confirmado (`mapa.php`, 180 presas con volumen NAME/NAMO), reemplaza el placeholder `SOURCE_URL="PENDIENTE-CONFIRMAR"` en `extractor_conagua.py` · DS-08 (CONAPO) procesado desde archivo local (sin URL de descarga estable), clave municipal corregida a 5 dígitos en `extractor_conapo.py` · Great Expectations: `suite_ds06_conagua` 7/7 ✅ (180 filas) y `suite_ds08_conapo` 7/7 ✅ (252,450 filas) · Fixtures CI: `bronze_ds06_conagua_sample.csv` (180 filas) y `bronze_ds08_conapo_sample.csv` (500 filas) | [[vault/_DevLog/2026-08-28-emilio-galnares-us121a-ds06-ds08]] | 🟡 En progreso |
| `REQ-003` | `US-302` | BUG-018 corregido: cobertura evaluada por ventana y predicción/SHAP alineados con `feature_names_in_` ✅ · ML-02 16 pruebas ✅ · Ruff ✅ | [[vault/_DevLog/2026-08-28-andres-gonzalez-bug018-ml02-cobertura]] | 🟡 En progreso |
| `REQ-003` | `US-303` | Verificador del Registry rechaza versiones nulas o no numéricas con error accionable ✅ · suite enfocada 15 pruebas ✅ | [[vault/_DevLog/2026-08-28-andres-gonzalez-us303-version-registry]] | 🟡 En progreso |

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-302` | `driver_dominante` real en `gold.features_escuela` (Célula 1): CTE argmax + desempate D1>D2>D3>D4>D5>D6 · 5 pruebas dbt nuevas ✅ · prueba de paridad SQL vs proxy Python ✅ · contrato y fixture actualizados · `dbt build --target dev` 173 pass · `pytest tests/ -q` 467 pass | [[vault/_DevLog/2026-08-28-diana-alvarez-driver-dominante-us302]] | 🟡 En progreso |
| `REQ-006` | `US-304a`, `US-304b`, `US-305`, `US-323` | RAG endurecido e integrado con guardarraíles ✅ · indexación idempotente y observable ✅ · 20 casos recorren el flujo completo simulado ✅ · 32 pruebas enfocadas | [[vault/_DevLog/2026-08-28-andres-gonzalez-integracion-rag-evaluacion]] | 🟡 En progreso |
| `REQ-006` | `US-304a`, `US-305` | BUG-024: `SELECT INTO` rechazado antes del ejecutor ✅ · sin coincidencias RAG diferenciado de caída operativa ✅ · 32 pruebas enfocadas · suite local 526 pass, 0 skips ✅ | [[vault/_DevLog/2026-08-28-andres-gonzalez-bug024-rag-empty]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-28 · higiene del vault y CI

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-503`, `US-004` | Guardia anti-mojibake en `vault_lint.py`, dentro del check requerido: 11 casos de validación ✅ · **BUG-014** cerrado con regresión versionada, `.github/scripts/probar_verificar_plantilla.sh` — 7 casos contra la plantilla real ✅ · `pytest tests/ -q` 467 ✅ | [[vault/_DevLog/2026-08-28-edgar-mojibake-higiene-vault]] | 🟡 En progreso |

---
---

## Leyenda de estado

- 📋 **Planeado** — cobertura de planeación completa (AC, US y arquitectura definidos); **ejecución no
  iniciada** (Test/DevLog/Release en ⬜).
- 🟡 **En progreso** — hay commits/PRs abiertos pero aún sin Test o DevLog completo.
- 🟢 **Done** — Test verde + DevLog + quedó en un Release.
- 🔴 **No iniciado** · ⚫ **Archivado/deprecado**.
- ⚠️ marca un **hueco de planeación** en alguna columna (ver abajo).

---

## Estado del proyecto

> **Transcripción del tablero, con su fecha de corte.** Estas cifras se **copian a mano** desde
> `vault/13_Reports/data/pm-dashboard.json`, que sí genera `scripts/generate_pm_dashboard.py` a
> partir de [[vault/12_Roadmap_Sprints/Execution_Status]]. **La cifra viva es la del tablero, no
> ésta**: si las dos no coinciden, manda el tablero.
>
> La primera redacción de esta nota decía "derivado, no capturado… no se editan a mano", y era falsa
> —lo señaló Marina García al revisar el PR—: el generador **lee** esta matriz para el hash de fuentes
> y sólo **escribe** `pm-dashboard.json`, `pm-dashboard-history.json` y `TABLERO_CONTROL_PM.html`.
> Declararlo derivado sin serlo es el mismo defecto que este bloque venía a corregir, un grado peor:
> una nota que dice que el dato se mantiene solo apaga la sospecha del siguiente lector.
>
> Que el generador escriba este bloque entre marcadores queda como follow-up **post-freeze**, no
> ahora: haría que cada refresco automático del tablero tocara la matriz, y hoy toda evidencia
> incremental se agrega al final de este mismo archivo — sería una máquina de conflictos a tres días
> de la demo. Anotado en el punto 7 de [[vault/_Meta/Vault_Steward]].

**Corte: 2026-09-06 · 89 historias en alcance (92 en catálogo − 3 recortadas por `DEC-014`)**

> Sin hash de commit **a propósito**: el tablero se regenera en cada merge a `main`, así que
> cualquier hash queda obsoleto antes de que alguien lea esta línea. Lo que importa es que estas
> cifras salen del snapshot de este PR y **manda el tablero**.

| REQ | Módulo | US hechas | Avance | Puntos asegurados |
|---|---|---|---|---|
| `REQ-001` | Data Engineering | 16 / 18 | 92.5 % | **2.31** / 2.5 |
| `REQ-002` | Frontend BI | 15 / 20 | 86.5 % | **2.16** / 2.5 |
| `REQ-003` | Modelos ML | 3 / 10 | 72.5 % | **1.09** / 1.5 |
| `REQ-004` | Backend, API y Auth | 7 / 12 | 75.4 % | **1.13** / 1.5 |
| `REQ-005` | Despliegue GCP | 2 / 12 | 47.1 % | **0.47** / 1.0 |
| `REQ-006` | Agente conversacional | 1 / 4 | 73.8 % | **0.37** / 0.5 |
| `REQ-007` | Equipo, Git y documentación | 8 / 13 | 81.9 % | **0.41** / 0.5 |
| | **Total** | **52 / 89** | **78.1 %** | **7.94 / 10** |

| Métrica | Valor |
|---|---|
| Historias mapeadas | 89 / 89 en alcance · 3 recortadas (`US-413`, `US-414`, `US-525a`) |
| Estados | 52 `done` · 21 `in_review` · 11 `in_progress` · 5 `planned` · 0 `blocked` |
| REQ con planeación completa | 7 / 7 — `System_Design.md` cerró el hueco de `REQ-005` |
| Readiness de la demo | 75 / 100 · **2 gates en rojo** |
| Gates en rojo | Tres modelos registrados (`REQ-003`) · Dry-run y contingencia (`US-006`) |
| Bloqueos abiertos | 1 — `BLOCK-005` (cubos no materializados en Cloud SQL) |

> **Lectura:** los dos módulos de mayor peso —Data Engineering y Frontend BI— están sobre 89 % y
> aportan **4.59 de los 5.0 puntos** que valen juntos. El riesgo no está donde hay más historias
> abiertas, sino en los **dos gates en rojo**: sin los tres modelos registrados se cae un cuarto del
> readiness, y `US-006` (ensayo de la demo) no ha arrancado a cinco días de presentar. `REQ-005` es
> el módulo con menor avance (47.1 %), y es también donde vive el hueco de desplegar FARO Web.

## Evidencia incremental — 2026-08-28 · etiqueta real y verificación E2E

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-302`, `US-313` | Filtro de ML-02 usa `driver_dominante` real como autoridad cuando existe: cierra el hueco de inferir cobertura por valor (BUG-016) ✅ · cadena ML-01 → filtro → ML-02 verificada con target real, F1 0.633 ✅ · 2 pruebas nuevas · suite 500 ✅ | [[vault/_DevLog/2026-08-28-hector-morales-filtro-etiqueta-real]] | 🟡 En progreso |
| `REQ-004` | `US-401`, `US-411` | **BUG-020**: en la URL pública toda ruta con base de datos responde HTTP 500 (`/predicciones`, `/predicciones/batch`, `/escuelas`); `/health` responde 200 y nunca se devuelve 401, así que el fallo precede a la validación de auth ⬜ | [[vault/_DevLog/2026-08-28-hector-morales-filtro-etiqueta-real]] | 🔴 Bloqueante |

## Evidencia incremental — 2026-08-28 · drivers en el artefacto publicado

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-311`, `US-312` | Drivers usados/excluidos publicados en `Evaluacion_Modelos.md` §5 y §5.1 y registrados en MLflow (`cobertura_drivers`, `drivers_sin_datos` por ventana) ✅ · **BUG-023**: el reporte no podía generarse con un driver excluido porque predecía con los 6 ✅ · §5 acota la tabla a la corrida que la generó y declara el estado real de D5 en Gold ✅ · registro MLflow con pruebas (doble inyectado, corre sin `mlflow` en CI) ✅ · 9 pruebas nuevas · suite 510 ✅ | [[vault/_DevLog/2026-08-28-hector-morales-drivers-en-evaluacion]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-28 · `cve_mun` en el contrato

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-325`, `US-311`, `US-313` | `agregar_a_municipio_nivel()` soporta `cve_mun` en el contrato sin colisión de sufijos y detecta el faltante con el indicador del merge ✅ · espejo `FeaturesEscuela` acepta el contrato antes y después del cambio (`extra=forbid`) ✅ · 4 pruebas nuevas verificadas contra regresión · suite 521 ✅ | [[vault/_DevLog/2026-08-28-hector-morales-cve-mun-contrato]] | 🟡 En progreso |
## Evidencia incremental — 2026-08-28 · reconciliación integral de estatus

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` | `US-113`, `US-121a`–`US-124a` | US-113: PR #81 + PR #105, estrella y 8 cubos sobre 4 ciclos reales, 149/149 ✅ · BUG-021/PR #115 corrige orden dbt ✅ · PR #107 abierto para DS-06/DS-08 | Deni/Diana validan DB-10 o el PM aprueba excepción; Diana revisa PR #107 | 🟡 En revisión |
| `REQ-002` | `US-204`, `US-213`, `US-221` | PR #100 mergeado: DB-06/DB-09, 50 pruebas y 15/15 charts ✅ · PR #114 DB-05/08 abierto · PR #106 gráficos KPI abierto | Manuel valida datos ML reales y revisa #106/#114 | 🟡 En revisión |
| `REQ-003` | `US-302`, `US-313`, `US-323`, `US-324` | PR #113/#116/#117 estabilizan ML-02 ✅ · US-323 pasa a `done` con PR #108 y documento `approved` ✅ · US-324 en revisión por fichas PR #110 | Andrés valida Gold/Registry/SHAP; Héctor/Diana ejecutan publicación real; dueños de modelos revisan fichas | 🟡 En revisión |
| `REQ-004` | `US-403`, `US-411`, `US-412`, `US-415`, `US-416` | US-415 pasa a `done` con PR #95 ✅ (contrato Pydantic, sin superficie desplegada) · US-403 PR #97 y US-416 PR #101 pasan a revisión · **BUG-020 mantiene abiertas US-411 y US-412**: ambas entregan rutas HTTP que responden 500 en producción, y el criterio adoptado el 28-ago las trata igual · autenticación verificada viva (`/auth/login` 302, `/auth/me` 401), así que US-402 no queda tocada por BUG-020 | Christian ratifica seguridad/cache; Christian+Luis corrigen BUG-020; Karla valida endpoints | 🔴 Bloqueante |
| `REQ-005` | `US-522a/b/c`, `US-524a` | **Fases 0/1/2 completas y mergeadas** (#138 US-502 · #141 US-504 · #144 US-505): GCP aprovisionado (VPC privada + connector, Cloud SQL IP privada + backups/PITR, Secret Manager, SA de mínimo privilegio, audit logs), Gold poblado en Cloud SQL y `faro-api` redesplegado por IP privada con secretos en Secret Manager → **BUG-020 curado en PROD** y cerrada la violación de `JWT_SECRET_KEY` en texto plano · #146 docs seguridad (CIS v8 / US IDs / generate-keys) mergeado · #148 **BUG-025 desplegado** (agente ya no es stub, degrada seguro) · **Validación prod 2026-08-30**: `/health` 200, `/escuelas` 200/25, `/predicciones/{cct}` 404 estructurado, `/agente/consulta` 200 degradado → **RISK-001 mitigado de facto** · PR #87 Airflow y PR #102 monitoreo siguen **abiertos (CONFLICTING)** | C5 (Luis) revisa/destraba #87 (renombrar ADR-007→ADR-008 + quitar fixture binario) y #102; pendientes gated: Fase 3 (ecosistema+IAP), CD `deploy.yml`+restore, credenciales OAuth para el flip de lectura | 🟡 En revisión |
| `REQ-006` | `US-304a`, `US-304b`, `US-305`, `US-323` | RAG y evaluación PR #108 ✅ · US-323 `done` · guardarraíles/widget/JWT mergeados · rama de integración RAG aún sin merge | Andrés/Carlos integran y ejecutan E2E; Christian entrega login y corrige BUG-020 | 🟡 En revisión |
| `REQ-007` | `US-004`, `US-521c`, `US-523a` | Reconciliación de 91 US ejecutada · US-523a pasa a `done` por PR #93/documento `approved` ✅ · US-521c conserva DevLog no filed | Edward regulariza DevLog; Edgar cierra acuerdos del reporte y regenera tablero | 🟡 En revisión |

## Evidencia incremental — 2026-08-28 · revisión de `main` y cobertura de fixtures (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-001` · `REQ-003` | `US-104`, `US-113`, `US-313` | **BUG-026** resuelto por **PR #129** (Diana Alvarez) ✅ · causa precisada por ella: `silver.matricula` nunca lee de `bronze.formato911_historico`, así que el hueco es aritmético en la tabla que sí está en el linaje — `con_target` sacrifica el primer ciclo, luego hacen falta 4 crudos para los 3 que exige `ventanas_posibles()` · **verificado corriéndolo**: `features_escuela` 1→3 ciclos, 60/60 CCT cruzan el catálogo, `--desde-gold` entrena ML-01 (MAE 12.2252) y se detiene en la guarda de BUG-017, no por falta de ciclos · generador reproducible (MD5) y carga idempotente | [[vault/_DevLog/2026-08-29-marina-garcia-correcciones-y-revision-pr129]] | ✅ Desbloquea `US-212` |
| `REQ-003` | `US-311`, `US-313` | **BUG-013** replanteado a `parcial`: mitad C3 ✅ (`--desde-gold`, Héctor) y mitad C1 ✅ con datos reales (4 ciclos, Diana, 149/149); lo que queda es reproducibilidad y pasa a BUG-026 · registrado además que el error de sklearn abierto en el DevLog de Diana **ya es BUG-015** ⚠️ **corregido el 29-ago**: el fix no es `4f22bd8` (primer intento, cobertura global, no sirvió) sino **`f906a7d`** (cobertura por ventana) — `d6_aire` tenía datos globalmente pero estaba vacío en el tramo de entrenamiento; cadena verificada por Héctor sobre `main` con target real, F1 0.633 y 78/80 predicciones con recomendación ✅ | [[vault/_DevLog/2026-08-28-marina-garcia-revision-main-bugs]] | 🟡 En progreso |
| `REQ-002` | `US-212`, `US-221` | `test_semantic_db03_db04` ✅ 29/29 sobre `main` de hoy · ningún archivo canónico de DB-03/DB-04 tocado en 123 commits · **cero colisiones de nombre** entre US-221 y DB-03/DB-04 · **BUG-027** → **superseded** por decisión de convención de Manuel Serranía (follow-up de US-221): se borran los `kpi_*.sql` y las tarjetas se remapean a los datasets canónicos, así que corregir los `sql_ref` sería trabajo sobre archivos que desaparecen. **Sobrevive el hallazgo de por qué CI no lo veía** —`test_kpis_us221` codifica `SQL_DIR` a mano y nunca lee `sql_ref`—: de ahí nace la guarda antiduplicación, aplicada por Oscar (patch de Manuel, verificado contra `main` con PR #134/US-205 ya integrado; `test_kpis_us221` reescrito a 4 casos de guarda estática, sin base de datos) ✅ | [[vault/_DevLog/2026-08-28-marina-garcia-revision-main-bugs]] · [[vault/_DevLog/2026-08-30-oscar-quiroz-us221-consolidacion-kpis|2026-08-30]] | ✅ |
| `REQ-002` · `REQ-003` | `US-212`, `US-104` | **BUG-017 / ADR-007** afectan directamente a DB-03/DB-04: con el target en alumnos absolutos y la sigmoide calibrada sobre fracción, `en_riesgo` (DEC-006, umbral 0.6) marcaría el 100 % de las escuelas y `pct_escuelas_en_riesgo` diría 100 % ⬜ · **incorporada como ratificadora** por PR #128 (Héctor Morales), junto con el costo asimétrico señalado ✅ · argumento que aporta Héctor y conviene registrar: **DEC-006 ya dice "`indice_riesgo` ≥ 0.6 ↔ pérdida de ~5 % de matrícula"**, y ese "~5 %" es una fracción — el umbral de DB-03/DB-04 ya presupone la unidad que ADR-007 propone, así que la alternativa A no es una opción nueva sino una reapertura de DEC-006 | [[vault/_DevLog/2026-08-29-marina-garcia-correcciones-y-revision-pr129]] | ⬜ Mesa convocada por el PM |

## Evidencia incremental — 2026-08-29 · BUG-031, unidad de KPI-02 (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-211a`, `US-212` | **BUG-031**: KPI-02 pintaba −54.5 % contra un valor real de −0.19 %, verificado en Postgres (32 312 alumnos contra 32 374) ⬜ · origen en §4.4 del contrato, no en la implementación de US-113 · DB-03 corregido a razón de sumas y verificado corriéndolo ✅ · tarjeta de DB-04 retirada hasta que C1 exponga `suma_matricula_anterior` ⬜ · §4.4 reescrito con la regla de que un componente aditivo es una suma simple, nunca un producto ponderado ✅ | [[vault/_DevLog/2026-08-29-marina-garcia-bug031-kpi02-unidad]] | 🟡 Parcial: falta el cubo (C1) |
| `REQ-002` | `US-212` | `test_una_metrica_de_porcentaje_no_multiplica_dos_medidas` ✅ — verificado reintroduciendo el defecto a propósito: falla con él y pasa sin él · cubre la **clase** de error, no la forma: la prueba anterior buscaba `* 100` y esta expresión nunca lo tuvo · 28 passed, ninguna prueba previa perdida (`--collect-only` comparado contra `main`) | [[vault/_DevLog/2026-08-29-marina-garcia-bug031-kpi02-unidad]] | ✅ |

→ Seguimiento de junta: [[vault/13_Reports/US_Validation_Followup_2026-08-28]]

## Evidencia incremental — 2026-08-29 · EDA y cobertura territorial ML-03

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-322`, `US-325` | EDA reproducible y auditoría de `SIN_DATO` por driver, entidad y municipio; `cve_mun` excluida del vector de clustering; validación de clave INEGI y cero inicial; 17 pruebas enfocadas ✅ · suite 544 ✅ · Ruff ✅ · Vault limpio ✅ | [[vault/_DevLog/2026-08-29-estefany-hernandez-us322-us325-territorial]] | 🟡 En revisión; US-325 espera ejecución sobre Gold real |

## Evidencia incremental — 2026-08-29 · ML-03 clustering temporal

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-321` | Pipeline KMeans + StandardScaler; selección de `k` por Silhouette walk-forward; perfiles de negocio; target y llaves excluidos; política provisional `casos_completos` sin imputar cero · 6 pruebas enfocadas ✅ · suite 533 ✅ | [[vault/_DevLog/2026-08-29-estefany-hernandez-us321-clustering]] | 🟡 En revisión; espera fallback ratificado y corrida real |
| `REQ-003` | `US-303`, `US-321` | ML-03 integrado al helper compartido y preparado para crear la versión canónica `ML03_ClusteringEscuelas` en Registry, sin promoción productiva · 7 pruebas enfocadas ✅ | [[vault/_DevLog/2026-08-29-andres-gonzalez-us303-registro-ml03]] | 🟡 En progreso; E2E local completado, falta servidor compartido |
| `REQ-003` | `US-303` | E2E local MLflow 3.15.1: `ML01_RegresionMatricula`, `ML02_DriverClasificador` y `ML03_ClusteringEscuelas` registrados y verificados como versión 1 ✅ | [[vault/_DevLog/2026-08-29-andres-gonzalez-e2e-registry-tres-modelos]] | 🟡 En progreso; falta servidor compartido y API C4 |

## Evidencia incremental — 2026-08-29 · ML-03 en la evaluación

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-312`, `US-321` | **AC-003.2 cubierto**: los tres modelos reportan métrica en `Evaluacion_Modelos.md` ✅ · ML-03 sin baseline fingido (`NaN`, no `0`) ✅ · §6 contrasta cada métrica contra su umbral y **afirma** si cumple — ML-03 no llega (0.1086 vs ≥0.30) ⚠️ · guarda de sincronía extendida a ML-03 ✅ · 6 pruebas nuevas · suite 595 ✅ | [[vault/_DevLog/2026-08-29-hector-morales-ml03-en-evaluacion]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-29 · ratificación de ADR-007

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-311`, `US-313` | **ADR-007 `accepted`** (29-ago) con la tabla de lo que falta para que surta efecto ✅ · `DOC-INDICE-RIESGO` reducido a una sola decisión abierta —el ancla `0.30`— tras cerrar el umbral (`DEC-006`) y la doble columna `valor`/`indice_riesgo` ✅ · **BUG-032** registrado: `Data_Model` §5.3 contradice a §4.5 ⬜ · suite 643 ✅ | [[vault/_DevLog/2026-08-29-hector-morales-adr007-ratificado]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-30 · cierre de US-213 y arranque de US-214b/US-215b (C2)

| REQ | Historias | Evidencia/decisión | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-213` | PR #114 confirmado **mergeado** (29-ago): DB-05 (6 tabs) y DB-08 (explorador) sobre `gold.cubo_*`, 51 pruebas, validado contra Gold real ✅ · revisado el remap mecánico de PR #134 (US-205, Manuel Serranía) línea por línea contra las 6 tabs: limpio, sin referencias viejas a KPI-19/`valor_promedio_driver` · 2 nits cosméticos corregidos aquí (wording "con"/"por" driver dominante; falta `cobertura:` en `pct_escuelas_sin_recomendacion`) | [[vault/_DevLog/2026-08-30-monserrat-miranda-cierre-us213-revision-pr134]] | ✅ `US-213` done |
| `REQ-002` | `US-214b`, `US-215b` | Arranca hoy: filtros/drill-down DB-05→DB-08 (`cve_mun`, `id_driver`), alcance acotado al lado Superset (la navegación cruzada en FARO Web es de Manuel/`src/frontend/`, coordinación aparte) · plan de pruebas de usabilidad/accesibilidad — ambas Sprint 5 | [[vault/_DevLog/2026-08-30-monserrat-miranda-cierre-us213-revision-pr134]] | ⬜ Arrancando |

## Evidencia incremental — 2026-08-30 · avance de US-214b/US-215b (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-214b` | Drill-down DB-05→DB-08 implementado (link HTML + `native_filters` RISON) y **validado en navegador real** contra Superset 6.1.0 en Docker local: filtro de Municipio/Driver preseleccionado correctamente, confirmado con 2 municipios distintos (el chart "Valor promedio del driver" de DB-08 cambió de 0.10 a 0.90 entre uno y otro) ✅ · `Cube_Specs_DB05_DB08.md` §3.4 pasa de "⬜ Propuesta" a "✅ Ratificada" ✅ | [[vault/_DevLog/2026-08-30-monserrat-miranda-avance-us214b-us215b]] | 🟡 En progreso |
| `REQ-002` | `US-215b` | Plan de pruebas de usabilidad/accesibilidad (`DOC-USABILIDAD-DB0508`) creado y filado en `vault/06_Quality_Testing/_index.md` ✅ · 3 casos ejecutados hoy (el link y su filtro en DB-08), el resto queda `⏳ pendiente` para siguiente pasada | [[vault/_DevLog/2026-08-30-monserrat-miranda-avance-us214b-us215b]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-31 · gate real de Ruff en CI (US-523b)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-523b` | Gate de Ruff en `ci.yml` dejó de usar `\|\| true`; ahora bloquea merge por errores de lint en todo el repo (`ruff check .` global) · los 58 errores preexistentes, ninguno introducido por este cambio, no bloquean el merge porque `ruff.toml` los exonera vía `per-file-ignores`, pendientes de limpieza en historia dedicada · gate de `pytest` ya bloqueaba correctamente desde antes, sin cambios necesarios · suite completa verificada: 643 passed, 5 skipped | [[vault/_DevLog/2026-08-31-edgar-jimenez-us523b-ci-quality-gate]] | **US-523b cierra por completo** |

## Evidencia incremental — 2026-08-31 · cierre de BUG-017/BUG-019

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-104`, `US-311`, `US-313` | **BUG-017/BUG-019 cerrados**: `target_variacion_matricula` pasa a **fracción** en `features_escuela.sql`, ratifica [[vault/03_Architecture/ADRs/ADR-007-unidad-target-variacion-matricula\|ADR-007]] (2026-08-29) — unidad declarada por primera vez en `contrato.py`/`Data_Model.md` §5.3 · verificado real: sin división por cero, dbt test nuevo de escala ✅, 643/643 suite ✅ · pendiente C3: regenerar 45 249 predicciones y reentrenar ML-01 | [[vault/_DevLog/2026-08-31-diana-alvarez-bug017-bug019-target-fraccion]] | 🟡 En progreso |

## Evidencia incremental — 2026-08-31 · BUG-031, mitad C1 (matrícula anterior)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-211a`, `US-212`, `US-221` | **BUG-031 (C1)**: expuesta `matricula_ciclo_anterior` en `fact_escuela_ciclo.sql` y propagada a `cubo_escuela_360.sql`, `cubo_comparador_municipio.sql` (alcance original de 3 archivos del registro) más **`cubo_matricula.sql` (DB-01/DB-06) y `cubo_riesgo_territorial.sql` (DB-02)**, encontrados por grep exhaustivo del mismo patrón defectuoso antes de dar por cerrado el alcance — ambos confirmados con Diana antes de tocarlos ✅ · `cubo_matricula_fact_parity.sql` actualizado a la columna nueva ✅ · `_gold__models.yml` documenta `matricula_ciclo_anterior` con `not_null` ✅ | [[vault/_DevLog/2026-08-31-diana-alvarez-bug031-matricula-anterior]] | 🟡 Parcial: falta C2 (Manuel Serranía) |
| `REQ-002` | `US-212` | Verificado con `dbt run --full-refresh` + `dbt test` completos sobre Postgres real (no `--select` escoped, para evitar el ruido de artefactos de corridas parciales): 5 modelos reconstruyen limpio (148/90/90/90/90 filas) · `cubo_matricula_fact_parity` y `not_null_fact_escuela_ciclo_matricula_ciclo_anterior` ✅ · de los 13 errores restantes del run, 8 ya documentados como preexistentes (DS-06/CONAGUA, DS-07/CONEVAL) y 2 diagnosticados en esta verificación (`cubo_recomendaciones_kpi11_parity`, `gold_ml_runtime_recomendaciones_fact_relationship`) como preexistentes y no relacionados — confirmado leyendo ambos tests (no referencian columnas de matrícula) y con `git diff main` (el fix es puramente aditivo, no toca el filtro `where matricula_ciclo_anterior is not null` que los origina) | [[vault/_DevLog/2026-08-31-diana-alvarez-bug031-matricula-anterior]] | ✅ |

## Evidencia incremental — 2026-09-01 · rama fija por persona y consolidación del vault (US-001)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-001`, `US-004` | **Rama fija `dev/{identidad}` como único modelo**: `vault/_Meta/ownership.yml` es la fuente única de identidad, rama y permisos de los 21 integrantes (handles verificados contra la API de GitHub) · `check_ownership.py` reprueba en CI la rama que no sea la del autor, el título fuera de estándar o firmado por otra persona, y el archivo fuera de alcance (`TEST-014`, 25 casos ✅) · gate de sincronía con `main` (G10) reprueba el PR con la rama atrasada · las 19 carpetas de documentación quedan bajo `vault/`, con los 4 puntos de ruptura del CI reparados · 14 contradicciones normativas resueltas entre `CLAUDE.md`, `AGENTS.md`, `vault/_Meta/` y los 21 planes de sprint · verificado: `vault_lint` limpio ✅, 749 passed / 5 skipped ✅, `ruff` ✅, plantilla de PR ✅, tablero PM ✅ | [[vault/_DevLog/2026-09-01-edgar-coronel-rama-fija-consolidacion-vault]] | 🟡 En progreso |

## Evidencia incremental — 2026-09-02 · el padrón permite lo que la plantilla exige (BUG-039)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-001` | **BUG-039 cerrado.** El gate de propiedad contradecía a la plantilla de PR: `vault/_DevLog/_index.md` y esta matriz eran alcance exclusivo del PM pese a que la plantilla los exige en casilla obligatoria, y `.gitignore`/`.gitattributes` no estaban en el alcance de nadie — medido con el propio gate: 20 de 21 personas con 4 rutas fuera de alcance. Al correr el gate contra la propia corrección apareció el alcance real: **los 6 registros de intake que `Definition_of_Filed` obliga a usar a cualquiera** (Bug_Register, Security_Audit_Log, Risk/Blocker/Decision/Incident) estaban cerrados a 0 o 1 persona — la regla que manda reportar un bug era imposible de cumplir. 11 rutas pasan a `comunes`, `03_Architecture/_index.md` al amarillo de C1/C4 y `tests/**` al del PM; matriz y `10_Risk_Governance/**` quedan en `criticos` para que el gate avise sin reprobar · verificado: `TEST-014` ampliado a 40 casos que recorren a los 21 contra cada ruta obligatoria y cada registro de intake ✅, suite completa 764 passed / 5 skipped ✅, `vault_lint` limpio ✅, `ruff` ✅ | [[vault/_DevLog/2026-09-02-edgar-coronel-desbloqueo-gates-ownership]] | **BUG-039 cierra por completo** |

## Evidencia incremental — 2026-09-02 · el parser del tablero respeta los pipes escapados (BUG-040)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-004` | **BUG-040 cerrado.** `table_cells()` partía las filas por todos los pipes, incluido el de un wikilink con alias —sintaxis que el vault escribe escapada 190+ veces y que usan 6 parsers—: las columnas se desplazaban y el snapshot publicaba basura **sin que nada fallara**. En `main` estaba `updated` con texto en vez de fecha para US-004 y la evidencia truncada; y el mismo defecto en el índice de DevLog contaba **25 autores en vez de 21** y dejaba a Manuel con 0 DevLogs teniendo 12, a Eloísa con 0 de 3 y a Carlos con 0 de 1. Se corrige el parser (no las filas), `parse_devlog_authors` deja su copia duplicada, y `validate_pm_dashboard.py` ya exige que `updated` sea fecha · verificado: `TEST-015` nuevo con 10 casos ✅, suite 774 passed / 5 skipped ✅, `vault_lint` limpio ✅, `ruff` ✅, y el validador reprueba con la fila rota inyectada | [[vault/_DevLog/2026-09-02-edgar-coronel-parser-tablas-tablero]] | **BUG-040 cierra por completo** |

## Evidencia incremental — 2026-09-02 · US-412/US-416: rutas responden en prod, BUG-020 curado

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-412`, `US-416` | **Contradicción de la fila REQ-004 resuelta con hechos.** BUG-020 quedó **curado en prod** en US-505 Fase 2 (C5, PR #144/#146) — la fila REQ-005 de esta misma matriz ya lo dice; la narrativa vieja de REQ-004 ("US-412 responde 500 en producción") quedó desactualizada. Verificado en vivo 2026-09-02 contra la URL pública: `GET /predicciones/{cct}` → **404 estructurado** `ErrorOut` (no 500), `POST /predicciones/batch` → **200** `{"items":[],"total":0}`, `GET /escuelas` → 200/25. Las dos rutas de US-412 **responden en el despliegue** (DEC-012); el 404/vacío es porque `gold.predicciones` está vacía en prod (falta US-313 / Carril A), no código de C4. **US-416**: última pieza mergeable — `RepositorioModelosPostgres._con_timeout` traduce cualquier `SQLAlchemyError` a `RepositorioModelosNoDisponible` → 503 (antes un `ProgrammingError` por esquema `gold` ausente escapaba a 500); `tests/test_repositorio_modelos.py` (+4) verde, `pytest tests/ -q` 778 passed / 5 skipped, `ruff` limpio, `API_Specification.md` §3.4/§5 actualizado | **@Edgar:** fallo DEC-012 para US-412 (¿404 estructurado con Gold ML vacío = "la ruta responde" → `done`, o espera US-313?) + voltear US-412/US-416 en `Execution_Status.md` al mergear + reconciliar `Bug_Register`/fila REQ-004 + corregir `github_user` de Juan (`juanmmayen98`→`juanmmayen98-pixel`) y conteo de PRs #95/#101 · **@Christian (TL C4):** ratificar diseño de US-416 + endurecimiento, confirmar E2E Postgres real = US-422 | 🟡 Listo para cierre salvo ratificación + flip del PM |

## Evidencia incremental — 2026-09-02 · US-224 manual de usuario de dashboards (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-224` | Manual de usuario creado (`vault/04_UX_Design/Manual_Usuario_Dashboards.md`): guía en lenguaje de negocio de los 10 dashboards, mapa de navegación, filtros globales y narrativa sugerida para el pitch de entrega. Se intentó levantar Superset local + mocks sancionados (`superset/mock/*.sql`) para tomar capturas reales de DB-01/DB-02, pero el Postgres local no tiene Bronze cargado (mismo bloqueo que DB-07/DB-10) y materializar los cubos requería una acción bloqueada por el clasificador del agente; se decidió no forzarlo por tiempo. El documento queda `status: draft` con 10 marcadores `[CAPTURA PENDIENTE]` explícitos y una tabla de estado de datos conocido por dashboard, en vez de simular capturas o dejar el hueco sin explicar | [[vault/_DevLog/2026-09-02-oscar-quiroz-diagnostico-us222-us223-cierre-documental]] | 🟡 En progreso — falta reemplazar capturas cuando el ambiente tenga Bronze |

## Evidencia incremental — 2026-09-02 · tableros DB-07/DB-10 y correcciones al manual, tras revisión de PR #190 (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-222`, `US-223` | Se agregan `superset/dashboards/db07_calidad_cobertura.yaml` (7 charts) y `db10_monitor_pipeline.yaml` (5 charts) — faltaba la definición de tablero, no solo la capa semántica; sin eso Superset no tenía charts que registrar aunque hubiera datos. YAML validado con `yaml.safe_load` + campos obligatorios por chart contra `_params_chart()` de `sync_semantic_layer.py`; solo usa columnas/métricas ya existentes en `db07_cubo_completitud.sql`/`db07_mapa_vacios.sql`/`db10_cubo_pipeline.sql`/`metrics_db07.yaml`/`metrics_db10.yaml`. No se probó contra Superset en vivo: `gold.cubo_completitud`/`gold.cubo_pipeline` no están materializados en este ambiente (bloqueo de Bronze ya documentado) y **BUG-029** (reservado a Oscar) advierte que un dataset roto aborta la corrida completa del sync — probarlo ahora sería ruido. Suite 774 passed, `vault_lint` limpio | [[vault/_DevLog/2026-09-02-oscar-quiroz-db07-db10-dashboards-y-correcciones-manual]] | 🟡 En progreso — código/tablero completos, registro real pendiente de Bronze (Diana Álvarez, C1) |
| `REQ-002` | `US-224` | Corregidas 2 imprecisiones del manual detectadas en revisión: la tabla de estado sobredeclaraba "datos reales" para DB-01/DB-02 (el DevLog de Luis Téllez del 2026-09-01 valida solo estructura, no números — cita textual verificada); y la frase "ML-01 llega en Sprint 4" en DB-03 quedó vencida (US-311 in_progress desde 2026-08-08, PR #28 mergeado, MAE 0.0141/RMSE 0.0177, verificado en `Execution_Status.md`) | [[vault/_DevLog/2026-09-02-oscar-quiroz-db07-db10-dashboards-y-correcciones-manual]] | 🟡 En progreso — capturas siguen pendientes de Bronze |
## Evidencia incremental — 2026-09-02 · cierre de la superficie de autenticación (C4)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-402`, `US-403`, `US-404`, `US-405` | **US-402 cierra e2e**: `RealGoogleVerifier` implementado (canje del `code` + verificación del `id_token` por JWKS RS256, `aud`/`iss`/`exp`/`email_verified`) y **`SEC-002`** corregido — el `state` era la constante `faro`, un login CSRF observable en la URL pública; ahora es un JWT firmado de un solo uso con cookie `HttpOnly`. **US-403 y US-404 cierran** con la revisión humana explícita de la regla 7 ([[vault/07_Security/Security_Review_US402_US403_US404]]) y `SEC-003`…`SEC-006` aceptados con dueño. 18 pruebas nuevas; 79 passed en la suite de API. **`US-405` sigue abierta y no se tocó**: vive en `src/frontend/**`, cuyo dueño es Manuel Serranía (C2) | Christian valida el login e2e en la URL pública tras el deploy · Edgar ratifica `ANALISTA_EMAILS` · Luis da de alta *test users* y coordina el flip de `AUTH_LECTURA_PUBLICA` · Manuel acuerda el arranque de US-405 | 🟡 En progreso |

## Evidencia incremental — 2026-09-02 · BUG-029 fixed, confirmado por Edgar Coronel (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-222`, `US-223` | **BUG-029 fixed.** El registro previo asignaba la solución de fondo a Manuel (US-205); Edgar Coronel confirmó explícitamente por Teams que cerrar esta parte es de Oscar para poder marcar sus historias como `done`. `ensure_datasets()` en `superset/sync_semantic_layer.py` ya no aborta toda la corrida cuando un dataset falla — se reporta y se continúa (mismo patrón que `_apply_metrics_and_columns`, ya existente en el archivo). `ensure_chart()` ya manejaba datasets ausentes con gracia, así que el fix fue un solo `try/except` quirúrgico. Nuevo `tests/test_sync_resiliencia_bug029.py` (3 casos, sin red, mismo patrón de import de módulo que `test_semantic_db01_db02.py`): confirma que `db10_cubo_pipeline` se registra aunque `db09_cubo_recomendaciones` falle — el escenario exacto que BUG-029 describía. Suite 781 passed, `vault_lint` limpio | [[vault/_DevLog/2026-09-02-oscar-quiroz-fix-bug029-resiliencia-sync]] | ✅ BUG-029 cerrado — sigue pendiente que Bronze cargue para materializar los cubos (Diana Álvarez, C1) |

## Evidencia incremental — 2026-09-02 · el path `--desde-gold` de ML truena con un driver 100 % SIN_DATO (BUG-041)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-313`, `US-311` | **BUG-041 reportado; parche preparado y validado en local, pendiente de aplicar por C3.** El path real `--desde-gold` de ML rompe la predicción cuando un driver queda 100 % `SIN_DATO`: `pd.read_sql_table` da columnas `quoted_name` (subclase de `str`) y sklearn no puebla `feature_names_in_` (exige `type(x)==str` exacto) → `construir_predicciones` cae al fallback `DRIVERS` (6) y desajusta la forma (`X has 6 features ... expecting 5`). Misma FAMILIA que BUG-015/018/023 pero **causa raíz nueva** (el patrón `feature_names_in_` nunca se activa en el path de la BD; los fixtures CSV lo ocultan) · **validación local con el parche** (`df.columns=[str(c) for c in df.columns]` tras `pd.read_sql_table` en `entrenar_ml01.py:203`): `feature_names_in_`=5 drivers usables, 55 predicciones (ML-01, MAE 0.0844) + 55 recomendaciones (ML-02, F1 0.6458), `/api/v1/kpis` escuelas_en_riesgo=2, diferenciador prescriptivo probado (15DJN0049A→D1, 09DSN0042A→D2) ✅ · **PR de C5 solo documental** (registro + DevLog + índice + matriz); `src/modelos/**` es verde de C3 → el código lo lleva Célula 3 (natural: Héctor Morales, US-311/US-313; TL Andrés González) con test de regresión, patrón BUG-018/BUG-008; merge del PO · `vault_lint` limpio ✅ | [[vault/_DevLog/2026-09-02-luis-tellez-bug041-quoted-name-desde-gold]] | **BUG-041 open — parche listo para C3** |

## Evidencia incremental — 2026-09-03 · cobertura completa de Execution_Status.md (BUG-042)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-004` | **BUG-042 fixed.** 24 de 91 historias no tenían fila en `Execution_Status.md`; `build_snapshot()` las contaba `planned` por default sin verificar (mismo patrón que BUG-040). Auditadas una por una contra PRs mergeados: 10 con evidencia real, 14 `planned` de verdad. Fila `US-206` cargaba evidencia de `US-205` por etiqueta equivocada (hallazgo de Manuel Serranía, PR #193) — corregida. Se agregan las 24 filas + reetiquetado; `build_snapshot()` ya no asume `planned`, falla y lista lo que falte. `TEST-034` nuevo (2 casos): cobertura de las 91 reales + inyección de una historia sin fila. Verificado: `pytest` 810 passed / 6 skipped ✅, `vault_lint` limpio ✅, `ruff` ✅, `generate_pm_dashboard.py` → 91 US ✅. De paso, dos huecos de `ownership.yml` que bloqueaban a terceros sin culpa suya: `ADRs/**`/`.env.example` sin dueño (hallazgo de Christian Ruiz, US-402) y `dags/**`/`common_alerting/**` fuera del alcance de Edgar Jiménez (US-524b) — los 4 escenarios confirmados uno por uno contra `check_ownership.py` | [[vault/_DevLog/2026-09-03-edgar-coronel-cobertura-execution-status-bug042]] | **BUG-042 cierra por completo — 91/91** |

## Evidencia incremental — 2026-09-02 · AC-003.4 no estaba cumplido (BUG-043)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-311`, `US-303` | **BUG-043 (critical) abierto.** La corrida de confirmación que faltaba desde el 18-ago no confirmó: reprobó. Con el servidor sin `--serve-artifacts`, su `--default-artifact-root` es una ruta interna del contenedor, así que `log_model()` falla con `OSError: Read-only file system: '/mlflow'` — pero `register_model()` **crea la versión de todas formas** y la deja `READY` apuntando a un artefacto inexistente. Así estuvo `ML01_RegresionMatricula` **v1 durante 15 días**: `verificar_registry` la reportaba en verde y `load_model()` respondía `No such artifact: 'MLmodel'`. **AC-003.4 se daba por cumplido sin estarlo**, y con él la ruta de inferencia de C4, que carga por `models:/…` · **C3 ✅**: `verificar_artefactos_descargables()` carga cada versión con `pyfunc` y reprueba nombrando modelo, versión y causa; el CLI la corre por defecto y `--sin-artefacto` declara que su verificación es débil · **C5 ⬜**: falta `--serve-artifacts --artifacts-destination /mlflow/artifacts` en `docker-compose.yml`, más **recrear el experimento** (`artifact_location` se fija al crearlo y no se recalcula) · verificado en los dos sentidos con override local: con el fix ML-01 **v2 registra, carga y predice**; sin él la guarda reprueba ✅ · 7 pruebas nuevas ✅, suite 780 passed / 5 skipped ✅, `ruff` ✅, `vault_lint` limpio ✅ | [[vault/_DevLog/2026-09-02-hector-morales-registry-us311]] | 🔴 **AC-003.4 bloqueado por C5** |
| `REQ-003` | `US-313` | Job batch verificado contra Postgres real: **80 predicciones + 80 recomendaciones** desde fixture, **idempotente** (dos corridas, **0 duplicados** en la llave natural), con `mlflow_run_id` real y conviviendo con el grano `municipio_nivel` (DEC-010) · **Corrección del 3-sep:** la causa raíz que publiqué el 2-sep era **equivocada** — afirmé que `features_escuela.sql` debía leer `matricula_historica` en vez de `ref('matricula')`, y el problema era que yo había cargado **2 de los 3** fixtures de Formato 911 en Bronze, faltando el de la serie histórica de BUG-026. Con los tres, Gold sale con **145 filas y 3 ciclos** (60/30/55), idéntico a lo que reportó C5. `features_escuela` nunca estuvo mal y **no hay nada que pedirle a C1 por este motivo** · con BUG-041 corregido, `--desde-gold` publica **55 + 55** contra Gold real | [[vault/_DevLog/2026-09-03-hector-morales-bug041-quoted-name]] | 🟢 **US-313 corre contra Gold real**

## Evidencia incremental — 2026-09-03 · BUG-041 corregido: `--desde-gold` publica contra Gold real

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-313`, `US-311` | **BUG-041 `fixed`.** C5 (Luis Téllez) diagnosticó y dejó el parche preparado; C3 lo verifica y lo aplica, porque `src/modelos/**` es su verde. **Verificación independiente antes de aplicar**: `pd.read_sql_table` devuelve `quoted_name` —subclase de `str`— en Postgres **y** en SQLite, y sklearn 1.9.0 no puebla `feature_names_in_` con él pero sí con `str` puro; el fallo se reprodujo end-to-end contra el Gold local (145 filas, 3 ciclos, D5 100 % `SIN_DATO`) con el mismo `ValueError: X has 6 features, but ... expecting 5` y el mismo MAE 0.0844 reportados por C5 · con el parche, `publicar_gold --desde-gold` publica **55 predicciones + 55 recomendaciones** (F1 0.6458) · 3 pruebas de regresión que **reprueban con el parche revertido** (comprobado) y corren sobre SQLite, así que el CI las ejercita sin Postgres — cerrando el hueco que Luis señaló: la suite no lo cazaba porque los fixtures CSV dan `str` puro · **desbloquea L1.3** (correr `publicar_gold` contra Cloud SQL), que es el piso de RISK-001 · suite 817 passed / 6 skipped ✅, `ruff` ✅, `vault_lint` ✅ | [[vault/_DevLog/2026-09-03-hector-morales-bug041-quoted-name]] | ✅ **BUG-041 cierra; US-313 corre contra Gold real** |

## Evidencia incremental — 2026-09-03 · AC-003.4 cumplido y BLOCK-001 cerrado

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-311`, `US-303` | **AC-003.4 CUMPLIDO — los 3 modelos registrados con versión y carga verificada**: `ML01_RegresionMatricula` v4, `ML02_DriverClasificador` v2, `ML03_ClusteringEscuelas` v2, comprobados con `verificar_registry` (que **carga** cada versión con `pyfunc`, no sólo consulta la fila). **BLOCK-001 → `resolved`**: el desajuste de versiones de PR #45 sí estaba resuelto (servidor 3.15.1 = cliente 3.15.1); lo que faltaba era la raíz de artefactos · **Corrección de mi diagnóstico del 2-sep**: culpé a la falta de `--serve-artifacts`, pero en MLflow 3.x **viene activo por defecto** (`--help`: `Default: True`) — pedirlo a C5 habría sido un no-op. La causa era `--default-artifact-root`, que sale de `MLFLOW_ARTIFACT_ROOT` en el `.env` (ruta `comunes`), así que se corrigió **sin tocar archivos de C5** · **Pendiente C5 ⬜ (BUG-043)**: sin `--artifacts-destination /mlflow/artifacts` los artefactos caen en la capa efímera del contenedor — **probado en los dos sentidos**: con el flag los tres sobreviven a `docker compose up --force-recreate`; sin él, los tres quedan `READY` sin artefacto. En Cloud Run el contenedor se recrea de rutina, así que esto vacía el registry de la demo | [[vault/_DevLog/2026-09-03-hector-morales-bug041-quoted-name]] | ✅ **AC-003.4 cumplido · BLOCK-001 cerrado · BUG-043 parcial (falta C5)** |


## Evidencia incremental — 2026-09-03 · `name` en el contrato de sesion (C4/C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-405`, `US-402` | `UserOut` gana **`name`** (opcional, default `""`), tomado del claim `name` del `id_token` de Google y propagado a los dos tokens — en el refresh también, porque al renovar no hay `id_token` que reconsultar. Cierra el choque con `app.py:24`, que leía un `user['name']` que el contrato no tenía. Acordado por escrito entre Christian Ruiz (C4) y Manuel Serranía (C2); avisado a C3. Frontera fijada por prueba: `name` es solo de presentación, el rol se resuelve por `email`. 5 casos nuevos, **84 passed**. **US-405 sigue bloqueada** por una pieza de diseño que falta: el puente del token de `/auth/callback` a `st.session_state` no está definido en `Frontend_Architecture.md` | Decidir el puente OAuth → Streamlit y escribir `src/frontend/auth.py` · Luis redespliega y carga `ANALISTA_EMAILS` · Christian valida el login e2e en vivo | 🟡 En progreso |


## Evidencia incremental — 2026-09-03 · US-405: puente OAuth y sesion de FARO Web (C4)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-405`, `US-402` | Se cierra la pieza que bloqueaba US-405 y que **nunca se había especificado**: cómo llega el token de `/auth/callback` al servidor de Streamlit. Decisión en [[vault/03_Architecture/ADRs/ADR-010-puente-oauth-frontend\|ADR-010]] — **código opaco de un solo uso**, no tokens en la URL (que acabarían en el historial, en los logs del proxy y en el `Referer`). Incluye allowlist estricta del `redirect` con comparación exacta, `redirect` dentro del `state` firmado, SHA-256 del código en vez del código, identidad en vez de tokens en reposo y canje atómico (`DELETE ... RETURNING`). `src/frontend/auth.py` implementado con la firma acordada con C2 (🟡 autorizado por escrito por Manuel Serranía); `app.py`, `1_Dashboards.py` y `superset_client.py` no cambian. **27 pruebas nuevas, 131 passed.** Riesgos nuevos registrados: `SEC-007` (el almacén degrada a memoria si falta permiso de `CREATE` — solo correcto con 1 instancia) y `SEC-008` (FARO Web usa la credencial **admin** de Superset para el guest token) | Christian valida el login e2e en la URL pública tras el redeploy y revisa los logs por `SEC-007` · Luis carga `FRONTEND_REDIRECT_URIS` y `ANALISTA_EMAILS` · Manuel confirma la URL del front desplegado | 🟡 En progreso |

## Evidencia incremental — 2026-09-03 · US-214a: drill-down cruzado DB-03/DB-04 (C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-214a` | **AC-002.2 ya estaba cubierto** desde US-212 (ciclo/entidad/nivel en ambos tableros) ✅ · lo que faltaba era la navegación cruzada declarada en `drill_down:` desde US-211a y nunca construida. **Implementadas las 2 rutas cuyo origen vive en esta historia**: `link_db04` en `db03_cubo_escuela_360.sql` y `link_db03` en `db04_cubo_comparador_municipio.sql`, con `native_filters` RISON + `allow_render_html` (patrón de US-214b, Monserrat) ✅ · filtro `cve_mun` agregado **al final** de `filtros_globales` en ambos tableros (índice 4): los IDs se generan por posición, insertar en medio rompe la navegación sin error visible ✅ · `tests/test_drill_down_db03_db04.py` (18 casos), **cada guarda validada reintroduciendo su defecto** — reordenar filtros, quitar `allow_render_html`, llave imposible, quitar el quoting `%27`: falla con el defecto y pasa sin él ✅ · verificado además contra el **Superset desplegado** decodificando el RISON con `prison` (la librería del propio Superset) y contrastando cada `NATIVE_FILTER-US203-{i}` contra `native_filter_configuration` real: 4/4 destinos correctos ✅ | Click-through visual en el navegador (queda para US-215a) | ✅ |
| `REQ-002` | `US-214a`, `US-211a` | **Corrección de contrato.** La ruta `DB-04 → DB-03` se declaró en US-211a con llave `cct` y es **imposible**: DEC-008 fijó el grano de `cubo_comparador_municipio` en `[cve_mun, nivel, id_ciclo]` y ese cubo no tiene columna `cct`. Corregida a `cve_mun` ✅ · el bloque `drill_down` ahora lleva `estado` por ruta (`implementado`/`bloqueado`/`ajeno`) para que la brecha entre lo declarado y lo construido sea legible ✅ · prueba que rechaza la **clase** de error, no la instancia: `test_ninguna_ruta_declara_una_llave_que_el_cubo_de_origen_no_tiene` ✅ | — | ✅ |
| `REQ-002` | `US-214a`, `US-204` | **2 rutas no se construyeron por falta de destino válido**: `DB-03 → DB-06` y `DB-03 → DB-09` requieren un filtro `cct` que esos tableros no exponen — sin él el link aterriza en el tablero completo, no en la escuela. No se shippea un link que no hace lo que promete ⬜ · **pendiente de Manuel Serranía (US-204)**: agregar `cct` al final de los `filtros_globales` de `db06_predicciones.yaml` y `db09_recomendaciones.yaml` | Manuel agrega los dos filtros | ⬜ Bloqueada en C2 |
| `REQ-002` | `US-214a`, `US-212` | **US-212 desbloqueada.** ADR-007 quedó implementado punta a punta por C1 (paso 1-2, Diana) y C3 (pasos 3-4, Héctor): `gold.predicciones.valor` en rango −0.0437…+0.0313 (**fracción**, no alumnos) y `indice_riesgo` 0.1637…0.5615 (**no saturado**, era el síntoma de BUG-017) ✅ · **AC-002.4 ya verificable**: 55 escuelas con `cobertura_prediccion = OK`, 90 con `SIN_DATO` ✅ · KPI-02 concuerda por tres caminos independientes (hecho, DB-03, DB-04): **−0.192 %** sobre 32 312/32 374, los mismos valores del 29-ago ✅ · regla `SIN_DATO` verificada: cero casos donde un driver marcado `SIN_DATO` traiga valor ✅ | Cerrar US-212 al 100 % | ✅ |
| `REQ-002` | `US-214a` | **BUG-037 reproducido** al agregar las columnas de link: los charts revientan con `Columns missing in dataset` porque el sync actualiza el SQL del dataset pero no vuelve a leer sus columnas, y el error solo aparece al abrir el tablero ⬜ · mitigado a mano con `PUT /api/v1/dataset/<id>/refresh` ✅ · el arreglo de fondo toca `sync_semantic_layer.py`, herramienta compartida de C2: **requiere acuerdo con Manuel Serranía**, no se tocó | Acordar el fix con Manuel | 🟡 Mitigado, no resuelto |

## Evidencia incremental — 2026-09-03 · US-212: cierre al 100 % (C2)

> Solicitud al PM: **US-212 pasa de `in_review` a `done`** en `Execution_Status.md`. Esa ruta
> (`vault/12_Roadmap_Sprints/**`) no está en el alcance de Célula 2, así que el cambio de estado
> lo hace Edgar Coronel. Aquí queda la evidencia que lo sustenta.

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-003` | `US-212`, `US-104`, `US-313` | **El único bloqueo declarado desapareció.** ADR-007 se ratificó el 29-ago y quedó implementado punta a punta: pasos 1-2 por **Diana Alvarez** (C1, 31-ago, target a fracción con cast explícito y rechazo de `matricula_previa = 0`) y pasos 3-4 por **Héctor Morales** (C3, 2-3 sep, `gold.predicciones` regenerada y ML-01 reentrenado) ✅ · **verificado corriéndolo, no leyéndolo**: `gold.predicciones.valor` en rango **−0.0437…+0.0313** (fracción, no alumnos) e `indice_riesgo` **0.1637…0.5615** (no saturado — era el síntoma con el que BUG-017 detuvo la publicación) ✅ | — | ✅ |
| `REQ-002` | `US-212` | **AC-002.4 verificado.** DB-03: **55** escuelas con `cobertura_prediccion = OK` y **90** con `SIN_DATO` (los ciclos sin predicción, no un hueco); los bloques de predicción y recomendación pueblan con datos reales de `gold.predicciones` ✅ · **24/24 charts** de DB-03 y DB-04 con datos ✅ · reproducible **solo con fixtures del repositorio**, que es justo lo que BUG-013 exigía y no se podía: 145 filas y 3 ciclos en `gold.features_escuela`, 55 + 55 publicadas — **mismas cifras que obtuvo Héctor Morales por separado el mismo día**, lo que descarta un ambiente afortunado ✅ | — | ✅ |
| `REQ-002` | `US-212`, `US-211a`, `US-221` | **BUG-031 → `fixed`.** El registro llevaba tres días declarando un pendiente de C2 que ya estaba hecho desde el 31-ago, y lo hizo **Luis Téllez** (`f013b20`, `b74a700`), no Manuel Serranía como decía la asignación ✅ · los tres `metrics_*.yaml` migrados a razón de sumas, `variacion_x_matricula` sin apariciones fuera de comentarios, y las dos aserciones **invertidas** ✅ · **KPI-02 concuerda por cinco caminos independientes**: hecho, `cubo_matricula` (DB-01/DB-06), `cubo_riesgo_territorial` (DB-02), `cubo_escuela_360` (DB-03) y `cubo_comparador_municipio` (DB-04) dan todos **−0.192 %** sobre 32 312 / 32 374 ✅ | — | ✅ |
| `REQ-002` · `REQ-003` | `US-212`, `US-313` | **BUG-013 → `fixed`.** Su último pendiente era literal: *"la dueña de DB-03 no puede verificar sus propios bloques ML (AC-002.4) ni CI ejercitar la ruta"*. Cerró con el tercer fixture de Formato 911 que Diana creó para BUG-026 ✅ | — | ✅ |
| `REQ-002` | `US-212` | **Regla `SIN_DATO` verificada de punta a punta**: D1 145/145, D2 0/145, D3 y D4 12/145, D5 145/145, D6 140/145 — y **cero casos** donde un driver marcado `SIN_DATO` traiga valor ✅ · **Salvedad honesta**: `en_riesgo = 0` en las 55 escuelas con predicción. No es defecto — el riesgo máximo es 0.5615 contra el umbral 0.60 de DEC-006; con datos de fixture nadie lo cruza. Conviene revisarlo contra datos reales antes de la demo ⬜ | Revisar el umbral contra los datos reales de C1 antes del 9-sep | ✅ con salvedad |

## Evidencia incremental — 2026-09-03 · L1 «Piso RISK-001»: datos reales + OAuth real + RBAC vivos en prod (C5)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-005` | `US-505`, `US-402`, `US-403` | **L1 «Piso RISK-001» GREEN en la URL pública.** Se importó el Gold real ya materializado del de-risk a Cloud SQL (`gcloud sql import sql` desde bucket privado `gs://faro-escuela-sensor-sql-import`, server-side → compatible con la instancia **solo-IP-privada**; 10 tablas, matrícula **20,638,574** idéntica al de-risk local) y se **selló la imagen de `main`** (rebuild `linux/amd64` con `buildx --build-arg GIT_SHA=e8ec818` desde contexto limpio de `origin/main`; `build-and-push.sh` construía **sin `--platform`** y la imagen arm64 no arranca en Cloud Run) → revisión **`faro-api-00008-nrj`**. Verificado en vivo (`https://faro-api-eanzfglvyq-uc.a.run.app`): `/version`=`e8ec818` (ya no `dev`), `/kpis` matrícula 20,638,574 / var **−1.81%** / completitud 0.1969, `/municipios` **200** (**BUG-035 cerrado en prod** por el rebuild; el fix ya estaba en `main`, PR #183), `/auth/login` **302→Google** con `state` **JWT real** + cookie `HttpOnly` (**US-402 OAuth real vivo**, deja de responder `state=faro`), RBAC `/admin/export`+`/admin/metrics` **401** sin token / lectura pública **200** (**AC-004.4**), y el **diferenciador prescriptivo** confirmado (`09DBN0007I`→D1/becas vs `09DAL0009J`→D2/rutas seguras). **Cierra RISK-001** (datos reales en la URL pública) **+ US-402** (OAuth en prod) **+ RBAC AC-004.4** (PO→`analista`). Único cambio de código: `deploy-cloud-run.sh` activa `ANALISTA_EMAILS` (correo del PO **efímero, no versionado**; secretos siguen en Secret Manager) | **@Edgar (PO):** mergear el PR de L1.6 y hacer el **primer login e2e** con su cuenta Google (verifica AC-004.4 → rol `analista`); si sale `redirect_uri_mismatch`, dar de alta `…/api/v1/auth/callback` en la consola OAuth de Google (C4/PO). `escuelas_en_riesgo=0` (H1) queda para C2; agente/L4 (US-405) para C3 | 🟢 **L1 GREEN en prod** · pendiente merge del PO + su login e2e |

## Evidencia incremental — 2026-09-03 · Fix real de dedup en matricula_historica + suites GE DS-01/DS-02 (C1)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` | — (deuda técnica / calidad de datos, sin historia US específica asignada) | **Fix real del bug de dedup en `matricula_historica.sql`** (3+1 tests dbt fallando): primer intento (`lote_mas_reciente`, hipótesis de fixture viejo vs carga real) confirmado **fallido** por Diana con `dbt test` real contra Postgres — resultado idéntico al de antes del fix. Diagnóstico real con `dbt show --inline`: `cct=11PDI0085S` reporta `turno=1/nivel=INICIAL` en los 3 ciclos que fallaban (2019-2020, 2023-2024, 2024-2025), nivel fuera del alcance que el propio modelo ya declaraba (`accepted_values` de `nivel`: PREESCOLAR/PRIMARIA/SECUNDARIA). Fix real: CTE `nivel_basica`, filtra a ese alcance antes del dedup por turno. **Verificado por Diana contra Postgres real**: `dbt test --select matricula_historica` → `PASS=8 WARN=0 ERROR=0` · **Suites de Great Expectations nuevas** para DS-01 histórico y DS-02 (cierra deuda de calidad de datos señalada por Deni Garrido el 30-ago): `validacion_formato911_historico.py` / `validacion_cct.py`, 10 pruebas offline nuevas en verde | Correr las suites GE contra los archivos reales completos (hasta ahora solo probadas con DataFrame sintético) | 🟢 dedup verificado y cerrado contra datos reales · GE suites implementadas, pendiente prueba contra archivos reales completos |

## Evidencia incremental — 2026-09-03 · verificación de la auditoría de Marina García (ownership + tablero)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-412`, `US-416` | **US-412 → `done`**: DEC-012 ya resuelto en sesión previa (404 estructurado + 200 verificados en vivo el 2-sep), solo faltaba reflejarlo en `Execution_Status.md`. **US-416 se queda `in_review`**: evidencia actualizada al cierre del 2-sep, pero Christian Ruiz (TL C4) no ha ratificado el diseño — condición explícita de la propia fila, no se fuerza el cierre sin ella | [[vault/_DevLog/2026-09-03-edgar-coronel-auditoria-marina-ownership-tablero]] | ✅ US-412 cerrado · US-416 pendiente de ratificación |

## Evidencia incremental — 2026-09-03 · US-411: BUG-020 curado confirmado, BUG-044 nuevo y corregido en código (C4)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-411` | **Karla Monter valida contra la URL pública** lo que las notas de C5/C3 ya reportaban: BUG-020 no reproduce (`/escuelas`, `/municipios`, `/kpis` → 200; `order_by` inválido → 422; entidad fuera de `SCOPE_ENTIDADES` → lista vacía; `/series` confirmado fuera de alcance de US-411). **La validación destapó `BUG-044` (critical):** sin `ciclo` explícito, `/escuelas` y `/kpis` sumaban los ~3 ciclos de `gold.fact_escuela_ciclo` a la vez — matrícula triplicada en prod (20.6M vs ~7M reales para las 4 entidades), escuelas duplicadas sin `id_ciclo` para distinguirlas. Corregido en `dev/karla-monter`: `RepositorioGoldPostgres` (y su fake) usan el ciclo más reciente (`MAX(id_ciclo)`) como default en `listar_escuelas`/`obtener_kpis`/`obtener_escuela`. 3 pruebas de regresión nuevas, suite completa **843 passed / 7 skipped**. **No cierro US-411 todavía** (mismo criterio DEC-012 que US-412/US-416): el fix vive en código, no en el despliegue — la URL pública sigue triplicando hasta el próximo redeploy | Luis Téllez (C5) redespliega `faro-api` con el merge de `dev/karla-monter` · Karla reverifica `/escuelas` y `/kpis` contra la URL pública tras el redeploy y entonces sí cierra US-411 | 🟡 Listo para cierre salvo redeploy |
## Evidencia incremental — 2026-09-03 · BLOCK-004 destrabado en 9/10 dashboards (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-222`, `US-223`, `US-224` | Restauré el dump real de Bronze de Diana + corrí el extractor oficial de CONEVAL de Deni Garrido (`extractor_coneval.py` + `cargar_bronze_coneval_real.py`) — `bronze.coneval_irs_2020`/`coneval_pobreza_2020` reales, columnas hasheadas verificadas contra el modelo antes de correr `dbt run`. `dbt run` completo: **22/24 modelos materializados**, `dim_municipio` ya no bloquea en cascada. `sync_semantic_layer.py`: **9/10 tableros (DB-01…DB-09) registrados con datos reales** (`gold.cubo_completitud` 162 filas, `cubo_matricula`/`cubo_riesgo_territorial` 27 filas c/u, `fact_escuela_ciclo` 132,566 filas reales — resuelve de paso el desajuste de CCTs de la sesión anterior). 8 capturas reales nuevas tomadas con Playwright e insertadas en `Manual_Usuario_Dashboards.md` (status → `approved`). **Único pendiente:** DB-10 (`gold.cubo_pipeline`) bloqueado por `bronze.conagua_presas` — dependencia aislada de Emilio Galnares (CONAGUA/DS-06), no de Diana/Deni. Los KPIs de predicción/recomendación en las 9 capturas muestran SIN_DATO porque `gold.predicciones`/`recomendaciones` (mock ML-01/02) no cruzan con el catálogo real de 77,712 escuelas — documentado explícitamente, no oculto. Suite 840 passed, `vault_lint` limpio | [[vault/_DevLog/2026-09-03-oscar-quiroz-destrabe-bronze-9-capturas-reales]] | 🟡 En progreso — 9/10 completos, DB-10 pendiente de Emilio Galnares |

## Evidencia incremental — 2026-09-04 · DB-10 completa, BLOCK-004 cerrado por completo (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-223`, `US-224` | Confirmé que las historias de Emilio Galnares (US-121a…124a, CONAGUA/DS-06) ya estaban `done` antes de contactarlo; corrí su extractor/loader real (`extractor_conagua.py` + `cargar_bronze_conagua_real.py`, 180 presas reales, endpoint público IMTA/SISUAR). `dbt run --select cubo_pipeline` → **10 filas**. Al sincronizar encontré un bug real en `superset/sync_semantic_layer.py`: `FORMATO_D3` sin entrada para `formato: fecha` hacía que Superset rechazara el PUT completo del dataset (HTTP 422) — ninguna de las 4 métricas de DB-10 se aplicaba. Corregido con prueba de regresión (`tests/test_sync_formato_d3_cobertura.py`, 3 casos: mapeo directo, ningún formato produce d3format vacío, todo `formato:` usado en el repo está mapeado). Captura real de DB-10 insertada en `Manual_Usuario_Dashboards.md` — **10/10 dashboards con datos reales**. Límite cosmético aceptado tras 3 intentos: el tile "Última ingesta" no puede mostrar fecha legible en un `big_number_total` (d3-format numérico, no d3-time-format) — documentado, no bloqueante. Suite 855 passed, `vault_lint` limpio | [[vault/_DevLog/2026-09-04-oscar-quiroz-db10-completa-10-de-10-dashboards]] | ✅ 10/10 dashboards vivos — BLOCK-004 resuelto por completo |

## Evidencia incremental — 2026-09-04 · Verificación de BUG-031 y cierre de status (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-221` | Un aviso de equipo reportaba **BUG-031** `fixed`, con la parte de C2 ya resuelta por Luis Téllez (no Manuel, como decía la asignación original de la fila de arriba). Verifiqué contra el repo, no contra el mensaje: los commits del fix (`f013b20`, `b74a700`, 31-ago) ya son ancestros de `dev/oscar-quiroz`; `metrics_db01_db02.yaml`, `metrics_db03_db04.yaml` y `metrics_db06_db09.yaml` ya usan razón de sumas, sin `variacion_x_matricula` activa; las 2 aserciones de guarda (`test_semantic_db01_db02.py`, `test_semantic_db06_db09.py`) ya pasan. Corregí de paso la `nota` de KPI-02 en `metrics_kpis_base_us221.yaml` (describía la fórmula vieja); verifiqué contra la metadata real de Superset que ese archivo no se sincroniza (estructura `tarjetas:`, no `datasets:`), así que no había ningún dato incorrecto expuesto en producción — corregí mi propia hipótesis inicial antes de darla por buena. `test_kpis_us221` 4/4, `vault_lint` limpio | [[vault/_DevLog/2026-09-04-oscar-quiroz-verificacion-bug031-cierre-status]] | ✅ Sin pendientes de C2 en BUG-031 |

## Evidencia incremental — 2026-09-04 · US-214a filtro de ciclo, umbral y US-215a (C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-214a`, `US-212` | **Defecto de agregación por ciclo, corregido.** Los filtros globales de ciclo existían desde US-212 pero **sin valor inicial**: al abrir el tablero nadie ha filtrado, así que toda métrica agregada sumaba los 3 ciclos del cubo — `KPI-15` (DB-03) y `KPI-01` (DB-04) pintaban **32 312 donde el ciclo tiene 11 828, 2.7× inflado**, sin ningún error visible ⬜ · Detonado por el aviso de **Luis Téllez (2026-09-04)**: mismo defecto en `/api/v1/kpis` de producción (20.6M contra 6.7M); allá se arregló en la API, pero **los tableros no pasan por la API**, leen la base directo ✅ · Arreglo: clave opcional `valor_por_defecto` traducida al `defaultDataMask` de Superset, **aditiva y opt-in** — los tableros de Manuel, Monserrat y Oscar no cambian, con prueba de compatibilidad hacia atrás que lo exige ✅ · Afectaba a las **8 tarjetas** de ambos tableros, no solo a las de matrícula ✅ | Confirmar visualmente en navegador (US-215a §1.1) | ✅ |
| `REQ-002` | `US-214a` | **Verificado contra la propia API de charts de Superset**, no solo por prueba estática: `KPI-15` y `KPI-01` devuelven **32 312 sin filtro contra 11 828 con el ciclo** — 2.73× de inflación evitada ✅ · `defaultDataMask` persistido en el tablero desplegado (`NATIVE_FILTER-US203-1` en DB-03, `-0` en DB-04) ✅ · **Sin regresión del drill-down**: los links de US-214a siguen apuntando a los índices correctos, el filtro nuevo no corrió ninguna posición ✅ · `tests/test_filtro_ciclo_por_defecto.py`, 8 casos, cada guarda validada reintroduciendo su defecto ✅ | — | ✅ |
| `REQ-002` | `US-212`, `US-211a` | **CORRECCIÓN de la evidencia del PR #211.** El KPI-02 que reporté como **−0.192 %** es la **mezcla de los tres ciclos**, no el valor de un ciclo, así que no es lo que el tablero muestra. El correcto del ciclo vigente es **−0.496 %** ⬜→✅ · La mezcla **esconde el signo**: 2022-2023 fue **+0.242 %**, 2023-2024 −0.483 %, 2024-2025 −0.496 %. Un tablero que dijera "cayó 0.19 %" promediaría un año que subió con dos que bajaron ⬜ · **La concordancia entre cubos que verificaba BUG-031 se sostiene igual**: los cinco caminos dan −0.496 % al grano correcto, así que el fix de BUG-031 no se toca — lo que estaba mal era el encuadre del número, no los cubos ✅ | — | ✅ Corregido |
| `REQ-002` · `REQ-003` | `US-214a`, `US-212` | **`escuelas_en_riesgo = 0` NO es un defecto** (H1 de Luis). `src/modelos/riesgo.py` fija la sigmoide con dos anclas explícitas: variación `0.00`→riesgo `0.30`, y **variación `-0.05` (pierde 5 %)→riesgo `0.60`**, que es exactamente el umbral de DEC-006. Entonces `≥ 0.6` significa "se proyecta que pierda 5 % o más" ✅ · Verificado en datos: la escuela peor proyecta **−4.37 %** (riesgo 0.562); **ninguna llega a −5 %** ✅ · El cero es la afirmación verdadera de que nadie cruza el criterio que el equipo definió. Bajar el umbral desharía el significado de −5 % y volvería el número incomparable entre ciclos ⬜ · **Recomendación de C2: no mover DEC-006**; resolver con ranking por riesgo + driver dominante, o con una banda "atención" (0.40–0.60, 15 escuelas) como KPI adicional. **No se cambió nada**: DEC-006 no es de esta célula | Mesa con Manuel Serranía y Edgar Coronel antes del 9-sep | ⬜ Insumo entregado |
| `REQ-002` | `US-215a` | **Desbloqueada y arrancada.** El PR #208 de Edgar puso `vault/06_Quality_Testing/**` en `comunes`, cerrando el hallazgo H3 que reporté el 3-sep (la carpeta no estaba en el alcance de nadie, ni siquiera de quien escribiera el documento) ✅ · `Usability_Accessibility_Test_Plan_DB03_DB04.md` (`DOC-USABILIDAD-DB0304`), calcando el formato del plan de Monserrat para DB-05/DB-08 ✅ · **7 de 20 casos verificados hoy** con evidencia de datos o de API; los que exigen navegador quedan `⏳` explícitos, ninguno marcado como verificado sin correrlo ⬜ · Dos huecos del proyecto documentados sin rellenarlos: no hay CI de accesibilidad pese a que `Accessibility.md` declara Lighthouse como bloqueante, y `UX_Guidelines.md` está vacío con `source_of_truth: true` | Segunda pasada con navegador para el §3 | 🟡 En progreso |

## Evidencia incremental — 2026-09-04 · BUG-045 registrado: CONEVAL no reproducible (C2 reporta, C1 arregla)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` · `REQ-002` | `US-112`, `US-113`, `DS-07` | **BUG-045 dado de alta** (`high`, `open`, owner **Célula 1** — Diana Alvarez / Deni Garrido). ID según DEC-013: BUG-044 era el máximo escrito en `main` ✅ · Hallazgo reportado por Marina García el 3-sep al reconstruir su ambiente desde cero, **validado claim por claim por Luis Téllez el 4-sep** en revisión de solo lectura: veredicto verdadero en los cinco puntos ✅ · `rezago_municipio.sql` exige 9 columnas hasheadas del extracto oficial (`c_b9548dbd414b`…) más `_periodo_medicion`; el único fixture del repo emite `cve_mun, entidad, municipio, indice_rezago_social, grado_rezago, pobreza_pct` — el contrato viejo, y su generador lo cita explícitamente (`Data_Model.md §6`). **Ningún CSV del repo tiene columnas `c_…`** ✅ · Cadena rota completa: sin los Excel reales no hay `silver.rezago_municipio` → sin `gold.dim_municipio` → **sin ningún cubo** → **sin ningún tablero**. Reproducido corriéndolo: `column "c_b9548dbd414b" does not exist` ✅ | C1 publica fixture con esquema `c_…` o extiende el generador | ⬜ Abierto, de C1 |
| `REQ-001` | `US-112`, `DS-07` | **Precisión que corrige el punto inferido de la validación.** Luis dedujo del Bug_Register que el fixture carga en `coneval_v2`; verificado hoy: **ese var ya no existe**. `sources.yml` migró a `bronze_coneval_irs_identifier` → `coneval_irs_2020` y `bronze_coneval_pobreza_identifier` → `coneval_pobreza_2020`, y **ningún source de dbt apunta a `coneval_v2`** ✅ · Hace el hallazgo **más agudo**: el fixture no solo tiene el esquema equivocado, **ya no tiene destino en el pipeline** — está huérfano, y `cargar_bronze_fixture.py` sigue aceptando `--esquema coneval` sin protestar, así que quien lo use cree haber ingerido DS-07 sin ingerir nada que el pipeline lea ⬜ | — | ✅ Verificado |
| `REQ-001` · `REQ-007` | `US-112`, `US-113` | **Es un bloqueo distinto de BLOCK-004**, confirmado: BLOCK-004 está `resolved`, pero su solución automatizada (`src/ingesta/reproducir_bronze_real.py`, "el Camino A en un solo comando") cubre **solo DS-02 y DS-01** — verificado, no menciona CONEVAL ni DS-07 en ninguna línea ✅ · El comando que cerró BLOCK-004 no reproduce DS-07 ⬜ · **CI no atrapa la clase de error**: `dbt-contract` solo corre `dbt parse`, que renderiza el manifest sin ejecutar Silver contra datos, así que un fixture incompatible pasa el gate en verde ⬜ · Guarda propuesta y **no implementada** (es de C1): comprobar que cada `source` tenga un fixture con las columnas que su modelo Silver referencia | C1 decide si adopta la guarda | ⬜ Propuesto |
## Evidencia incremental — 2026-09-04 · US-106: verificación y declaración de freeze del esquema Gold (C1)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` | `US-106`, `US-113` | `vault/03_Architecture/Data_Lineage_US106.md` seguía fechado al 23-ago con 2/6 ítems del checklist de freeze en `[ ]`. Verificado contra evidencia real que ambos ya estaban cerrados desde el 30-ago: los 4 cubos de DEC-009 con cierre runtime real (Deni, `dbt test` 134/134 PASS sobre los 9 cubos Gold) y `coneval_periodo_medicion` resuelto de raíz (var eliminada de `dbt_project.yml`, período viaja como metadato real desde el extractor oficial). Diana reprodujo el flujo real de DS-07 en vivo el mismo día (`bronze.coneval_irs_2020` 2,472 filas, `bronze.coneval_pobreza_2020` 2,483 filas), confirmando la evidencia de Deni de forma independiente. Con los 5 ítems sustantivos cerrados, la evidencia sustenta el **freeze** del esquema Gold, dos días antes del objetivo del Sprint S5 (6-sep). **Nota de alcance (2026-09-04, Diana Alvarez):** el flip formal de `Data_Lineage_US106.md` (`status: draft` → `approved`, v1.0) NO se incluye en el PR de BUG-045/US-106 de Célula 1 — el archivo está fuera del `ownership.yml` de Diana (`check_ownership.py`, sección 3, hard fail) y fue revertido a su estado en `main` (`draft`) en ese PR para no bloquear el merge. El contenido del flip (frontmatter + checklist) queda preparado para que Edgar Coronel lo aplique en su propia rama/PR | `RISK-008` en `Risk_Register.md` sigue `abierto` — pendiente no bloqueante, ya solicitado a Deni directamente que actualice esa fila · flip formal de `Data_Lineage_US106.md` pendiente de Edgar Coronel (fuera de alcance de C1 en `ownership.yml`) | 🟡 evidencia de freeze verificada y cerrada · flip formal del documento pendiente (fuera de alcance de C1, para Edgar) |

## Evidencia incremental — 2026-09-04 · BUG-045 resuelto: fixtures de CONEVAL alineados al esquema real (C1)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` · `REQ-002` | `US-112`, `US-113`, `DS-07` | **BUG-045 cerrado** (`open` → `fixed`), atendido primero entre los pendientes del día por instrucción explícita de Diana Alvarez. Extendido `ESQUEMAS` en `src/ingesta/cargar_bronze_fixture.py` con `coneval_irs`/`coneval_pobreza` (columnas `c_…` hasheadas) y reescrito `generate_bronze_drivers_fixtures.py::generar_coneval` para emitir `bronze_coneval_irs_sample.csv` + `bronze_coneval_pobreza_sample.csv` en vez del fixture huérfano de esquema viejo, que se retiró del repo ✅ · **Mapeo hash→columna verificado contra evidencia real**, no inferido: los manifiestos de la propia carga DS-07 de Diana del 4-sep (`data/bronze/coneval/manifests/ds07_postgres_columns_{irs,pobreza}_2020.json`, artefactos locales) dan las 9 columnas completas, no solo las 4 ya documentadas en el registro del bug ✅ · Se tomó el camino "extender el generador" (opción 2 de las dos propuestas por Luis), consistente con el precedente de los 4 generadores de Formato 911 ✅ · Verificado sin Postgres (device_bash no alcanza Docker/`127.0.0.1:5432`): ambos CSV nuevos, leídos con `pd.read_csv(dtype=str, keep_default_na=False)`, no tienen columnas faltantes contra el esquema que el modelo exige — 12 filas c/u, 1 `SIN_DATO` ✅ · `tests/test_cargar_bronze_fixture_conteo.py` (único test existente sobre el módulo) solo usa `esquema="cct"`, sin riesgo de romperse ✅ — | 🟢 Verificado real contra Postgres: `pytest tests/ -q` → 884 passed/7 skipped; `dbt run --select rezago_municipio` → éxito (2,469 filas) contra las tablas reales de DS-07 de Diana; `dbt test --select rezago_municipio` → 6/6 pruebas propias del modelo en verde (el único ERROR de la corrida, `cubo_pipeline_rows_parity`, es de DS-06/CONAGUA, ajeno). De paso se encontró y corrigió un caso real: cargar el fixture contra una tabla que ya tiene datos reales (creada por el loader de producción, sin UNIQUE) fallaba con traceback crudo — ahora `cargar_fixture()` lo detecta y da un error accionable sin tocar ninguna fila real |

## Evidencia incremental — 2026-09-05 · BUG-048: SESNSP real, CONAPO diagnosticado, cubo_pipeline destrabado (C1)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` | `US-113`, `US-122b` | **BUG-048 `fixed (parcial)`** (alta hoy, Diana Alvarez): `dbt/models/sources.yml` apuntaba `bronze.sesnsp` al fixture (`sesnsp_test`) pese a existir el cargador real de Luis García — corregido a `'sesnsp'` (commit `165c911`), verificado contra Postgres real (`silver.delitos_municipio` 2,486 municipios, `comp_media` D2 estable). Avisado a Luis García ✅ · **Segundo hueco destapado, no resuelto**: D2/D1 siguen dependiendo de población CONAPO (DS-08) en fixture (36 filas) — Emilio Galnares sin responder; se investigaron y descartaron con evidencia real dos fuentes alternas (SSL expirado; grano estatal no municipal) y se documentó una tercera viable (`lapanquecita/poblacion-estimada`, GitHub, MIT) pendiente de decisión explícita si no hay respuesta antes del freeze ⬜ · **US-113/Deni**: `gold.cubo_pipeline` bloqueaba por falta de `bronze.conagua_presas` en el ambiente local — resuelto corriendo el extractor+cargador real de DS-06/CONAGUA (ya existentes, de Emilio): 180 presas reales, `cubo_pipeline` → 11 filas ✅ · **Hallazgo abierto, no corregido aquí**: `cubo_pipeline` reporta DS-08 como `cobertura_pipeline='OK'` (36 filas) aunque sea fixture, porque su `_source` coincide con el que usaría una carga real — valida en concreto la preocupación de Deni Garrido sobre ambigüedad fixture/dato-real, pendiente de discutir con ella y Edgar Coronel · Entregados a C3 (Andrés González Habib) y C5 (Luis Téllez) el mismo dump de Gold (`gold_bug048_drivers_2026-09-05.sql`, 18 tablas) para no generar versiones distintas, y a Deni Garrido un dump más reciente (`gold_bug048_pipeline_2026-09-05.sql`, 19 tablas, incluye `cubo_pipeline`) para que valide US-113 de punta a punta sin esperar a Emilio — ambos dumps verificados con evidencia real (conteo de `COPY`/`DROP TABLE IF EXISTS` = tablas esperadas, filas cruzadas contra corrida en terminal) y con el caveat explícito de que todo es real salvo DS-08/CONAPO | Emilio Galnares responde con el extractor real de DS-08, o Diana/Edgar deciden adoptar la fuente alterna de GitHub antes del freeze · comunicar a Deni/Edgar el hallazgo de `cubo_pipeline` sobre DS-08 · gitignorar o mover fuera del repo los dumps `.sql` sueltos en la raíz (hallazgo de hoy, no estaban protegidos como sí lo está `*.dump`) | 🟡 SESNSP/D2 real y cerrado · CONAPO/DS-08 sigue abierto, no bloqueante para el freeze gracias a los dumps entregados |
| `REQ-001` | `US-113`, `US-122b` | **Actualización el mismo día — CONAPO cerrado:** Emilio Galnares compartió el Parquet real de DS-08 (252,450 filas) por Teams. Verificado antes de programar (18 columnas de edad suman exacto `POB_TOTAL`, grano municipio×sexo×año) y construido `src/ingesta/cargar_bronze_conapo_real.py` (agrega sexo en el loader para no perder mitad de la población en el dedup de Silver, normaliza `_source` a `'DS-08_CONAPO'`) + default de `sources.yml` corregido a `'conapo'` (commit `19fecc4`). Verificado real contra Postgres: `comp_media` D2 sube de ~0.48 a ~0.62 y `d2_sin_dato` cae de ~36,700 a **0** en los 3 ciclos ✅ · Con esto **BUG-048 queda `fixed` de parte de C1** (SESNSP + CONAPO, los dos denominadores de D2); ya no aplica adoptar la fuente alterna de GitHub · Al sincronizar con `main` se encontró que esta alta compartía ID con una alta previa del PM sobre el mismo Gold empobrecido, ya con seguimiento de C3 (rerun de ML) — consolidadas en una sola fila en `Bug_Register.md` para no dejar dos altas activas con el mismo ID (mismo criterio que BUG-047/BUG-050, DEC-013) | Pendiente C5: importar el Gold regenerado a Cloud SQL y validar Superset — ahí cierra el bug completo | 🟢 C1 cerrado (SESNSP + CONAPO + cubo_pipeline) · pendiente despliegue de C5 |

## Evidencia incremental — 2026-09-04 · Fix ciclo por defecto en 7 dashboards + cct + st.html (C2, Manuel Serranía)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-203`, `US-204`, `US-205`, `US-213`, `US-222` | **BUG-047 dado de alta** (espejo de BUG-044 en dashboards, `high`, `fixed`, owner C2). BUG-044 (C4) fija el ciclo en la API; los tableros **no pasan por la API** (leen la base directo) y no estaban cubiertos. `valor_por_defecto: "2024-2025"` agregado (aditivo/opt-in → `defaultDataMask` de Superset) en los **7 dashboards** que declaran `id_ciclo`: DB-01, DB-02, DB-05, DB-06, DB-07, DB-08, DB-09 (DB-03/04 ya los cubrió Marina en US-214a; DB-10 monitoreo no declara `id_ciclo` y no aplica) ✅ · selectbox de `1_Dashboards.py` fija `index=len(CICLOS)-1` (2024-2025) ✅ · `cct` agregado **al final** de `filtros_globales` (índice 3) en DB-06/DB-09 para destrabar el drill-down de Marina (los IDs se generan por posición) ✅ · migración `st.components.v1.html` → `st.html()` (deprecado tras 2026-06-01; se conserva st.html sobre st.iframe por borde/border-radius + `allow=fullscreen`) ✅ · fix de caché + transporte (`st.cache_data.clear()` + `_tableros.clear()` + nuevo `except httpx.HTTPError`) ✅ · **causa raíz real del test en secuencia** (no era `st.cache_data`): AppTest comparte `sys.modules` y `SUPERSET_URL` queda congelada del test previo → fixture purga `MODULOS_FRONTEND` ✅ · `streamlit==1.62.0` en `requirements.txt` raíz (el CI solo instala el raíz; antes los tests de frontend se saltaban en silencio) ✅ | Confirmar visualmente en navegador (US-215a §1.1) | ✅ |
| `REQ-002` | `US-203`, `US-222` | Suite de la célula (frontend + capa semántica + sync + filtro ciclo): **226 passed**, incluido `tests/test_frontend_dashboards_streamlit.py` (33, los 3 antes-saltados ahora corren) ✅ · `ruff` / ownership (`test_check_ownership.py` 40) / `vault_lint` limpios ✅ · En la suite observable completa del ambiente persisten 21 `failed` en tests de validación (`great_expectations`, `'project_root_dir' and 'context_root_dir' are conflicting args` — conflicto de versión preexistente, ajeno) y 12 módulos que no colectan por faltar `limits` (slowapi de `src/api`) — ambos preexistentes y fuera del alcance de este PR | — | ✅ |

## Evidencia incremental — 2026-09-04 · US-214a: rutas a DB-06/DB-09, corrección del umbral y BLOCK-005 (C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-214a`, `US-204` | **Las 2 rutas que faltaban quedan implementadas.** Manuel Serranía agregó el filtro `cct` al final de los `filtros_globales` de `db06_predicciones.yaml` y `db09_recomendaciones.yaml` (PR #215, índice 3, solo sobre los datasets de grano escuela) ✅ · `link_db06` en `KPI-17 · Detalle de la predicción` y `link_db09` en `KPI-18 · Recomendación prescriptiva`, ambos fijando `id_ciclo` (0) y `cct` (3) ✅ · El de DB-09 es además el camino a la narrativa que Edgar eligió para la demo: de la ficha de escuela al ranking prescriptivo ✅ · **Contrato en 4 de 7 rutas implementadas**; las 3 restantes nacen en DB-01/DB-02, archivos de Manuel | Confirmación visual en navegador (US-215a §1.9 y §1.10) | ✅ |
| `REQ-002` | `US-214a` | **La guarda se cazó a sí misma, segunda vez.** Al extenderla a los links nuevos, `test_los_indices_del_link_apuntan_a_la_columna_que_dicen` falló en los tres links de DB-03: escaneaba el `.sql` completo buscando `NATIVE_FILTER-US203-N`, y ese archivo ahora define **tres** links con índices distintos, así que comparaba los índices de uno contra los filtros del tablero de otro. **La prueba estaba mal, no el código** ⬜→✅ · Corregida acotando el fragmento de cada link antes de validarlo ✅ · 24 casos, falsificados los dos escenarios nuevos (apuntar al índice equivocado; que Manuel quitara su filtro) ✅ · Verificado en vivo: **4 links, 8/8 destinos correctos** contra `native_filter_configuration` real ✅ | — | ✅ |
| `REQ-002` · `REQ-003` | `US-214a`, `US-212` | **CORRECCIÓN del análisis del umbral (§8.quinquies).** El dato *"la escuela peor proyecta −4.37 %, ninguna llega a −5 %"* se midió con **D1 vacío en las 145 escuelas** por BUG-045, con ML-01 en 4 de 6 drivers: no era representativo ⬜ · Con los fixtures de Diana el riesgo máximo pasa de 0.5615 a **0.7423** y **2 escuelas cruzan 0.60**, proyectando **−7.60 %** y **−6.19 %** — más del 5 % que define DEC-006, mientras la de 0.538 (−4.00 %) no cruza. **La calibración queda verificada empíricamente**, no solo por lectura del código ✅ · En producción sigue en 0 (esos CCT allá tienen 0.129 y 0.098): los fixtures son sintéticos y no representan a las ~132 000 escuelas reales; **el número de la demo será el de producción** ✅ · **Mejora el argumento**: ya no es "nadie cruza, confíen", sino "sí dispara, aquí está el caso a −7.60 %" | — | ✅ Corregido |
| `REQ-002` | `US-214a`, `US-212` | **DEC-006 ratificada por el PM, no se reabre.** Edgar Coronel aceptó la recomendación de C2: el umbral 0.6 se queda, la banda "atención" se descarta, y para la demo se usa el ranking de mayor riesgo con driver dominante y recomendación ✅ · Ese ranking ya existe y funciona con datos reales (chart de DB-09, US-204), y desde US-214a se llega a él directo con `link_db09`. **No queda trabajo de C2 por esta decisión** ✅ | — | ✅ Cerrado |
| `REQ-001` · `REQ-002` | `US-112`, `US-113`, `DS-07` | **BUG-045 verificado de punta a punta antes de darlo por bueno.** Cargados los fixtures de Diana en la base local: `silver.rezago_municipio` 0 → **12 filas**, D1 145/145 `SIN_DATO` → **145/145 con dato**, `gold.dim_municipio` gana **10 municipios** con pobreza y rezago ✅ · El ambiente **reproduce exactamente los números de Héctor** (ML-01 MAE 0.0844, ML-02 F1 0.6458, 5 de 6 drivers), lo que confirma que el repo ya se reproduce sin los Excel de nadie ✅ · Cierra el caso 2.7 de US-215a (KPI-14 salía vacío) ✅ | — | ✅ |
| `REQ-002` | `US-212`, `US-214a` | **BLOCK-005 dado de alta: los cubos `gold.cubo_*` no existen en Cloud SQL**, así que ningún tablero puede apuntar a producción. El import de L1 subió las 10 tablas de Gold y **0 matviews**, y los datasets virtuales leen `gold.cubo_*` y nunca `gold.fact_*` (regla 1 de `superset/README.md`, protegida por `test_semantic_repunteo_cubos`) ⬜ · **Diagnóstico corregido**: el DevLog de L1 dice que los 9 cubos referencian silver/bronze; verificado dependencia por dependencia, **eso aplica solo a `cubo_pipeline`**. Los otros **8** leen únicamente dims, `fact_escuela_ciclo` y `predicciones`/`recomendaciones` — **todo ya importado**. No hace falta subir silver ni bronze ✅ · Dueños propuestos: Luis Téllez (acceso/ejecución) y Diana Alvarez (validar modelos). No lo ejecuta C2: la instancia es de solo IP privada | Que C5 materialice los 8 cubos | ⬜ Abierto |
| `REQ-002` | `US-203`, `US-222` | Suite de la célula (frontend + capa semántica + sync + filtro ciclo): **226 passed**, incluido `tests/test_frontend_dashboards_streamlit.py` (**2** — el archivo tiene 2 tests; son 15 los de los 3 archivos de frontend que el CI se saltaba en silencio y ya corren) ✅ · `ruff` / ownership (`test_check_ownership.py` 40) / `vault_lint` limpios ✅ · En ambiente limpio (CI con `requirements.txt` raíz) la suite completa corre sin `failed` (0); los 21 `failed` de validación `great_expectations` y los 12 módulos sin colectar (falta `limits`, slowapi) observados en este venv local son **artefactos del entorno**, no del PR | — | ✅ |

## Evidencia incremental — 2026-09-04 · BUG-012 cerrado: runbook del ambiente local (PM)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-005` · `REQ-001` | `US-502`, `US-112`, `US-113` | **BUG-012 `open` → `fixed`** tras 6 días asignado al PM. Se escribe [[vault/00_Start_Here/Runbook_Ambiente_Local]] (`approved`, `source_of_truth: true`) y **se verifica corriéndolo de punta a punta** en una máquina sin `.env` ni `~/.dbt/profiles.yml` ✅ · Documenta los 4 pasos que ningún artefacto recogía y que el equipo venía reconstruyendo de DevLogs ajenos: `dbt seed` antes de `dbt run`, **ML antes de los cubos**, `export POSTGRES_HOST=localhost` (el `.env` trae `db`, que solo resuelve dentro de Compose) y `DATABASE_URL` para `publicar_gold` ✅ · **Corrige dos cifras que la documentación previa tenía mal**: son **tres** fixtures de Formato 911 a la misma tabla (242 filas), no dos —el tercero llegó con BUG-026— y **ocho** fixtures de drivers, no siete, porque BUG-045 partió CONEVAL en `irs`/`pobreza` el mismo día ✅ · Cada paso queda escrito con su cifra esperada (242 / 145 / 145-3 ciclos / 55+55 / 8-de-9 / 103 charts / 9 tableros / `kpis`=11 828) para que una desviación futura se note en el paso exacto donde ocurre, que es justo lo que falló cuando Héctor Morales siguió un runbook desactualizado y atribuyó a un modelo de C1 un defecto propio ✅ | Que C1 convierta `dbt/README.md` (hoy el scaffold de `dbt init`) en un puntero al runbook | ✅ |
| `REQ-003` | `US-313` | **Hallazgo al verificar el runbook**: con el pipeline completo post-BUG-045 el ambiente de fixtures devuelve **2 escuelas en riesgo** (máx. `indice_riesgo` **0.7423**, peor variación proyectada **−7.60 %**) y 12 en la banda 0.40–0.60 ⬜ · Las corridas del 3-sep reportaban **0** con máximo 0.5615. No es aleatoriedad —ML-01 fija `random_state: 0`— sino **cobertura de datos**: aquel día CONEVAL no tenía fixture compatible y **D1 iba vacío**; hoy `d1_pobreza` tiene dato en **145 de 145** ✅ · No invalida la ratificación de DEC-006 (el umbral 0.60 ↔ −5 % sigue siendo el criterio correcto y el ranking sigue siendo la narrativa de la demo), pero **sí invalida la premisa de que el conteo siempre dará cero**: quien cite aquel «0» está citando un ambiente con un driver menos | Reconfirmar el conteo contra los datos reales de producción antes del 9-sep | ⬜ Insumo para la demo |
| `REQ-005` | `US-502` | Pendiente ajeno detectado y **no corregido** por alcance: `scripts/verificar-servicios.sh` consulta `localhost:8000/health`, pero `src/api/app.py:131` monta todo bajo `/api/v1` → reporta un **FAIL falso** de la API a quien lo use para verificar su ambiente. También consulta ChromaDB en `/api/v1/heartbeat` cuando el healthcheck del compose usa `/api/v2/heartbeat` ⬜ · `scripts/**` es amarillo de C5, no del PM | Luis Téllez corrige las dos rutas | ⬜ Abierto, de C5 |

## Evidencia incremental — 2026-09-04 · plan de cierre de Estefany post-PR #197

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-321`, `US-322`, `US-325` | Revisión contra código, pruebas, fichas y `Execution_Status`: PR #197 ya resolvió la reproducción de Bronze DS-01/DS-02 y sus suites GE, pero aún se requiere materializar Gold y ejecutar la evidencia real. Se publica [[vault/15_ML_Models/Plan_Cierre_Estefany_US321_US322_US325]] con fases, criterios de paro, prompt maestro y revisores. Se conserva el baseline de ML-03 (**Silhouette 0.1086 < 0.30**) y no se cambia ninguna historia a `done` sin nueva evidencia | Ejecutar Bronze → Silver → Gold; correr EDA/cobertura real; iterar ML-03 y solicitar decisión de Andrés/Edgar si no alcanza el umbral | 🟡 Plan listo; ejecución pendiente |

## Evidencia incremental — 2026-09-04 · inicio de ejecución de Estefany

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-321`, `US-322`, `US-325` | `src/modelos/ejecutar_cierre_ml03.py` incorpora un punto de entrada desde `gold.features_escuela` para EDA, correlaciones sin target, cobertura territorial y ML-03 temporal; emite sólo agregados, no registra MLflow por defecto y conserva el bloqueo si `casos_completos` deja cero filas. Tres pruebas nuevas cubren privacidad, bloqueo y ejecución. La corrida Gold real no se declara: el host no tiene Docker ni `.env` | Ejecutar el comando en un ambiente conforme al runbook; revisar agregados y solicitar decisión de Andrés/Edgar si Silhouette sigue <0.30 o no hay casos completos | 🟡 Ejecución iniciada; evidencia real pendiente |

## Evidencia incremental — 2026-09-04 · BUG-048: producción sirve un Gold empobrecido (PM)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-001` · `REQ-005` | `US-113`, `US-505`, `DS-07` | **BUG-048 dado de alta** (`high`, `open`; ID según DEC-013, BUG-047 era el máximo en `main`). Hallado al comparar producción contra el ambiente local reconstruido con el runbook nuevo: la URL pública devuelve `indice_completitud_drivers` **0.197** y `escuelas_en_riesgo` **0**, mientras el mismo código con el pipeline completo da **0.648** y **2 escuelas** ⬜ · **No es regresión de código ni de despliegue**: `/version` = `33fcbbb` trae los fixes de BUG-044 y BUG-046, y la matrícula de prod es la correcta del ciclo vigente (6,704,229) ✅ · La causa es que el snapshot `gold_real.sql` importado a Cloud SQL en L1 se generó el **3-sep, antes del fix de BUG-045**, cuando CONEVAL no tenía fixture compatible y **D1 iba vacío**; hoy en local D1 tiene 145/145 ⬜ · Completitud por driver medida en local: D1 145/145, D2 145/145, D3 133/145, D4 133/145, D6 5/145, D5 0/145 (CONAGUA no ingerida, correcto por cobertura parcial) ✅ | C1 regenera Gold post-BUG-045 y C5 lo sube con el procedimiento de L1 | ⬜ Abierto, de C1+C5 |
| `REQ-005` | `US-505` | **Impacto directo en la demo del 9-sep**: quien abra la URL pública hoy ve cero escuelas en riesgo y 20 % de completitud — los dos números que peor cuentan la historia del proyecto, y ninguno refleja el estado real del pipeline. Prioridad por encima del resto de la cola de PRs ⬜ · **Hueco de observabilidad que lo permitió**: nada compara la completitud de producción contra la del pipeline, así que una degradación de datos es invisible mientras el código siga en verde — el smoke de despliegue solo valida rutas HTTP y la cifra de matrícula ⬜ · Guarda propuesta y **no implementada** (es de C5): que el smoke falle si `indice_completitud_drivers` cae por debajo de un umbral acordado | C5 decide si adopta la guarda | ⬜ Propuesto |

## Evidencia incremental — 2026-09-04 · BUG-047: resolución dinámica añadida sobre el fix de Manuel/Marina (C2)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-203`, `US-204`, `US-211a`, `US-211b`, `US-222` | Edgar reportó (mismo aviso a Manuel) revisar tiles "total" de matrícula/escuelas sin filtro de ciclo vigente. Verifiqué dos veces la afirmación de Manuel por Teams ("ya quedó... 2024-2025 como default") contra `git fetch`/diff de ramas/PRs abiertos vía API y contra Superset en vivo — en ambas ocasiones no encontré evidencia, porque el fix real (**BUG-047**, `dev/manuel-serrania`) llegó a `main` después de esas dos verificaciones. Diagnostiqué la causa raíz por mi cuenta antes de verlo y, al sincronizar con `main`, encontré que ya estaba registrado — retracté mi propio número duplicado (`BUG-050`) por DEC-013 y consolidé mi evidencia dentro de `BUG-047` ([[vault/06_Quality_Testing/Bug_Register]]), mismo criterio que usó Héctor con BUG-041→043. Aporté una mejora aditiva: el mecanismo de Manuel/Marina (`valor_por_defecto`, estático, "hay que actualizar este valor a mano cada ciclo") queda como respaldo; agregué `default: ultimo_ciclo` en `_filtros_nativos()`, que resuelve el valor más reciente dinámicamente contra los datos reales vía `/api/v1/chart/data` y tiene prioridad cuando puede resolver. Preservé la firma y las pruebas existentes (`tests/test_filtro_ciclo_por_defecto.py` sigue pasando sin tocarlo). Verificado antes/después en los 9 datasets con métrica de conteo absoluto de los 9 dashboards con `id_ciclo` (tabla completa en `BUG-047`); DB-10 confirmado fuera de alcance. Recapturé la evidencia visual de mi propio tablero (DB-07) en `Manual_Usuario_Dashboards.md` v1.3. `tests/test_filtros_nativos_default_dinamico.py` (5 casos nuevos). Suite 872 passed, `vault_lint` limpio | [[vault/_DevLog/2026-09-04-oscar-quiroz-bug047-resolucion-dinamica-ciclo]] | ✅ Mejora aditiva aplicada y verificada sobre el fix ya existente de Manuel/Marina |

## Evidencia incremental — 2026-09-05 · Corrección: el cero de producción tampoco es confiable (C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-003` | `US-214a`, `US-212` | **Segunda corrección al análisis del umbral, señalada por Edgar Coronel al aprobar el PR #229.** En §8.quinquies.3.bis escribí *"el número de la demo será el de producción"*, dando por bueno el `escuelas_en_riesgo: 0` de allá ⬜ · **Es el mismo error, una capa más abajo**: producción sirve el snapshot de Gold **anterior al fix de BUG-045**, o sea con D1 vacío — exactamente la contaminación que volvió no representativa mi medición original de −4.37 % ⬜ · **Verificado el 2026-09-05**: `indice_completitud_drivers` vale **0.1966** en producción contra **0.6485** en local con el pipeline completo ✅ · Los 0.129 y 0.098 que consulté allá salen de ese mismo Gold empobrecido, así que **no se sabe qué dará `escuelas_en_riesgo` en producción tras el refresco**. Registrado como **BUG-048**, va junto con **BLOCK-005** ⬜ · Corregido en el contrato y en el DevLog, dejando visible qué se afirmó y por qué era falso | Refrescar Gold en Cloud SQL (BUG-048 / BLOCK-005) antes del 9-sep | ✅ Corregido |
| `REQ-002` | `US-214a`, `US-212` | **Lo que sí se puede afirmar el 9 de septiembre**, acotado explícitamente para que nadie cite una cifra que no se sostiene: que el umbral 0.6 significa "proyecta perder ≥ 5 %" y está calibrado contra DEC-006 ✅ · que la sigmoide dispara cuando debe y no cuando no debe (0.7423→−7.60 % y 0.6692→−6.19 % cruzan; 0.5384→−4.00 % no) ✅ · **lo que NO**: que el conteo visible en producción sea el número real del país, hasta que se refresque Gold ⬜ · Ninguno de los dos entornos da hoy una cifra defendible de cuántas escuelas están en riesgo; lo defendible es que el mecanismo funciona y es auditable | — | ✅ |

## Evidencia incremental — 2026-09-05 · US-207 entregada, BUG-049 con causa raíz y US-215a 22/22 (C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-207` | **Entregada.** Llevaba desde el 7-ago con 10 líneas de andamiaje. Al re-verificar los supuestos, **tres de las cuatro razones por las que la creía bloqueada eran falsas**: `src/frontend/**` está en el amarillo de C2, `criticos` avisa y no reprueba, y la API responde sin token (`AUTH_LECTURA_PUBLICA=true`) ✅ · `prediccion_client.py` (nuevo, seam HTTP inyectable) + `pages/2_Panel_ML.py` reescrito: **ML-01** (riesgo contra el umbral 0.60 de DEC-006), **ML-02** (driver dominante + recomendación) y **ML-03 como `SIN_DATO` explícito** — US-321 no ha aterrizado y el contrato devuelve `cluster: null`; se pinta el hueco en vez de inventar un segmento ✅ · **Verificado contra la API real, local y producción**: `15DJN0049A` riesgo 0.7423 → D1 → becas; `09DSN0042A` riesgo 0.6692 → D2 → rutas escolares seguras. **Riesgo parecido, recomendación distinta: el diferenciador del proyecto funcionando** ✅ · 22 casos nuevos, falsificados (convertir `cluster` nulo en 0, mover el umbral a 0.50, tratar el 404 como error genérico) ✅ | Que el PM ratifique el alcance (2 de 3 modelos) | 🟡 Entregada, pendiente de alcance |
| `REQ-002` | `US-215a`, `US-212`, `US-214a` | **BUG-049 `fixed`, y la causa raíz NO era la que registré.** Lo di de alta como "ajustar `alto`/`ancho`"; al investigarlo resultó que `_layout_grilla()` ponía **cada chart en su propia fila**, así que el `ancho` de los YAML no servía para nada — cuatro tarjetas de `ancho: 3`, escritas para sumar 12, se apilaban con nueve doceavos vacíos a la derecha ⬜→✅ · Verificado contra el tablero desplegado **antes** de tocar nada: DB-03 tenía 11 charts en 11 filas ✅ · Nuevo `_agrupar_en_filas()`: **DB-03 pasa de 11 filas a 5, DB-04 de 13 a 5**, y los 7 tableros que usan este layout dejan de ser tiras verticales ✅ · **Toca 7 tableros de 3 dueños**, por eso las guardas son de invariantes y no del caso propio: ninguna fila excede 12, no se pierde ni duplica ningún chart, se respeta el orden, y el árbol conserva sus `parentId`. 18 casos ✅ | **Revisión visual de Manuel Serranía y Oscar Quiroz** sobre sus tableros | ✅ |
| `REQ-002` | `US-215a` | **22 de 22 casos cerrados — historia completa.** El último caso, el **3.1** (contraste), cerró el 2026-09-05 con el criterio de **DEC-016** ya publicado ✅ · Medido con `getComputedStyle` **elemento por elemento** sobre color y fondo **efectivos**, en DB-03 y DB-04 y en los dos temas — no con el score agregado de Lighthouse, que señala que hay contraste insuficiente pero no **en qué elemento**, y DEC-016 decide justamente por elemento ✅ · **Todo lo que escribe FARO pasa**: peor caso **19.26:1** en claro y **18.42:1** en oscuro ✅ · **Reprueban dos elementos heredados** del shell de Superset —`Published` **2.16:1** y `Edit dashboard` **3.07:1** claro / **3.41:1** oscuro— que por DEC-016 son limitación conocida con su medición y **no bloquean**; adjuntada a **BUG-050**, que sigue `open` ✅ · **Medir por elemento destapó un defecto propio que el score escondía**: los 4 `<a href>` de drill-down de DB-03/DB-04 tomaban el azul de acento de Superset y daban **3.26:1** en tema claro —en oscuro pasaban con 5.91:1, por eso no se había visto el 4-sep. Ningún color único resuelve los dos temas (los rangos de luminancia no se cruzan), así que heredan el color de la celda y se marcan con subrayado: **19.26:1 / 21:1**, y cumple WCAG 1.4.1 ✅ · Guarda `test_el_link_no_depende_del_color_del_tema`, validada reintroduciendo sus dos defectos ✅ | **Hallazgo para Monserrat Miranda:** `db05_cubo_driver.sql:70` tiene el mismo link sin estilo; cae en `US-215b`/`BUG-051` y no se toca desde aquí | ✅ **22/22** |
| `REQ-002` | `US-207`, `US-214a` | **Nota de alcance, para que quede trazado**: se actualizó una aserción incidental de `test_semantic_db01_db02.py::test_el_layout_genera_estructura_v2` (prueba de **Manuel Serranía**), que codificaba el comportamiento defectuoso de BUG-049 — con dos charts de ancho 3 y 6, que suman 9 y caben juntos, exigía dos filas. Su intención declarada (el árbol v2) se conserva entera ✅ · Cambio acotado sobre `tests/**`, que es amarillo, avisado en el DevLog y en el PR | Revisión de Manuel | ✅ |

## Evidencia incremental — 2026-09-05 · US-207: documento de vault, cierre de Definition of Filed (C2)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-207` | **Cierra el entregable completo de la historia.** Su plan de sprint pide *"Código en `src/frontend/` + **documento en el vault con frontmatter** + fila en la matriz"*; el código y la matriz estaban, el documento **faltaba** ⬜→✅ · `vault/04_UX_Design/Panel_ML_US207.md` (`DOC-PANEL-ML-US207`), listado en el `_index` de su carpeta ✅ · Va en `04_UX_Design` y no en `03_Architecture` porque **esa carpeta no está en el alcance de Célula 2**: `Frontend_Architecture.md` es de Manuel Serranía y sigue siendo el canónico de la capa web. El documento nuevo especifica **una sola página** al nivel que la arquitectura no baja, igual que los `Cube_Specs_*` respecto de `Data_Model.md` (regla 1 del vault) ✅ | Ratificación de alcance por el PM | ✅ |
| `REQ-002` · `REQ-003` | `US-207`, `US-321` | **Queda documentado por qué se entregan 2 de 3 modelos.** ML-01 y ML-02 con datos reales; **ML-03 como `SIN_DATO` declarado**, no escondido: `gold.predicciones` no tiene columna `cluster` porque US-321 (Estefany Hernández, C3) sigue en curso, y el contrato lo marca explícitamente (`schemas.py::PrediccionOut`, `cluster: StrictInt \| None`) ✅ · Verificado el 2026-09-05: `cluster` viene `null` **en local y en producción** ✅ · Cuando US-321 publique **no hay que tocar la página**: el cliente ya propaga el valor y hay una prueba que lo verifica (`test_cuando_ml03_aterrice_el_cluster_se_respeta`) ✅ | US-321 aterriza | ✅ Declarado |
| `REQ-002` | `US-207` | **Hallazgo para Manuel Serranía**, encontrado al implementar: [[vault/03_Architecture/Frontend_Architecture]] §5 dice que el front hace **`POST`** a los endpoints de inferencia, pero el contrato real es **`GET /api/v1/predicciones/{cct}`** (US-412) — verificado en local y en producción al construir el cliente ⬜ · No se corrige desde C2: `vault/03_Architecture/**` no está en su alcance y el documento es suyo | Manuel corrige §5 | ⬜ Reportado |
## Evidencia incremental — 2026-09-04 · US-215b cerrada y BUG-038 corregido (C2, Monserrat Miranda)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-215b` | **13 de 13 casos ejecutados: 12 ✅, 1 ⚠️, 0 ❌.** Plan `DOC-USABILIDAD-DB0508` pasa a `status: approved`. Método de US-215a (Marina): verificar por datos/API lo verificable y **no marcar nada sin correrlo**. `2.4` cerrado con la prueba discriminante — **309 filas `SIN_DATO` sin un solo valor**, conviviendo con **60 ceros legítimos** en filas `OK`: el cero real existe y no se confunde con el hueco (R2) ✅ · `2.3` pivote `rowTotals`/`colTotals` en `false`, `/api/v1/chart/data` responde 180 filas ✅ · `2.1` **reescrito antes de ejecutarlo**: su esperado era anterior a BUG-047 y habría marcado falla falsa, hoy el Ciclo llega preseleccionado a propósito ✅ · regresión de índices por posición revisada: `link_db08` sigue apuntando a `-3`/`-4` correctos ✅ · `3.3` foco visible ✅ **con matiz que invalida la auditoría ingenua**: el indicador es `box-shadow`, no `outline` (que es `none`), y un `.focus()` programático no dispara `:focus-visible` · `3.2` ✅ **con comprobación humana**: el navegador automatizado no entregaba `Enter` a los tabs de React y habría dado un **falso negativo**; se dejó sin marcar hasta que la autora verificó a mano que `Tab`+`Enter` cambia de pestaña — era artefacto de medición, no defecto | — (§3.2 y el tema claro de §3.1 se cerraron el mismo día) | ✅ `US-215b` |
| `REQ-002` | `US-213`, `US-214b` | **BUG-038 `fixed`** — eran **dos** defectos y por eso las dos hipótesis del 30-ago fallaron, cada una tapaba uno: `ROOT_ID` declarado `type: "TABS"` (Superset espera `ROOT` con el contenedor de tabs aparte) **y** un `GRID-<id>` interpuesto entre cada `TAB` y sus `ROW` (con tabs las filas cuelgan directo del TAB). Árbol corregido a `ROOT_ID(ROOT) → TABS-ROOT → TAB → ROW → CHART`; **cambio aditivo**, `_layout_grilla()` y los 8 tableros del camino plano intactos (suite 886 passed / 0 failed). **Los tests estaban en verde y mentían**: 3 codificaban la estructura defectuosa como la esperada —el registro decía 1— más 3 que dependían del GRID; los 6 reescritos con dos guardas nuevas por defecto. **Verificado en navegador real** contra Superset 6.1.0, que es la lección del bug: 6 tabs navegables con valores propios (D1 52.7 %/18, D4 30.9 %/17) y **los filtros globales llegando a los charts** — Jalisco → 0 de 7, contrastado contra `gold.cubo_driver`, no sólo contra la pantalla | Revisión de Manuel Serranía (dueño de la convención US-202) | ✅ |
| `REQ-002` | `US-215b`, `US-213` | **BUG-051 dado de alta** (`low`, `open`; ID según DEC-013, BUG-050 era el máximo en `main` tras el sync). La etiqueta del tab activo de DB-05 queda en **4.07:1**, bajo el AA de 4.5:1 para texto normal. Es el acento de Superset, no un color propio, pero el texto lo introdujo US-213 y es el control con el que se navegan los 6 drivers. En el mismo barrido **30 de 32 nodos sí cumplen AA**. No se corrige por cuenta propia: el acento afecta a los 10 tableros y **`UX_Guidelines.md` está vacío con `source_of_truth: true`**, así que hoy no hay paleta oficial contra la cual ratificar un reemplazo | Decisión de Manuel (US-202) + sistema de diseño | 🟡 abierto |

## Evidencia incremental — 2026-09-05 · US-004 cerrada: cola de validación resuelta y estado al día (PO)

| REQ | Historias | Evidencia / decisión | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-004` | **Se resuelve el acuerdo pendiente de [[vault/13_Reports/US_Validation_Followup_2026-08-28]]**, que era el único ítem abierto de esta historia: *"resolver los acuerdos de la tabla, regenerar el tablero y cerrar TEST-002"*. De las **20 historias** que quedaron en la cola de validación del 28-ago, **8 cerraron** (`US-121a`, `US-122a`, `US-123a`, `US-124a`, `US-213`, `US-221`, `US-411`, `US-412`) y **12 están en revisión con PR mergeado**; **ninguna quedó en `in_progress` ni `planned`** ✅ · `TEST-002` (`validate_pm_dashboard.py`) en verde sobre el snapshot regenerado ✅ · El bloque §Estado del proyecto de esta matriz llevaba cifras de agosto ("0 REQ Done", "7/7 pendientes de ejecución") y se sustituyó por cifras **derivadas** de `Execution_Status` vía el generador, no capturadas a mano ✅ · La parte de **mantenimiento** de la historia no se declara terminada: pasa al rol rotativo de `US-005`, que es donde debe vivir | El Vault Steward de cada sprint refresca este bloque con el tablero | ✅ Entregable cerrado; el mantenimiento continúa bajo `US-005` |
| `REQ-007` | `US-005` | **Rotación del Vault Steward creada**: [[vault/_Meta/Vault_Steward]] con rol, lista de verificación semanal y turnos asignados. **Se registra sin adornos que la rotación no operó en S1–S4** y se documenta el costo medible de esa ausencia, con los hallazgos de esta semana como evidencia: el hueco de `ownership.yml` que apareció **cinco veces**, `guia-ambiente-local/configuracion.env` versionado contra `Secrets_Policy`, la fila mal formada de `BUG-018` que contaba como abierta, tres documentos de ambiente local solapados contra la regla 1, y la colisión de `BUG-049` entre dos personas el mismo día ✅ · Cierra la mitigación de `RISK-006`, que dependía de "linter + steward rotativo" y sólo tenía la mitad | Turno de S6 corre la lista antes del freeze | ✅ Rol definido y turnos asignados |
| `REQ-007` | `US-004`, `US-005` | **Estado `descoped` introducido** (`DEC-014`): `US-413`, `US-414` y `US-525a` salen del alcance sin marcarse como entregadas. El generador las reporta aparte y el validador exige que **alcance + recortadas = 91**, para que una historia no pueda desaparecer del tablero en silencio ✅ · Alcance 91 → 88, avance 73.5 % → 76.4 % sin afirmar ninguna entrega que no ocurrió | — | ✅ Aplicado y validado |


## Evidencia incremental — 2026-09-05 · BUG-052: la suite de tableros de Streamlit es intermitente (PO)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-206`, `US-214b` | **`BUG-052` dado de alta** (`medium`, `open`; ID según `DEC-013`, `BUG-051` era el máximo en `main`). `tests/test_frontend_dashboards_streamlit.py` falla **2 de cada 6 corridas** sin que cambie el árbol, y **no siempre la misma prueba** — medido por **Marina García** sobre `f2ee9d2`, worktree limpio, mismo intérprete ✅ · Causa raíz **ya documentada en el propio fixture** (`superset_fake`, líneas 66-71): `AppTest` comparte `sys.modules` y `SUPERSET_URL` queda congelada de un puerto efímero ya apagado; la purga de módulos mitiga pero no cierra, y no se limpia `cache_resource` ✅ · **Corroborado de forma independiente antes del reporte**: el docstring del fixture `autouse` de `test_frontend_panel_ml_streamlit.py` ya decía que *"la fragilidad de esa suite es previa"* ✅ · **Corrección de mi propio diagnóstico:** lo declaré "no reproducible" tras **una sola corrida verde**, que es inferencia inválida sobre algo que falla 1 de cada 3. Volví a medir: 12 de 12 en macOS, así que no lo reproduzco aquí — pero eso acota la plataforma, no desmiente el defecto ✅ · **No lo introduce ni lo arregla el PR #234**: sin cambios entre `f2ee9d2` y la punta en ese archivo ni en `src/frontend/**` ✅ | C2 aplica el patrón que ya existe en el repo: limpiar `cache_data` **y** `cache_resource` antes de cada prueba | ⬜ Abierto, de C2 (Manuel Serranía) |
## Evidencia incremental — 2026-09-05 · BUG-048 rerun ML sobre Gold post-BUG-045 (C3)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-302`, `US-311`, `US-313` | Dump definitivo C1 con CONAPO real importado: 136,046 features/3 ciclos, D1/D2 completos y nueve cubos. `loss="absolute_error"` mantiene MAE 0.141458; riesgo 0.0292–0.5717 y 0 escuelas sobre 0.6, porque ninguna caída predicha alcanza el 5 % ratificado. ML-02 F1 0.8333; 45,276 recomendaciones de cinco drivers (D1 2,843; D2 27,075; D3 2,104; D4 12,835; D6 419). `gold_bug048_final2_2026-09-05.sql` restaurado desde cero: ocho vistas + `cubo_pipeline`; SHA-256 `6d81527b92aa186ae15837e66a03d177dffa8e466e5282847f05047a95e53b09` ✅ | C5 importa con backup y valida producción/Superset | 🟡 C3 completado; BUG-048 abierto hasta C5 |
| `REQ-003` · `REQ-004` | `US-302`, `US-412` | **BUG-053, paso C3 completo:** `TreeExplainer` batch persiste `shap_d1`…`shap_d6` nullable en `gold.recomendaciones`; migración idempotente de tabla existente; D1–D4/D6 poblados en 45,276 filas y D5 en 45,276 `NULL` reales, cero `NaN`. Dump restaurado desde cero con SHA-256 `6d81527b92aa186ae15837e66a03d177dffa8e466e5282847f05047a95e53b09`. 60 pruebas enfocadas ✅ · Ruff ✅ | Diana revisa esquema; C4 lee Gold y reemplaza mock de `/explicacion` | 🟡 C3 listo; pendiente C1/C4 |


## Evidencia incremental — 2026-09-05 · BUG-044: cache del ciclo y alcance global del default (C4)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-411` | **Cierre de los dos pendientes menores que Christian Ruiz dejó al revisar `BUG-044`.** La historia y el bug ya estaban cerrados y **no cambian de estado**: esto es endurecimiento y documentación, no un arreglo nuevo ✅ · **(1) Cache TTL de `_ciclo_mas_reciente()`**, 300 s, a **nivel de clase** y no de instancia — `get_repositorio_gold()` crea una instancia por petición vía `Depends`, así que un cache de instancia no habría servido de nada; evita un `SELECT MAX(id_ciclo)` extra en cada llamada a `/escuelas`, `/kpis` y `/escuelas/{cct}`. El TTL se elige contra la cadencia real de `dbt` (días), no contra la de las peticiones, y se prefiere sobre la invalidación explícita que sugería el reviewer por el riesgo de servir un ciclo obsoleto ✅ · **(2) Alcance del default documentado en tres lugares**: es el **máximo global** de `fact_escuela_ciclo`, **no por `cve_ent`/`cve_mun`**. Una entidad sin filas para ese ciclo devuelve **lista vacía**, no su propio último ciclo disponible. **No es un bug** —es el comportamiento correcto—, pero sin documentarlo se lee como uno; para ver una entidad rezagada hay que pedir `ciclo` explícito ✅ · 3 pruebas nuevas (`tests/test_repositorio_gold_cache.py`), sin Postgres, con motor falso que cuenta ejecuciones | Vigilar en la demo: filtrar por una entidad sin datos del ciclo vigente devuelve vacío por diseño, no por falla | ✅ `US-411` sigue `done`; `BUG-044` sigue `fixed` |
## Evidencia incremental — 2026-09-05 · Cierre de hallazgos de C4 y séptimo hueco de propiedad (PO)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` | `US-416`, `US-412`, `US-422` | **Fila que faltaba del PR #240 de Christian Ruiz**, que la plantilla pide y su PR no traía — la agrega el PM porque la matriz es su crítico. **`US-416` ratificada** por C4 tras leer el código, no el reporte: `SET LOCAL` en vez de `SET` (muere con la transacción y no deja el `statement_timeout` pegado en conexiones que vuelven al pool compartido con `RepositorioGold`) y endurecimiento a `SQLAlchemyError` ✅ · **Ventana de `KeyError` cerrada en `cache_predicciones.py`**: leer con `in` + `[]` consulta el reloj **dos veces** y una entrada vigente puede caducar entre ambas → `KeyError` → 500; el `Lock` no protege de eso, guarda el estado pero no detiene el tiempo. **Verificado por el PM revirtiendo el parche**: las 2 pruebas de regresión fallan con `KeyError` real desde `cachetools/__init__.py:105`, y pasan con el parche ✅ · **Corrección de documentación de seguridad**: `API_Specification` prometía que `/batch` y `/explicacion` eran solo analista con 403 y **nunca lo fueron** — el router se monta con `require_lectura` (`v1/__init__.py:25`). Se alineó la documentación al código, no al revés (`DEC-017`) ✅ | El E2E con Postgres real es `US-422` (Eloisa González), confirmado contra el padrón | ✅ `US-416` ratificada; cierra al pasar a `done` |
| `REQ-003` · `REQ-004` | `US-412`, `US-302` | **`BUG-053` dado de alta** (`medium`, `open`; ID según `DEC-013`, `BUG-052` era el máximo en `main`): `/explicacion` sirve `mock_data`, no SHAP, y **no existe fuente que leer** — `explicar_driver` no lo llama nadie y `publicar_gold` no persiste contribuciones. Verificado de forma independiente por el PM con `grep` en todo `src/` ✅ · Orden de cierre: **C3 persiste en Gold → C4 lee → prueba**; el primer paso no es de C4. Las 8 pruebas de contrato ya están escritas ✅ · **Alcance para la demo**: el driver dominante y la recomendación de `/predicciones/{cct}` sí son reales; lo mock es el desglose | C3 decide si entra antes del freeze | ⬜ Abierto, declarado, no bloqueante |
| `REQ-007` | `US-001` | **Séptimo hueco de propiedad cerrado**: `guia-ambiente-local/` no estaba en ningún alcance —ni verde, ni amarillo, ni comunes—, así que el gate **reprobaba a quien intentara cumplir `Secrets_Policy`** haciendo el `git rm --cached` de `configuracion.env`, versionado y reportado por **Monserrat Miranda** y ratificado por **Christian Ruiz** ✅ · Carpeta a `comunes` y archivo des-trackeado en este PR; sigue en disco y ya lo cubre `*.env` de `.gitignore`. Revisado antes de tocarlo: 7 líneas, puertos de Airflow y MLflow, **cero credenciales** — riesgo bajo, incumplimiento real ✅ | El turno de Steward de S6 corre el punto 8 de la lista | ✅ Cerrado |

## Evidencia incremental — 2026-09-05 · contraste del link de DB-05 y §3 sobre DB-08 (C2, Monserrat Miranda)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-215b`, `US-214b` | **El `<a>` de `link_db08` reprobaba AA y está corregido.** Reportado por Marina García al cerrar US-215a; **no se citó su medición, se midió el propio en DB-05** sobre color y fondo efectivos y en los dos temas, como exige DEC-016: antes `#2893b3` sobre `#f5f5f5` = **3.26 : 1** en claro (5.91 en oscuro, que es donde no falla), después **19.26 : 1 claro / 21 : 1 oscuro** con subrayado. **Por DEC-016 este sí bloqueaba**, a diferencia de BUG-051: el ancla la escribe FARO. Arreglo con el patrón ya resuelto en el repo (`color:inherit` + `text-decoration:underline`); no se buscó otro azul porque ningún color único pasa contra los dos fondos. Guarda `test_el_link_db08_no_depende_del_color_del_tema` **validada reintroduciendo cada defecto por separado** ✅ · **965 passed / 0 failed** | — | ✅ |
| `REQ-002` | `US-215b` | **§3 medido por primera vez sobre DB-08.** El plan declaraba cubrir los dos tableros pero la evidencia era de DB-05 —citaba "6/6 tabs y 3/3 filtros", y DB-08 no tiene tabs y tiene 5—. Medido: **claro 1085/1209, oscuro 1208/1209**, 1056 elementos enfocables, **5/5 filtros alcanzables**, foco visible con el mismo anillo `box-shadow`. Los resultados quedan **separados por tablero**, no fundidos. Pendiente puntual: la activación por teclado en DB-08, que el navegador automatizado no puede probar sin arriesgar un falso negativo | Comprobación humana de §3.2 en DB-08 | ✅ `US-215b` |
| `REQ-002` | `US-215b`, `US-213` | **BUG-056 dado de alta** (`low`, `open`; ID según DEC-013, BUG-055 era el máximo en `main` y se re-verificó libre). 122 celdas `pvtVal` del pivote de DB-08 a **3.55 : 1** sobre blanco en claro. **Verificado contra el YAML antes de clasificarlo**: `db08_explorador_cubo.yaml` no declara ningún color ni formato condicional, así que es acento heredado de Superset → por **DEC-016** limitación conocida con su medición, **no bloquea**. Se registra aparte de BUG-051 porque es otro tablero y otro elemento. **Sólo ocurre en tema claro** | Re-medir si FARO adopta paleta propia (REQ-005) | 🟡 abierto |

**Nota de método, aplicable a cualquier auditoría de contraste del proyecto:** el barrido del 4-sep
dio verde sobre DB-05 y aun así dejó pasar el defecto del link, por dos razones que conviene no
repetir — se midió **un solo tema** (el oscuro, donde no falla) y **sólo el viewport inicial**,
mientras que la tabla del link está debajo del pliegue y Superset la renderiza perezosamente: sus 30
anclas no existen en el DOM hasta hacer scroll.

## Evidencia incremental — 2026-09-05 · BUG-054 y la URL pública de Superset sin registrar (PO)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-204`, `US-214b` | **`BUG-054` dado de alta** (`high`, `open`; ID según `DEC-013`, `BUG-053` era el máximo en `main`): la tarjeta *"% escuelas con recomendación"* de **DB-09** muestra `No data`. Reportado por Marina García fuera de su historia al cerrar `US-215a` ✅ · **Descartado que sea el modelo**: verificado contra el Gold local, `gold.cubo_recomendaciones` tiene 55 filas con `recomendacion_emitida = 1` sobre 55 CCT — la métrica daría **100 %**, no `No data` ✅ · Sospechoso principal **`BUG-037`**, que sigue `open` y Marina ha reportado tres veces: el dataset no refresca su esquema tras cambiar el SQL ✅ · **Es la tarjeta de cobertura del tablero prescriptivo**, la narrativa central del proyecto | C2 refresca el dataset y reconfirma; si no alcanza, se decide retirar la tarjeta o explicarla — no dejarla así | ⬜ Abierto, de C2 · **verificar antes del 9-sep** |
| `REQ-005` | `US-502` | **La URL pública de Superset no está registrada en el vault.** Superset quedó desplegado en Cloud Run con Google SSO obligatorio y login e2e verificado en vivo (Luis Téllez, revisiones `00001`→`00004`), pero buscando en `vault/` la única URL pública que aparece es la de la API. La rúbrica evalúa **URL pública viva** y la demo del 9 corre sobre ella ⬜ · Los dos fixes de runtime que Luis cazó desplegando —`userinfo` como URL relativa en el OIDC discovery, y la colisión del correo del admin de servicio con la lista blanca SSO— **no se veían por lectura de código**: el primero habría hecho rebotar TODO login SSO | C5 registra la URL en `00_Start_Here` junto a la de la API | ⬜ Pendiente de C5 |


## Evidencia incremental — 2026-09-05 · BUG-048 cerrado en producción y el par de demostración que ya no existe (PO)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-005` · `REQ-001` | `US-505`, `US-113` | **`BUG-048` cerrado, verificado en vivo por el PM contra la URL pública** tras el import de Luis Téllez: `indice_completitud_drivers` pasa de **0.197 a 0.6194** ✅ · `escuelas_en_riesgo` sigue en **0**, y con el rerun de CONAPO real **ése es el número correcto**: la caída máxima predicha es −4.53 %, bajo el umbral de −5 % de `DEC-006`. Deja de ser síntoma de dato viejo y pasa a ser resultado ✅ · Procedimiento correcto de C5: **verificación read-only del dump antes de importar**, con dos dumps intermedios descartados por no pasarla | Reconfirmar los KPIs el 9 antes de abrir la demo | ✅ Cerrado |
| `REQ-002` · `REQ-003` | `US-207`, `US-214a` | **El par de demostración ya no existe en ningún ambiente, y esto es nuevo.** `DEC-015` decía que el par era cierto en local y falso en producción *mientras `BUG-048` siguiera abierto*. `BUG-048` cerró y **la salvedad sigue viva por otra razón**: el rerun con CONAPO real cambió el modelo (`absolute_error`) y la distribución de drivers. Medido en producción por el PM: `15DJN0049A` → **0.3466 · D4 · conectividad** (era 0.129·D3 con el Gold viejo; 0.7423·D1·becas en el local de Marina) y `09DSN0042A` → **0.1339 · D2** ⬜ · **0.35 contra 0.13 es 2.6× — ya no es \"riesgo parecido, recomendación distinta\"** | **C2 (Marina García) elige un par nuevo con los datos de producción antes del 9**, no recicla el viejo | ⬜ Acción abierta, bloquea la narrativa de la demo |
| `REQ-004` | `US-412`, `US-402` | **La imagen de la API desplegada va por detrás de `main`**: `/api/v1/version` devuelve `33fcbbb`, anterior a los merges de hoy. El refresco de `BUG-048` fue de **datos**, no de código, así que sigue sin desplegarse el `SIN_DATO` de `/explicacion` (`BUG-055`, Christian Ruiz) ni lo que entre después ⬜ · Consecuencia concreta: producción todavía responde `0.0` donde no hay dato, afirmando que un driver contribuyó cero | C5 redespliega la API antes del 9, después de que entren los PRs de hoy | ⬜ Pendiente de C5 |
| `REQ-002` | `US-204` | **`RISK-009` dado de alta** (prob. 3 · impacto 4; ID según `DEC-013`, `RISK-008` era el máximo en `main`). Reportado por **Marina García** verificando mi propio barrido antes de darlo por cerrado, y **sus tres cifras salieron exactas contra las mías, que estuvieron mal dos veces**: son **33 métricas con división en 5 archivos** (yo conté 15 y luego 18), sólo `pct_escuelas_recomendadas` devuelve entero, y `variacion_ponderada_pct` existe en **5 copias, 2 con `* 1.0` y 3 sin** ✅ · Su hallazgo de fondo, verificado: **las que se salvan no se salvan todas por lo mismo** —unas por `SUM(bigint) → numeric`, `matricula_por_escuela` sólo porque su denominador es `bigint`, y `variacion_ponderada_pct` por el `* 1.0` de `BUG-031`— y **`matricula_total` tiene tipo distinto según el cubo**: `integer` en `cubo_escuela_360`/`cubo_pivot`/`cubo_recomendaciones`, `bigint` en tres más, `numeric` en `matricula_municipio_nivel` ✅ · **No se levanta como bug porque hoy nada falla**; es fragilidad latente, y esa clasificación es de ella | Guarda post-freeze: prueba que reprueba si alguna métrica con división devuelve tipo entero | ⬜ Riesgo abierto, de C2 |
| `REQ-005` | `US-501`, `US-502` | **Las URLs públicas entran al punto de entrada del vault.** `PROJECT_INDEX.md` —el archivo que mira primero quien evalúa— **no listaba ninguna**; la de Superset sí estaba en `Cloud_Run_Deploy.md §5.1` (Luis Téllez, PR #248), pero enterrada en un documento de C5 ✅ · **Verificadas respondiendo el 2026-09-05**: Superset `/health` → 200 con botón de Google presente en `/login/`; API `/api/v1/health` → 200 ✅ · **Corrección al bloque propuesto**: decía *"Swagger en `/docs`"* y **`/docs` devuelve 404** — `src/api/app.py:95` monta la documentación en `/api/v1/docs`. Mandar al evaluador a `/docs` habría sido un 404 en vivo ⬜→✅ · Se documentan además los dos 404 esperados de la raíz, para que nadie los lea como caída | Reconfirmar las dos URLs el 9 antes de abrir | ✅ Cerrado |
| `REQ-004` · `REQ-005` | `US-403`, `US-411`, `US-412`, `US-501` | **`BUG-057` dado de alta** (`high`, `open`; ID según `DEC-013`, `BUG-056` era el máximo en `main`). **La URL pública dejó de servir datos**: `/api/v1/kpis`, `/escuelas` y `/predicciones/{cct}` pasaron de **200 a 401** en minutos; `/health`, `/version` y `/docs` siguen en 200 ⬜ · **No es defecto de código**: `require_lectura` (`rbac.py:61`) hace exactamente lo especificado, y `config.py:72` describe este paso —*"cuando C5 entregue credenciales, `AUTH_LECTURA_PUBLICA=false`"*—, que C5 cumplió hoy ✅ · La imagen **no cambió** (`/version` = `33fcbbb`), así que fue variable de entorno, no redespliegue ✅ · **Se registra porque cambia lo que ve el evaluador**: con Superset ya en SSO obligatorio, **ambas superficies exigen login** y la rúbrica evalúa *URL pública viva* (`RISK-001`, techo 6.0) | Decisión del PO: confirmarlo como postura y dar de alta al evaluador en las dos superficies, o reabrir la lectura en la ventana de demo | ⬜ Decisión abierta · antes del 9-sep |
| `REQ-007` | `US-006` | **Guion de la demo escrito** ([[vault/01_Product/Guion_Demo_US006]], `approved`): 10 minutos, reparto por persona con su verificación previa, plan B por modo de falla y checklist de la mañana del 9 ✅ · Incluye **las tres cosas que decimos antes de que las pregunten**: `escuelas_en_riesgo` = 0 como resultado y no como falla, `/explicacion` sin SHAP, y la deuda de contraste medida sobre el bundle real ✅ · **`US-006` queda `in_progress`, no `done`**: el objetivo del plan dice *"preparar **y ensayar**"* y el ensayo con cronómetro queda comprometido para el **lunes 7-sep**. Un guion sin ensayar no cumple la historia | Ensayo completo el 7-sep; si un bloque no se sostiene, se corta y se redistribuye el minuto | 🔵 En revisión — falta el ensayo |
| `REQ-004` | `US-416` | **`US-416` cerrada a `done`.** Su fila pedía como condición *"que Christian Ruiz (TL C4) ratifique el diseño y confirme que la prueba con Postgres real corresponde a `US-422`"*, y **el PR #240 cumplió las dos** el 2026-09-05: ratificación leyendo el código, no el reporte — destaca el `SET LOCAL` en vez de `SET`, que muere con la transacción y no deja el `statement_timeout` pegado en conexiones que vuelven al pool compartido con `RepositorioGold` ✅ · Endurecimiento a `SQLAlchemyError` ratificado ✅ · E2E real confirmado como `US-422` (Eloisa González) **desde el padrón, no de memoria** ✅ · La línea quedó obsoleta al mergear #240 y el flip lo pidió **Juan Macías**, cuyo alcance pasa a **100 %** | — | ✅ `done` |
| `REQ-007` | `US-001` | **Los dos padrones discrepaban y el tablero leía el equivocado.** `Developer_Onboarding.md` decía `juanmmayen98` para Juan Macías; `ownership.yml` decía `juanmmayen98-pixel`. **El generador toma el handle de `Developer_Onboarding`, no del padrón**, así que le contaba **0 PRs teniendo 3 mergeados** (#95, #101, #191) ⬜→✅ · Verificado contra el snapshot de actividad: de los 252 PRs, `juanmmayen98` aparece en **0** y `juanmmayen98-pixel` en **3** ✅ · **Barrido de las 21 personas: es la única discrepancia** ✅ · Importa más allá del tablero: `AC-007.1` exige que los 21 integrantes tengan commits, y esto hacía ver a un contribuyente como si no hubiera contribuido | Que el generador lea el handle de `ownership.yml`, que es el padrón — follow-up post-freeze | ✅ Corregido |


## Evidencia incremental — 2026-09-05 · SEC-006 cerrado: la lectura de la API ya exige sesión (C5)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-004` · `REQ-005` | `US-403`, `US-404`, `US-402` | **`SEC-006` cerrado por configuración: se apagó `AUTH_LECTURA_PUBLICA`.** El hallazgo (Security Audit Log, `low`, `accepted_risk` desde 2026-09-02) se aceptó *como decisión de demo* con una condición de cierre explícita —apagarlo cuando el login e2e estuviera validado— y **ese login quedó validado en vivo** al cerrar `BUG-046`: `/auth/login` 302→Google con `state` JWT de un solo uso, `/auth/me` 401 sin token, RBAC `analista`/`ciudadano` sobre `/admin/*` ✅ · Ejecutado el flip `AUTH_LECTURA_PUBLICA=false` en Cloud Run (rev **`faro-api-00012-pq5`**), cambio de **variable de entorno**, sin rebuild —`/api/v1/version` sigue en `33fcbbb`— y reversible en segundos con `=true` (punto de rollback `faro-api-00011-hr5`) ✅ · **Verificado en vivo, sin token, sondeando todo `/api/v1/openapi.json`**: `kpis`/`escuelas`/`municipios`/`predicciones/*`/`agente/consulta` pasan de **200 a 401**; `version`/`health`/`docs`/`auth/login` siguen públicos; `admin/*` sin cambio (siempre `analista`) ✅ · Matriz resultante: **público** (health/version/docs/login) · **lectura** (cualquier sesión, `ciudadano` o `analista`) · **admin** (solo `analista`) · Cierra `SEC-006` en el Security Audit Log (→ `resolved`, ☑); ver [[vault/_DevLog/2026-09-05-luis-tellez-sec006-flip-auth-lectura-publica]] | Revisión de regla 7 de C4 (Christian Ruiz, dueño de `07_Security`) + merge del PO (cambio de seguridad) | ✅ Cerrado (flip verificado) · el **redeploy de código** de la API es aparte y sigue abierto (fila `REQ-004`/`US-412` de la sección anterior) |
| `REQ-002` · `REQ-005` | `US-526`, `US-206`, `US-207` | **`US-526` dada de alta y asignada a Luis Téllez (C5) por decisión del PO.** El hueco no era de ejecución sino de **planeación, y es mío**: se decidió contenerizar FARO Web y **la historia nunca se creó**. Verificado antes de darla de alta: la raíz de la URL pública devuelve `404`, no existe `docker/frontend.Dockerfile` y no hay servicio de frontend en `docker-compose.yml` ✅ · **Consecuencia que lo hace urgente**: `US-206` (shell y embebido, Manuel Serranía) y `US-207` (panel de ML, Marina García) están **construidas y no se pueden mostrar** — existen en el repositorio, no en la URL pública ⬜ · **Alcance**: Dockerfile, servicio en compose, despliegue a Cloud Run y el **CORS de Superset**; sin lo último la página carga y los 10 tableros salen en blanco, y `AC-002.1` no se cumple aunque el despliegue funcione ⬜ · Catálogo **91 → 92**; la guarda de conteo del generador y del validador se actualizó a mano **a propósito**, para que el número no cambie por accidente | Si no alcanza para el 9, aplica el plan B de [[vault/01_Product/Guion_Demo_US006]] y **se avisa a Manuel y a Marina antes del ensayo del lunes** | ⬜ `planned`, de C5 |

## Evidencia incremental — 2026-09-05 · US-313 contra Gold real y `Publicacion_Gold` aprobado (C3, Héctor Morales)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-313` | **`Publicacion_Gold` pasa a `approved`**: el pendiente que lo mantenía en revisión era ejecutar contra `gold.features_escuela` real, y está hecho **por dos vías independientes que coinciden** — la mía del 4-sep (Gold con CONEVAL) y el rerun de Andrés del 5-sep (con CONAPO real). 136,046 filas · 46,547 escuelas · 3 ciclos; **MAE 0.1592 y 0.159148** con pérdida cuadrática, **el mismo número por separado**; 45,276 + 45,276 filas publicadas; **1,168 huérfanas, cifra idéntica en ambas corridas**. §9 nuevo documenta por qué ML-01 cambió a `absolute_error` (con pérdida cuadrática **no le gana al baseline temporal**: 0.159148 contra 0.159223) y **declara que el umbral sigue incumplido**: 0.141458 contra el `ML-01_mae: 0.03` de `evaluar.py`, 4.7× por encima, reportado con el mismo criterio que el Silhouette de ML-03 en US-312. **Las anclas de DEC-006 no se movieron.** Las 1,168 huérfanas **se conservan a propósito** (decisión de Andrés a petición de Luis Téllez): filtrarlas pondría `cubo_recomendaciones_kpi11_parity` en verde escondiendo un desfase real entre `features_escuela` (45,276 en 2024-2025) y `fact_escuela_ciclo` (44,114, que filtra `matricula_ciclo_anterior is not null`) | Acordar con C1 si el desfase se cierra en `features_escuela` o en el hecho | 🟢 Aprobado |
| `REQ-003` · `REQ-001` | `US-313`, `DS-01`, `DS-02` | **Los cargadores de DS-01/DS-02 no son idempotentes — hallazgo para C1.** El dump de Bronze del 4-sep trae `formato911_historico` con **2,746,978 filas contra 1,373,574 distintas** y `cct_siged_202608` al doble: **dos pasadas completas de ingesta el 3-sep** (04:04 y 06:56). Mecanismo verificado: la llave UNIQUE de `formato911_historico` **incluye `_ingested_at`**, así que `ON CONFLICT DO NOTHING` sólo protege dentro de una misma corrida; `cct_siged_202608` **no tiene ningún índice**. `DS-01_Formato_911` §11 afirma que *"correrlo de nuevo no duplica nada"* — no es cierto. **No bloquea**: `silver/escuela.sql` y `silver/matricula_historica.sql` dedupan por `_ingested_at desc`. Probablemente explica por qué la expectativa de unicidad sobre Bronze DS-01 quedó fuera de la suite de Great Expectations | Diana Alvarez decide si corrige la llave o la documentación de §11 | 🟡 Reportado a C1 |
| `REQ-004` · `REQ-005` | `US-411`, `US-501` | **`BUG-020` está resuelto de hecho y su fila del registro está desactualizada**: ninguna ruta responde ya 500. Medido hoy contra la URL pública: `/health`, `/version` y `/docs` **200**; `/kpis`, `/escuelas`, `/municipios` y `/predicciones/{cct}` **401** a un visitante anónimo. Edgar leyó `/kpis` esta mañana al cerrar `BUG-048`, así que la condición **cambió después**; `SEC-006` (flip de lectura pública) estaba explícitamente "a decisión C5+PO". Se reportó la medición sin afirmar la causa, y al sincronizar apareció: **es intencional** — `SEC-006` cerrado por C5 apagando `AUTH_LECTURA_PUBLICA` en Cloud Run (rev `faro-api-00012-pq5`), condición de cierre que esperaba el login e2e de `BUG-046`. La medición independiente **confirma el flip desde fuera**: `/health`, `/version` y `/docs` siguen en 200 y sólo las rutas de datos pasan a 401. Queda una consecuencia para la demo: quien abra la URL pública sin sesión ya no ve KPIs ni predicciones | Actualizar la fila de `BUG-020`, que sigue diciendo `pendiente` cuando ninguna ruta responde ya 500 | 🟢 Explicado |
## Verificación local de Estefany — 2026-09-05

| Requisito | Historias | Evidencia | Próximo paso | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-321`, `US-322`, `US-325` | Dump Gold post-BUG-048 restaurado en base local aislada: 136,046 filas, 46,547 escuelas, 3 ciclos, cero duplicados `cct × id_ciclo`; US-322/US-325 ya tienen evidencia agregada real. Andrés ratificó el 6-sep el vector ML-03 D1–D4 + `indice_completitud_drivers`: D5 100% `SIN_DATO` y D6 98.70% sin dato quedan sólo como cobertura, sin imputación. La prueba C3 exige `--confirmar-registro` junto con `--tracking-uri`, para separar la revisión agregada del registro MLflow. [[vault/_DevLog/2026-09-05-handoff-estefany-hernandez-entorno-pr231]] · [[vault/_DevLog/2026-09-06-andres-gonzalez-ratifica-vector-ml03]] | Edgar revisa y cierra US-322/325; para US-321 falta corrida temporal real, `k`, Silhouette y MLflow `run_id` | 🟡 Avance parcial; sin cierre |
| `REQ-001` · `REQ-005` | `US-114`, `US-525c` | **Barrido de asignaciones y dos reasignaciones del PO (2026-09-05).** De **89 historias en alcance**: 51 `done`, **21 `in_review`**, 12 `in_progress` y sólo **5 `planned`** ✅ · **El hallazgo del barrido no son las reasignaciones**: son las **21 en revisión, de las cuales 15 no declaran ninguna dependencia** — trabajo ya entregado esperando validación del PM. Cerrarlas es la palanca más grande que queda y **no depende de nadie más que de mí** ⬜ · **`US-114` → Diana Alvarez** (era de Deni): Deni tiene `US-113` en revisión, Diana cerró sus 6 al 100 %, y el entregable cae en `dbt/**`, su verde. Sin dependencias — el Gold existe y los 9 cubos están materializados ✅ · **`US-525c` → Marina García** (era de Edward Ruiz, **14 días sin PR mergeado** con 4 historias abiertas): documentación sin dependencias, Superset ya desplegado, y es quien más lo conoce. Se le abrió en `ownership.yml` **sólo ese archivo** de `08_CICD_DevOps`; C5 sigue en `criticos` y el gate pedirá su revisión ✅ | Cerrar las 15 `in_review` sin dependencia declarada — acción del PO | ⬜ Reasignadas; el barrido queda como línea base |
| `REQ-007` | `US-001`, `US-004` | **Marcadores de conflicto commiteados a `main`, y fueron míos.** En el merge `70fb30d` hice `git add -A` sobre un merge a medias: la matriz quedó con `<<<<<<<` y llegó a `main` con el PR #254 ⬜→✅ · **Nada lo detuvo**: ni `vault_lint`, ni las 991 pruebas, ni el gate de propiedad, ni los 4 checks de CI. Y `git status \| grep UU` **tampoco lo ve**, porque el `add` ya los había marcado como resueltos — que es justo lo que me hizo creer que estaba limpio ✅ · Se detectó por accidente, revisando por qué GitHub marcaba conflicto en el PR #258 de Manuel, que en realidad era el falso positivo conocido de `merge=union` ⬜ · **Bloque resuelto conservando los dos lados**: mis 4 filas y la sección de `SEC-006` de C5, verificado por assert de que ninguna se perdiera ✅ · **Guarda agregada a `vault_lint.py`**: cualquier `<<<<<<<`, `=======` o `>>>>>>>` en un `.md` del vault ahora reprueba, con el mensaje de que `git status` no los ve tras un `add`. Probada inyectando un bloque: lo caza en las 3 líneas ✅ | El turno de Steward de S6 corre el linter, que ahora sí lo cubre | ✅ Corregido y con guarda |
| `REQ-003` | `US-311` | **`US-311` cerrada a `done`**, a solicitud de Héctor Morales y con sus cuatro afirmaciones **verificadas por el PM antes de cerrar**: `AC-003.2` pide *"cada modelo **reporta** su métrica"* (`Requirements_Detailed:84`), `evaluar.py:50` los llama *"umbrales provisionales"*, `US-312` ya cerró `done` con Silhouette 0.1086 contra umbral 0.30, y `BLOCK-001` está resuelto ✅ · Su argumento de coherencia es correcto: **aplicarle a ML-01 un criterio que no se le aplicó a ML-03 sería incoherente**, y donde sí importa el modelo mejoró — le gana al baseline temporal por 11.04 %, cosa que con pérdida cuadrática no hacía ✅ · **La salvedad del `mlflow_run_id` se cuelga de `BUG-043`, no mantiene la historia abierta**, como él mismo propuso: las predicciones llevan un literal de relleno y no son trazables a una corrida real de MLflow. Medido en las dos superficies: `local-sin-mlflow` en su base, `bug048-20260905-temporal-robusto` en producción ⬜ | Si preguntan en la demo de qué corrida salió una predicción, la respuesta hoy es un literal — está en `BUG-043` | ✅ `done`; salvedad trazada |
| `REQ-004` | `US-403`, `US-405` | **Postura de acceso asentada como `DEC-018` el 2026-09-05**, cerrando lo que `src/api/config.py:72` marcaba como *"PROVISIONAL; la definitiva la decide Edgar/PO"* y `SEC-006` dejaba como decisión de producto ✅ · **OAuth de Google en las dos capas**: `AUTH_LECTURA_PUBLICA=false` en producción, la lectura pasa a exigir sesión, y es reversible sin rebuild ✅ · **La asimetría queda declarada, no descubierta**: Superset filtra con lista blanca estricta (`SUPERSET_SSO_ALLOWED_EMAILS`, guarda fail-loud si queda vacía en `docker/superset_config.py:160`), pero **la API no tiene lista blanca de acceso** — `resolve_role` (`src/api/security/roles.py:18-22`) devuelve `ciudadano` a cualquier correo que Google autentique, y sólo `analista` está restringido por `ANALISTA_EMAILS`. Se acepta a propósito: lo que se protege es escritura y admin, y todo el dato es agregado a nivel escuela ✅ | **Modo de falla del 9, pendiente del PO:** si el correo del evaluador no está en la lista blanca de Superset, entra correctamente con Google y **lo rechazamos** sin que nada reviente ni explique por qué | ✅ `DEC-018` publicada |

## Evidencia incremental — 2026-09-05 · lectura horizontal de DB-05 (C2, Monserrat Miranda)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-213`, `US-202` | **DB-05 se leía como una tira vertical y ya no.** `_layout_tabs()` en `sync_semantic_layer.py` ponía cada chart en su propia fila, ignorando el `ancho` del YAML — **el mismo defecto de `BUG-049`**, cuya corrección se acotó al camino plano (`_layout_grilla()`) porque el docstring de su guarda afirmaba que "DB-05 y DB-08 van por `_layout_tabs()`, que no se toca". Falso en las dos mitades: **DB-08 no declara `tabs:`** (camino plano, siempre agrupó bien) y **DB-05 es el único tablero con tabs**. El YAML nunca estuvo mal: ya declaraba `3,3,3,3` y `6,6`, el patrón de DB-03. Ahora `_layout_tabs()` agrupa con la **misma** `_agrupar_en_filas()` del camino plano: **42 filas → 24**, 4 por tab, verificado contra el `position_json` publicado (6 tabs idénticos, ninguna fila excede la grilla de 12, los 36 charts intactos). **Sin bug nuevo** — se corrige dentro de la historia, misma vía que Marina con el link de contraste | Que Manuel Serranía revise el cambio en la herramienta compartida (US-202) | ✅ corregido y verificado en vivo |
| `REQ-002` | `US-213`, `US-214b` | **La tabla municipal pasa a fila completa, decidido midiendo y no a ojo.** A media fila (`ancho: 6`) necesitaba **999 px con 504 visibles**: desbordaba 495 px y ocultaba la columna `link_db08`, que es **el drill-down a DB-08 que entrega US-214b**. A `ancho: 12` el desborde baja a **7 px** y los **30 links** quedan visibles sin scroll horizontal. Aplicado a los 6 tabs | Recapturar DB-05 para el `Manual_Usuario_Dashboards` (Oscar Quiroz) — la captura vigente es del layout viejo | ✅ verificado en navegador |
| `REQ-002` | `US-215b`, `US-213` | **Contraste remedido en los dos temas, porque cambiar el layout cambia lo que se puede medir.** El barrido del 4-sep alcanzaba **34 nodos** en DB-05: la tabla estaba bajo el pliegue y Superset la renderiza perezosamente. Con el tablero en horizontal se mide entera — **claro 280/292 · oscuro 281/292**. **Nada de lo que escribe FARO reprueba**: los 30 `link_db08` dan **15.39:1 claro / 13.40:1 oscuro** con el subrayado intacto, y las 60 celdas de tabla medidas pasan en ambos temas. Lo que reprueba es chrome heredado (`Published`, `Edit dashboard`, placeholders `rgba(…,0.25)`, 6 insignias de conteo de filtro y la etiqueta del tab activo de `BUG-051`) → por `DEC-016`, limitación conocida que no bloquea. Corregido de paso el número del link de ayer: 15.39:1, no 19.26:1 — el color heredado es `rgba(0,0,0,0.88)`, no `#000000` | — | ✅ medido en claro y oscuro |
| `REQ-002` | `US-213` | **Las tarjetas no bastaba con ponerlas en fila: había que alinearlas.** Agrupadas, los cuatro números seguían cayendo a alturas distintas (**103 / 154 / 78 / 154 px**, 76 px de dispersión) y el primero se salía de su tarjeta. Dos causas, ninguna el alto de la fila: el título de KPI-07 era **el único que envolvía a 3 líneas** (83 px contra 58) y **sólo dos de las cuatro tarjetas tenían subtítulo**, que corre el número. **El primer intento —subir `alto` de 38 a 52— se revirtió**: Superset escala la cifra al contenedor, así que los números salían desproporcionados frente a los demás tableros, y no atacaba la causa. Lo que funciona es reducir lo que debe caber: título de KPI-07 acortado a `Dx · KPI-07 · % driver dominante` (los otros tres nombres **no se tocan**) y subtítulo en las cuatro. Medido contra **DB-03 como referencia**: mismo alto de tarjeta (304), misma cifra (51 px) y los cuatro números en **78 px — dispersión 0**. La barra de scroll interna del `big_number_total` (277/210) **no es de este cambio: DB-03 tiene exactamente la misma** | — | ✅ verificado en navegador contra DB-03 |
| `REQ-002` · `REQ-007` | `US-213`, `US-202` | **Defecto latente en `_position_con_uuid()`, destapado al renombrar y corregido.** Emparejaba uuid con nodos CHART **por posición** —uuid ordenados por id contra nodos en orden de declaración—, lo que sólo acierta si los ids ascienden en el orden del YAML. Deja de cumplirse **en cuanto un chart se renombra**: `ensure_chart()` identifica por `slice_name`, así que un nombre nuevo crea un chart con id alto; el de KPI-07 va primero en cada tab, tomó los ids 110-115 y cada nodo recibió el uuid de otro. **DB-05 se sincronizó en verde y se rompió al abrirlo** —tarjetas vacías o intercambiadas, sin error en el sync ni en la consola—, reproducido en vivo esta sesión. Corregido buscando el uuid **por `meta.chartId`**, que el nodo ya declara: elimina el supuesto de orden. **Afecta a los 10 tableros, y por eso lleva guarda de equivalencia**: el caso normal (ids ascendentes) pasa con **ambas** implementaciones, y las dos guardas del caso roto fallan con la vieja. Verificado además abriendo DB-03 tras el cambio | Que Manuel Serranía lo revise: es la misma herramienta compartida de US-202 | ✅ corregido, con guarda de no-regresión |
| `REQ-006` · `REQ-007` | `US-006`, `US-005` | **Dos afirmaciones falsas del PM, cazadas por Marina García del Buey al revisar el PR #264 contra el árbol y no contra el diff, y corregidas aquí.** **(1)** El guion excluía la pregunta de latitud del bloque del agente afirmando que *"no existe `latitud` a nivel escuela"*: **falso** — el `grep` que lo respaldaba se truncó con `head -6` y sólo vio los aciertos de `agua_region` y `aire_estacion`. La columna existe en `silver/escuela.sql`, `gold/dim_escuela.sql`, `gold/cubo_escuela_360.sql`, `features_escuela.sql`, `fact_escuela_ciclo.sql` y `api/db.py`, con regresión dedicada `valid_escuela_georreferencia.sql` (`BUG-034`) — y `src/agente/indexar_esquema.py:31` **se la declara al propio agente**. La pregunta vuelve al banco ✅ · **(2)** `Vault_Steward.md` —`source_of_truth: true`— narraba en pasado un merge del PR #263 que **no ha ocurrido** (verificado con `git merge-base --is-ancestor`; el #263 sigue `OPEN`) y le atribuía a ella el aval. Contradecía al DevLog de la misma rama, escrito minutos antes. **Reescrito a una sola versión**, con la corrección visible en el documento y no borrada ✅ | Las dos son fallas de **verificación**: un `grep` truncado y un hecho futuro escrito en pasado, los dos en documentos `source_of_truth` que otros citan | ✅ Corregido en el mismo PR |

## Evidencia incremental — 2026-09-06 · DEC-019: capa dbt del umbral de alerta (C1, Diana Alvarez)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-001` | `US-113` | **Capa dbt de `DEC-019` cerrada por C1.** Bajado `LINEA_DE_ALERTA` de 0.60 a 0.50 en los 5 sitios hardcodeados (`cubo_comparador_municipio.sql:81`, `cubo_escuela_360.sql:52`, `cubo_riesgo_territorial.sql:57` y sus 2 tests de paridad), más el comentario de cabecera que citaba el umbral viejo. `dbt run` + `dbt test` → 2/2 parity en verde. Verificado dos veces contra datos reales: primero se detectó que el Postgres local traía predicciones de una corrida sin trackear (`local-sin-mlflow`, avg 0.99, 96 % de escuelas "en riesgo") — descartada y reemplazada por el run real `bug048-20260905-temporal-robusto` antes de rematerializar. Con los datos correctos: **7 de 45,276 escuelas en riesgo**, coincide al dígito con lo medido por C2/C5 en producción. Gold reexportado (`gold_dec019_2026-09-06_v2.sql`) y entregado a Luis Téllez (C5) para reimportar a Cloud SQL | [[vault/_DevLog/2026-09-06-diana-alvarez-bug048-validacion-final]] | ✅ Capa dbt (C1) lista · pendiente reimport a Cloud SQL (C5) |
## Evidencia incremental — 2026-09-06 · P0 del panel de ML y la línea de alerta de DEC-019 (C2, Marina García)

| REQ | US | Evidencia | Pendiente | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-207` | **P0 cerrado: el panel ya dice de qué escuela habla.** Ficha **antes** del índice —nombre, nivel, municipio, entidad, sostenimiento, matrícula y completitud de drivers— y **búsqueda en cascada** entidad → municipio → nivel → plantel, ordenada por riesgo descendente para dejar el ranking prescriptivo a la vista ✅ · Cliente nuevo `src/frontend/catalogo_client.py` sobre `/api/v1/escuelas`, `/escuelas/{cct}` y `/municipios` — **sin API nueva**, como anticipó el PM ✅ · **La búsqueda se construyó solo con `st.selectbox`**: las pruebas direccionan `app.text_input[0]` y `app.button[0]` **por índice**, así que un campo o botón nuevo antes del formulario las rompe en silencio. Se agregó la guarda de ese invariante y se validó metiendo un botón extra: **reprueban las dos**, la vieja por una razón que no lo parece y la nueva diciendo exactamente por qué ✅ · 23 casos nuevos, cada guarda validada reintroduciendo su defecto ✅ · Verificado **en el navegador** contra la API local, no solo con dobles ✅ | **El par de demostración sigue sin elegirse** y es criterio de aceptación del [[vault/01_Product/Guion_Demo_US006\|guion]]: *"el par elegido responde en producción ese día"*. Hay que fijarlo contra producción, con sesión, y dejarlo escrito ⬜ | ✅ entregado |
| `REQ-002` | `US-215a` · `BUG-058` | **El umbral eran dos números en uno, y por eso no se podía mover.** `UMBRAL_RIESGO = 0.60` servía a la vez para **explicar** qué significa el índice (0.60 ≡ perder 5 %, la calibración de la sigmoide) y para **decidir** cuándo enciende la alerta. Separados en `ANCLA_SIGMOIDE = 0.60` (no cambia) y `LINEA_DE_ALERTA = 0.50` (**DEC-019**) ✅ · **Evidencia del 0.50**: sobre el Gold real de producción (45,276 escuelas) el máximo de ML-01 es **0.5717 ≈ −4.53 %**, o sea que el corte estaba por encima del techo del fenómeno y el KPI daba 0 **por construcción**; 0.50 ≈ perder **3.4 %**, justo por debajo del **3.7 %** de deserción real en secundaria, así que enciende **antes** de la norma nacional. Bajar más diluye: 0.40 marca el 26 % del universo y 0.35 el 55 %. Con 0.50 son **7 escuelas de 45,276** ✅ · La sigmoide **no se toca**: `test_riesgo.py::test_reproduce_el_ancla_del_umbral_de_negocio` sigue verde ✅ | **Lo que BUG-058 subestimaba, y es la cadena:** `escuelas_en_riesgo` y `en_riesgo` son **columnas materializadas** por dbt con el `0.6` **hardcodeado en 5 archivos** (no hay `var`), y los tableros leen esas columnas, no la constante del frontend. **Exige `dbt run` + reimportar Gold a Cloud SQL** — hasta entonces el panel y los tableros **discrepan**. Por eso **no se tocó** el `umbral: 0.6` de `metrics_db03_db04.yaml`: es metadato, y cambiarlo antes que el dato lo volvería mentira. Toca **Diana** (dbt + rematerializar), **Luis** (reimportar), **Christian** (`repositorio_gold.py:325`), **Manuel** (6 archivos de Superset) y **Edgar** (firmar DEC-019) ⬜ | 🟡 parte de C2 hecha |
| `REQ-002` | `US-206` | **Hallazgo para el dueño de `1_Dashboards.py`:** su selector de nivel ofrece **«Media Superior»**, que **no existe en Gold** — `dbt/models/silver/matricula_historica.sql` filtra a preescolar/primaria/secundaria y `NIVELES_BASICA` lo fija. Elegirla devuelve siempre cero resultados, sin error. El catálogo nuevo del panel no repite el error y tiene prueba que lo fija | Corregir el selector — es de **Manuel Serranía** ⬜ | ⬜ reportado |

## Evidencia incremental — 2026-09-06 · el par de demostración y el sesgo del driver dominante (PM, Edgar Coronel)

| REQ | US | Evidencia | Pendiente | Estado |
|---|---|---|---|---|
| `REQ-007` | `US-006` | **El par de demostración queda fijado en el guion, con su criterio escrito.** `15DPR0920D` y `15DPR2254O`, ambas primarias en Ecatepec de Morelos, con `indice_riesgo` **idéntico al cuarto decimal (0.4774)** y driver dominante distinto (**D4** conectividad vs **D2** inseguridad). Elegido por C2 (Marina García) sobre el Gold rematerializado tras `DEC-019`. Se descartó la alternativa de Toluca (`15EES1468A` / `15EPR0628Y`) porque introduce **dos variables extra** —distinto nivel y distinto riesgo— y el bloque existe para aislar una sola | **Verificación en vivo**: elegido contra Gold, **no** contra producción con sesión. Lo corre C2 en el ensayo del **lunes 7** y otra vez la mañana del **9** (criterio de checklist, no de este PR) | in_progress |
| `REQ-007` | `US-006` · `DEC-019` · `BUG-053` | **Tres lugares del guion contradecían a `main`, y sólo uno venía reportado.** (1) La celda del minuto 5:00–6:30 anunciaba `escuelas_en_riesgo` = 0 cuando con `DEC-019` son **7 de 45 276** — reportado por C2. (2) El punto 1 de *lo que decimos antes de que lo pregunten* repetía el mismo 0 con el argumento viejo del −4.53 % — **no reportado**. (3) El punto 2 declaraba `/explicacion` sin SHAP citando `BUG-053`, que está **`fixed`** desde el 2026-09-05: el endpoint lee `gold.recomendaciones.shap_d1..shap_d6` vía `RepositorioModelos` — **no reportado**; el guion pedía disculpas por algo que ya funciona. Se agrega además la respuesta preparada a la pregunta que el par abre: está en **0.4774, debajo** de la línea de alerta de 0.50, y no es contradicción — la línea es *triage*, la recomendación es prescriptiva y se deriva del driver | — | done |
| `REQ-007` | `US-206` · `US-207` · `US-405` | **`DEC-020` asienta la excepción acotada al CODE FREEZE del 6-sep** (P0 y P1, corte 18:00) y deja escrito lo que **no** autoriza: tocar Gold/modelos/esquema, correr `sync_semantic_layer.py`, o mergear frontend sin evidencia de navegador. **La excepción se anunció al equipo como `DEC-019`**, ID que ya ocupaba la línea de alerta del KPI-04; se reasienta con número nuevo en vez de reciclarlo (regla 3 del vault, mismo criterio de `DEC-013`), y se deja dicho porque la ambigüedad ya se había propagado a un documento de C2 | — | done |
| `REQ-001` · `REQ-003` | `US-302` · `US-113` | **`BUG-062`: `driver_dominante` premia al driver con cobertura más angosta.** Reportado por C2 como posible **imputación** de D6 (dominante en 419 de 45 276 escuelas pero en **12 de las 40** de mayor riesgo, en municipios rurales). **La hipótesis de imputación no se sostiene**: `features_escuela.sql` filtra `distancia_km <= 15` y el ensamblado marca `coalesce(d6.d6_cobertura, 'SIN_DATO')` — la regla de `SIN_DATO` se respeta. El defecto real es de **comparabilidad de escalas**: cada driver se reescala **min-max sobre su propio conjunto cubierto**, y como el min-max siempre estira su entrada hasta llenar el `[0,1]`, D6 (~1.3 % del universo) sale inflado frente a D1/D2 de las mismas escuelas y gana el argmax de más | **`open`, `medium`, NO se corrige antes del 9-sep**: rematerializar cambia todas las predicciones publicadas, el par de demostración recién elegido y el conteo de 7 que sostiene `DEC-019`. Mismo criterio con el que se difirió `BUG-037`. Dueño propuesto: **C1 (Diana Alvarez)** por `dbt/**`, con **C3 (Andrés González)** por la especificación del argmax | open |

## Evidencia incremental — 2026-09-06 · DEC-019: capa de modelos (C3, Héctor Morales)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-311`, `US-313` · `DEC-019` · `RISK-010` | **C3 era la última capa sin implementar `DEC-019`.** C4 (`repositorio_gold.py`), C2 (`prediccion_client.py`) y C1 (dbt, PR #273) ya habían separado el ancla de la línea de alerta; `src/modelos/**` no, y `RISK-010` nombra mis archivos línea por línea. **Cuatro docstrings afirmaban que `0.60` era \"el umbral con que los tableros cuentan escuelas en riesgo\"** (`riesgo.py:13/25/60`, `publicar_gold.py:180`) — falso desde la firma de `DEC-019`. Pesa más de lo que parece porque **`publicar_gold.py` cita `DOC-INDICE-RIESGO` —documento de C3— como autoridad de esa frase**: la fuente canónica estaba muda sobre una decisión que tres células ya habían implementado leyendo de ella ✅ · `RIESGO_UMBRAL` → **`ANCLA_SIGMOIDE`**, el mismo nombre que ya usaban C4 y C2, con alias conservado para no romper importaciones; **el valor no se mueve (0.60)** porque `DEC-019` cambia el criterio de alerta, no la calibración ✅ · **`LINEA_DE_ALERTA` NO se define en `riesgo.py` a propósito**: `RISK-010` sigue abierto porque `0.50` ya está escrito dos veces sin prueba que las ate, y una tercera definición agravaría justo ese riesgo — fuente única = post-freeze ✅ · **Hallazgo:** el `0.50` de `DEC-019` sale de `Indice_Riesgo_ML01.md` §4.1, que desde agosto decía que 0.50 ≈ **−3.4 %**; reverificado corriéndolo (`variacion_equivalente(0.50) = −0.03382`), así que el ancla `0.30` —decisión aún abierta— **no es cosmética**: mueve lo que la línea significa en negocio ⬜ · **Dejado abierto a propósito:** `prioridad_de_riesgo()` marca `ALTA` desde el ancla (0.60), no desde la línea (0.50); moverlo reescribiría la columna `prioridad` de las **45,276 filas ya publicadas** en `gold.recomendaciones` y `DEC-019` prohíbe cambiar valores publicados ⬜ · §4.4 nuevo en `Indice_Riesgo_ML01.md` · 4 pruebas nuevas, las 2 guardas **falsificadas** (con el ancla en 0.50 reprueban) · **1042 passed, 8 skipped**, `ruff` limpio, `vault_lint` ✅ | [[vault/_DevLog/2026-09-06-hector-morales-dec019-en-c3]] | ✅ **`DEC-019` completo en las 4 capas · parte C3 de `RISK-010` cubierta** |
| `REQ-003` | — (higiene de ambiente) | **179 archivos sin rastrear que no eran del repositorio, y reprobaban la suite de cualquiera con el clon bajo iCloud.** Tras el merge de 434 commits aparecieron copias de conflicto con patrón `<nombre> 2.<ext>` —`mock_data 2.py`, `Execution_Status 2.md`, `ownership 2.yml`, `Traceability_Matrix 2.md`—: **iCloud sincroniza `~/Desktop`**, donde vive el clon, y `pytest`/`ruff`/`vault_lint` los recogían como archivos reales → **20 pruebas reprobadas y 36 problemas bloqueantes de `vault_lint`, ninguno del código** ✅ · Verificado que **los 179 tenían un original rastreado** antes de tocarlos y movidos a cuarentena fuera del repositorio, no borrados → **1042 passed, 0 failed** ✅ · **Riesgo real evitado:** un `git add .` a ciegas el día del freeze habría metido 179 archivos basura al PR, incluidos duplicados de `ownership.yml` y `Execution_Status.md` ⬜ · **No se agregó patrón a `.gitignore`** aunque está en `comunes`: `* 2.*` es lo bastante ancho para ocultar un archivo legítimo y cambiar infraestructura compartida en freeze sin pedirlo no corresponde a C3 | [[vault/_DevLog/2026-09-06-hector-morales-dec019-en-c3]] | ⬜ Reportado al equipo |

## Evidencia incremental — 2026-09-06 · cierre de estado previo a la demo (PM, Edgar Coronel)

| REQ | US | Evidencia | Pendiente | Estado |
|---|---|---|---|---|
| `REQ-007` | 23 historias (`US-004`, `US-005`, `US-113`, `US-204`, `US-207`, `US-215b`, `US-302`, `US-303`, `US-304a`, `US-304b`, `US-313`, `US-322`, `US-324`, `US-325`, `US-403`, `US-405`, `US-504`, `US-521c`, `US-522a`, `US-522b`, `US-522c`, `US-524a`, `US-526`) | **Cierre de estado: el tablero pasa de 52/89 (78.1 %) a 75/89 (88.5 %).** Triaje con tres criterios por historia: evidencia presente en `main`, aplicabilidad de `DEC-012` (superficie desplegada), y visibilidad de la ausencia en la demo. **Grupo A (6)**: el registro iba atrasado — `US-526` en `planned` con FARO Web sirviendo, `US-405` en *«solo andamiaje»* con el PR #267 mergeado, y tres filas citando PRs abiertos que **ya estaban mergeados** (#87, #102, #228, verificados contra GitHub). **Grupo D (17)**: `in_review` cuyo pendiente era ratificación y no trabajo | **Deuda declarada, no borrada.** El grupo D cierra **por decisión del PO sin confirmación de cada dueño** —recomendación contraria del análisis, registrada en el DevLog y en cada fila—; el *«falta»* de cada historia se conservó dentro de su celda de evidencia. Residuales que conviene vigilar: `US-324` (ficha de ML-03 afirma implementación mientras `US-321` sigue abierta y la UI imprime `SIN_DATO`), `US-521c`/`US-522c` (DevLog sin `.md`/ID/estado, incumple `Definition_of_Filed`), `US-204` (validación sobre mock sin repetir contra Gold real), `US-302` (abre `BUG-062`) | done |
| `REQ-007` | `US-321`, `US-114`, `US-524c`, `US-525b`, `US-525c` | **No se cierran, a propósito.** Son historias sin PR ni commit; en el caso de `US-321` el Panel de ML **imprime en pantalla** *«SIN_DATO — US-321 sigue en curso»*, así que un `done` en el tablero se contradiría con lo que el evaluador ve. Marcarlas `done` no dañaría la historia: dañaría la credibilidad del registro, que es justo lo que evalúa el módulo de trabajo en equipo y documentación | **Decisión de alcance pendiente del PO:** la vía honesta es `descoped` con motivo y fecha, y el guion ya tiene el minuto de cierre para *«qué se cortó y por qué»* | open |
| `REQ-002` | `US-207`, `US-305`, `US-405`, `US-526` | **`REQ-002` sólo describía los tableros, y el §3.5 del PRD pide cinco componentes.** Al cotejar el PRD del profesor contra nuestro registro apareció que *Frontend & BI Interactivo* enumera **KPIs en tiempo real, gráficos interactivos, Panel de ML, widget del agente y módulo de auth por rol** — y los cinco cargan sobre los **mismos 2.5 puntos**. Nuestro requisito describía los dos primeros; el panel de ML y el chat **no tenían criterio de aceptación aquí** aunque `US-207` y `US-305` ya trazaban a `REQ-002` desde `User_Stories.md:90`. Ampliado el 2026-09-07 con **`AC-002.7`** (panel de ML), **`AC-002.8`** (widget del agente) y **`AC-002.9`** (login/logout y vistas por rol) ✅ · Se agrega la tabla de correspondencia §3.5 → AC para que el evaluador pueda cotejarla componente por componente ✅ · **Brecha declarada, no maquillada**: el §3.5 pide predicciones de *"los 3 modelos"* y el panel sirve **ML-01 y ML-02** con datos reales; **ML-03 no**, porque su corrida quedó `bloqueada` con D5 al 100 % `SIN_DATO` y D6 al 98.7 %. Queda escrito en el propio `AC-002.7` y se dice en la demo antes de que lo pregunten | El panel de ML sirve de evidencia a `REQ-002` **y** a `REQ-003` —el §3.2 pide que los modelos se integren al Frontend/BI—, pero no se cuenta dos veces en la rúbrica | ✅ Requisito alineado al PRD |

## Evidencia incremental — 2026-09-07 · BUG-063, la prioridad ALTA inalcanzable (PM, Edgar Coronel)

| REQ | US | Evidencia | Pendiente | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-003` | `US-204` · `US-313` · `DEC-019` | **`BUG-063` registrado: el corte de `prioridad` quedó por encima del techo del fenómeno.** `src/modelos/publicar_gold.py:197` asigna `ALTA` sólo con `riesgo >= ANCLA_SIGMOIDE` (0.60) y el máximo real sobre el Gold de producción es **0.5717**, así que **ninguna de las 45 276 escuelas** alcanza la categoría. `superset/dashboards/db09_recomendaciones.yaml:51` expone la tarjeta «Recomendaciones de prioridad ALTA» y el gráfico por `prioridad`: **la tarjeta que dice a qué escuelas entrar primero muestra cero, en el tablero del diferenciador.** Es `BUG-058` en otra columna — `DEC-019` arregló el conteo del KPI-04 y no tocó `prioridad`. `BUG-054` no lo cazó porque corrigió la comparación de mayúsculas y se verificó contra un fixture de ~55 filas con «alta 2»; en 45 276 ese 2 es 0 | **No se corrige antes del 9-sep (decisión del PO):** republicar reescribiría las 45 276 filas publicadas y `DEC-019` prohíbe mover un solo valor. Se responde en la sala, como `escuelas_en_riesgo`. **Fix de fondo post-demo (C3 + PO):** decidir si `prioridad` sigue el **ancla de la sigmoide** o la **línea de alerta**, que desde `DEC-019` son cosas distintas y hoy usa la primera sin que nadie lo haya decidido. **Guarda propuesta:** prueba que contraste el corte de la categoría más alta contra el máximo observado en el Gold publicado — habría cazado `BUG-058` y éste | open |
| `REQ-007` | `US-006` · `BUG-053` | **Detectado al revisar el PR #280: el guion quedó desalineado con producción.** Tras el `ALTER` de C5 las seis columnas `shap_d1..shap_d6` existen en `gold.recomendaciones` pero están en `NULL`, así que `/explicacion` devuelve `SIN_DATO` hasta que C3 repueble el Gold. `BUG-053` sigue correctamente en `fixed` —el código lee las columnas reales— pero el dato no está | **Error del PO al redactar la línea del 2026-09-06**, que afirma *«ya devuelve SHAP real… si sale la pregunta, se enseña»*. Corregir antes del ensayo del lunes, o que C3 repueble | open |

## Evidencia incremental — 2026-09-08 · QA pre-demo de los nueve tableros (C2, Monserrat Miranda)

| REQ | Historias | Evidencia / decisión de estado | Próxima validación | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-213`, `US-214b` | **QA de los 10 tableros contra producción** (`TEST-PLAN-PRE-DEMO`): 20 casos — 10 ✅, 5 ⚠️, 3 ❌, 2 ⏭️, **cero bugs nuevos**. Los 10 cargan y ninguno sale vacío. Cifras cuadradas con el bloque de Diana: matrícula **6,704,229**, completitud **62 %**, riesgo **0.36**, escuelas **44,114**; **KPI-04 = 7** en DB-02, con lo que el punto 6 del plan queda respondido (el Gold desplegado sí se rematerializó). DB-08 sale limpio con 44,114 escuelas **sin inflar** — contraste con el ×6 de `BUG-067`: su métrica es `COUNT(DISTINCT cct)` y no un `SUM` sobre el grano de driver. Bitácora en [[vault/06_Quality_Testing/QA_Logs/2026-09-08-monserrat-miranda-qa-pre-demo]] | Seguir los enlaces `link_db08` de DB-05, no alcanzados (abren en pestaña nueva, donde la sesión se pierde) | ✅ ejecutado en producción |
| `REQ-002` | `US-213`, `BUG-054` | **DB-05 sigue en vertical y DB-09 sigue en `0 %` en producción, y ninguno es un defecto de código**: las dos correcciones están en `main` y ninguna se ha desplegado. La carga a producción corrió el **2026-09-05 19:40** y el fix de layout de DB-05 se mergeó a las **23:39**; verificado además que **ningún workflow de `.github/workflows/` corre el `sync`** (0 menciones en los cinco), así que sólo se actualiza a mano. **Es un solo pendiente de despliegue, no dos bugs** | Ventana de re-sync — decisión del PO, ejecución de C5 | 🟠 pendiente, ajeno a C2 |
| `REQ-002` · `REQ-007` | `US-213`, `US-202` | **Respondido el punto 5 del plan de pruebas**, que congela el `sync` porque *"esa interacción no está verificada"*. Verificado sobre la fuente de Superset 6.1 **en la imagen desplegada**: el importador de dashboards llama `import_chart(config, overwrite=False)` **cableado** (`:147`) —el `overwrite=true` del formulario sólo alcanza a `import_dashboard` (`:183`)— y con `overwrite=False` hace `return existing` **sin ejecutar ningún `setattr`**; para los charts no *timeseries*, el `PUT` omite `query_context`, ningún campo de `ChartPutSchema` es obligatorio y `DAO.update` sólo asigna las claves enviadas. **Un re-sync no borra el `query_context` de nadie.** Corrige un aviso propio del 6-sep que circuló como lo contrario | Confirmación empírica (correr el sync y comparar antes/después) — exige justamente correrlo, así que queda con el PO | ✅ verificado en fuente, ⬜ sin confirmación empírica |
## Evidencia incremental — 2026-09-08 · corrida temporal ML-03 y cobertura indirecta (Estefany)

| Requisito | Historias | Evidencia entregada | Pendiente | Estado |
|---|---|---|---|---|
| `REQ-003` | `US-321`, `US-325` · `RISK-011` | Corrida sobre dump final1 verificado y base aislada de solo lectura: 136,046 filas, 114,200 elegibles, 21,846 excluidas; k=2..6 en una ventana temporal, seleccionado k=3 con Silhouette 0.4644549058. Perfiles, exclusión por territorio y proveniencia en [[vault/15_ML_Models/ML03_Entrenamiento_US321]]. Auditoría: cluster 2 coincide con las 1,648 observaciones elegibles con D6 observado por medio de completitud; alta RISK-011 en [[vault/10_Risk_Governance/Risk_Register]]. | Revisión técnica de Andrés y decisión de Edgar sobre RISK-011; registro MLflow, Gold/API y comparación Panel/DB-03 pendientes. No se publica ni se modifica el vector para mejorar la cifra. | Evidencia de corrida entregada para revisión; US-321 continúa `in_progress`, US-325 conserva `done` |

## Evidencia incremental — 2026-09-08 · el embebido de Superset llega a `main` (C2, Marina García)

| REQ | Historias | Evidencia entregada | Desbloquea | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-004` · `REQ-007` | `US-206` · `US-526` · `US-006` · `BUG-061` · `BUG-071` · `BUG-073` · `BUG-074` | **El embebido de Superset llega a `main`: `BUG-061` cerrado.** El codigo que corre en produccion no estaba en ninguna rama del repo —`_local/` esta en `.gitignore` y vivia solo en la maquina de C5—; lo sube C2 por indisponibilidad de Manuel Serrania, con respaldo del PO. Evidencia: `tests/test_frontend_superset_client.py` (5) y `tests/test_frontend_dashboards_streamlit.py` (3), **1117 pruebas en verde**, `ruff` limpio. `BUG-071` cerrado en las tres paginas, con 6 pruebas nuevas: guarda completa en Dashboards y Chat, controles apagados en el Panel de ML — y `AppTest` **hace cumplir `disabled`**, asi que la prueba mide comportamiento. `BUG-074` (flaky del mock HTTP/1.0) corregido y validado con 5 corridas consecutivas. **`BUG-073` cerrado**: los ejemplos del Panel de ML pasan al par oficial de `US-006` (`15DPR0920D` / `15DPR2254O`), con guarda validada reintroduciendo el defecto. Evidencia de navegador local segun `DEC-020`: las tres paginas verificadas sin sesion | [[vault/_DevLog/2026-09-08-marina-garcia-embebido-superset-guardas-sesion]] | **Desbloquea a C5 para reconstruir la imagen del frontend y cerrar `BUG-070`** (`critical`): el refresco de token esta en `main` desde el 6-sep pero no en la imagen desplegada | fixed |

## Evidencia incremental — 2026-09-08 · cierre de construcción y despliegue final (PM)

| REQ | Historias | Evidencia | Residual / siguiente gate | Estado |
|---|---|---|---|---|
| `REQ-001`…`REQ-007` | 91 de 92 US | `DEC-021` cierra administrativamente la fase de construcción. PR #294 promueve API/agente y reconstruye FARO Web desde `main`; FARO Web, API y Superset responden 200. El dictamen completo, URLs y correspondencia con el PRD viven en [[vault/13_Reports/Cierre_Proyecto_2026-09-08]] | `US-006`: ejecutar demo y entrega el 9-sep. Diana Alvarez presenta el guion completo. No entra código durante el freeze | ready_for_delivery |
| `REQ-003` | `US-321` | PR #292: corrida real ML-03, `k=3`, Silhouette 0.4644549058, perfiles y cobertura documentados | `RISK-011`: no se promueve a Gold/API/UI. El módulo de tres modelos sigue **parcial bajo la letra estricta del profesor** | accepted_with_residual |
| `REQ-004` · `REQ-005` · `REQ-006` | `US-305`, `US-404`, `US-423`, `US-505` | OAuth/RBAC 401/403/200 real, hardening revisado, agente completo en `faro-api-00018-gjx`, frontend `faro-frontend-00009-way` y smoke integral verde | UI del agente single-turn; sesión >16 min se repite en el checklist del día | ready |

## Evidencia incremental — 2026-09-10 · rediseño del chat, Fase 2 (historial de turnos, post-cierre)

> Reapertura autorizada del agente conversacional pese al `DEC-021` (código congelado el 8-sep):
> el chat quedó calificado de "basura" en la revisión del profesor y se relanza en 4 fases, sin
> fecha fija. Ver [[vault/15_ML_Models/Agente_Guardrails_US304a]] y el diagnóstico de Andrés
> González (`DOC-DIAGNOSTICO-AGENTE-CHAT-2026-09-09`, aún sin filed en el vault). Esta entrada
> cubre solo la Fase 2 (memoria conversacional), alcance de Karla Monter en `src/api/**`.

| REQ | Historias | Evidencia | Residual / siguiente gate | Estado |
|---|---|---|---|---|
| `REQ-004` · `REQ-006` | `US-305` | `HistorialTurnoIn` + campo `historial` (opcional, retrocompatible, máx. 10 turnos) en `AgenteConsultaIn` (`src/api/schemas.py`), mismo criterio de entrada hostil que `ContextoConversacionalIn`: `extra="forbid"`, cotas de tamaño, sin caracteres de control. El endpoint (`src/api/v1/agente.py::_construir_contexto_conversacional`) lo fusiona con `contexto` en el `Mapping` que `procesar_consulta` ya acepta, bajo la clave `"historial"`. 20 pruebas nuevas ✅ (`tests/test_agente_historial.py`), `openapi.v1.json` regenerado, suite completa (1161 casos) y `ruff` en verde | `construir_prompt_sistema` (`src/agente/prompt.py`, alcance de Andrés) todavía no lee la clave `"historial"`: el campo llega validado a la API pero sin efecto end-to-end hasta que C3 lo consuma. Fases 3 (streaming) y 4 (robustez/observabilidad) del plan siguen pendientes | 🟡 En progreso (contrato listo, consumo pendiente de C3) |
## Evidencia incremental — 2026-09-10 · estructura del frente de UX/UI y storytelling (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `DEC-022` | **Alta de `vault/04_UX_Design/FARO_Storytelling_UX/` como propuesta (`status: draft`)**: plan maestro, `_index` y un entregable por integrante del Equipo 3, todos con frontmatter, ID y trazas ✅ · **Ocho correcciones a la propuesta original**, registradas en la §0 del plan en vez de aplicarse en silencio; la de fondo es que ubicaba la carpeta en la **raíz del repositorio**, ruta ausente del verde/amarillo/comunes de todo el padrón, con lo que `check_ownership.py` habría reprobado el PR de los cuatro integrantes ✅ · **Mapeo verificado contra `api/openapi.v1.json`** (§10 del plan), que es lo que hace exigible el guardarraíl de no dibujar lo que no existe: construibles hoy el conjunto en riesgo (`GET /escuelas` con `order_by=indice_riesgo`), los seis drivers (`GET /escuelas/{cct}`, `d1..d6`, `indice_completitud_drivers`), recomendación y dominante (`GET /predicciones/{cct}`), su evidencia (`/explicacion`) y los tres filtros obligatorios `ciclo`/`cve_ent`/`nivel` ✅ · **`prioridad` NO es construible**: no aparece en `EscuelaOut`, `EscuelaDetalleOut`, `PrediccionOut` ni `ExplicacionSHAPOut`, y arrastra `BUG-063` —el corte de `ALTA` sigue en `0.60` contra un máximo real de `0.5717`, así que exponer el campo sin decidir el corte deja en cero la tarjeta de la pantalla del diferenciador ⬜ · **Umbral confirmado a medias**: los tres cubos de `dbt/models/gold/` ya calculan con `>= 0.5` (`DEC-019`) y la QA del 2026-09-08 de Monserrat Miranda midió **KPI-04 = 7 en DB-02 contra producción**; falta confirmar que `GET /api/v1/kpis` devuelva ese mismo 7, que es la superficie que consumirá Front y no la que se probó ⬜ · Sin código productivo, conforme al gate del jueves de `Plan_Recuperacion_2026-09-09`. `vault_lint` limpio ✅ | **Seis peticiones abiertas (§11 del plan), ninguna resoluble por este frente.** `P-01` exponer `prioridad` y decidir el corte de `BUG-063` → E5 (contrato) + E4/PO (corte). `P-02` definición oficial de las bandas Alto/Medio/Bajo como `DEC-###` → PO. `P-03` rebautizar el chat como *Watson* → E2 (Andrés) + PO. `P-04` ADR que retire el embebido de Superset y adopte gráficas nativas en Front, porque sustituye `US-206` → E5 + PO. `P-05` confirmar `escuelas_en_riesgo` en `/api/v1/kpis` → E5. **`P-06` es la que urge**: ratificar qué documento manda en UX, porque `03_Visual_Identity` propone sustituir a `UX_Guidelines.md` —`approved` y `source_of_truth`, owner del PO— y sin esa decisión la regla 1 del vault deja dos sistemas de diseño vivos → PO | 🟡 estructura entregada · propuesta pendiente de aprobación del PO |

## Decisión incremental — 2026-09-10 · autorización del rediseño UX/UI (PO)

| REQ | Historias | Decisión y evidencia | Pendiente | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-006` · `REQ-007` | `US-621` · `US-641` · `US-651` | PR #297 aprobado y mergeado. `DEC-023` ratifica la propuesta de Marina y [[vault/03_Architecture/ADRs/ADR-011-rediseno-ux-graficas-nativas]] autoriza UX narrativa y gráficas nativas, con Superset como evidencia/respaldo. `DEC-024` elimina dependencias secuenciales: nivel de atención derivado de `indice_riesgo` (`alta >= 0.50`, `media >= 0.30`, `baja < 0.30`), nombre **Asistente FARO** y KPI `escuelas_en_riesgo` validado contra contrato, implementación y pruebas existentes. [[vault/04_UX_Design/UX_Guidelines]] queda como baseline histórico | E3, Front, Chat, ML y QA construyen y prueban en paralelo mediante contratos versionados; un pendiente de otro equipo no detiene mockups, componentes, pruebas contractuales ni integración incremental | 🟡 ejecución paralela · P-01…P-06 resueltas |

### Gobernanza incremental — alcance de implementación de `US-641`

`frontend/**` queda asignado a Diana Alvarez como líder E5; su acceso de integración se limita a `docker/frontend-entrypoint.sh`, `docker/frontend-react.Dockerfile`, `docker/nginx-frontend.conf.template` y `vault/08_CICD_DevOps/**`. Docker y CI/CD conservan revisión crítica de Luis Téllez. Evidencia y handoff: [[vault/_DevLog/2026-09-10-edgar-coronel-alcance-diana-frontend-s7]].

## Evidencia incremental — 2026-09-10 · Fase 1 del plan de mejora del chat (C3, Andrés González Habib)

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-006` | `US-304a`, `US-304b`, `US-305` | El profesor calificó el agente de "basura"; diagnóstico ubicó la causa en la whitelist rígida de `pregunta_en_alcance`, cero transparencia de cobertura geográfica, bug conocido de Nuevo León y falta de auto-corrección. Fase 1: puerta híbrida (vocabulario ampliado + respaldo semántico del RAG), respuesta directa sin SQL, auto-corrección de SQL (1 reintento), cobertura de las 4 entidades explicada en el prompt, few-shot con el join que corrige Nuevo León. 61/61 pruebas propias ✅ · `ruff` ✅ · **validado con Anthropic + ChromaDB + Postgres reales**: 5/5 casos de prueba correctos | [[vault/_DevLog/2026-09-10-andres-gonzalez-fase1-chat-agente]] | 🟡 En progreso (Fase 2 Karla / Fase 3 Alejandro pendientes) |

## Evidencia incremental — 2026-09-10 · `construir_prompt_sistema` lee el historial de turnos (C3, Andrés González Habib)

> Cierra el residual dejado por la entrada de Karla Monter arriba ("Fase 2, historial de turnos"):
> el campo ya llegaba validado a `contexto_conversacional["historial"]`, pero `construir_prompt_sistema`
> (`src/agente/prompt.py`, alcance de Andrés) todavía no lo leía.

| REQ | Historias | Evidencia de prueba | DevLog | Estado |
|---|---|---|---|---|
| `REQ-004` · `REQ-006` | `US-305`, `US-304a`, `US-611` | `construir_prompt_sistema` agrega un bloque "Historial de la conversación" cuando `contexto_conversacional["historial"]` viene no vacío, con la transcripción literal `Usuario:`/`Agente:` de cada turno. Mismo criterio de dato hostil que `ccts`/`ciclo`/`filtros`/`resumen`: se advierte que el historial es texto de usuario, no instrucciones nuevas ni SQL, y que cualquier orden dentro de un turno pasado se ignora (defensa contra inyección). Retrocompatible: sin `historial` o con lista vacía, el prompt no cambia. Además, `redactar_respuesta_con_llm` colapsa saltos de línea/tabs de la respuesta del agente para que sea segura de reenviar como turno de `historial` (`HistorialTurnoIn` rechaza caracteres de control); la cota de 500 caracteres sigue pendiente de acuerdo con Karla/Diana (cliente vs API). 5 pruebas nuevas ✅ (`tests/test_agente_prompt.py`, `tests/test_agente_llm.py`), 40/40 en verde, `ruff` limpio | [[vault/_DevLog/2026-09-10-andres-gonzalez-historial-en-prompt-us305]] | 🟡 Backend listo (contrato + prompt); E2E pendiente: que el cliente envíe `historial` y la prueba con el LLM real en la candidata |

## Evidencia incremental — 2026-09-10 · alineación del paquete de UX/UI a `ADR-011` (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `ADR-011` · `DEC-023` · `DEC-024` | **Las seis peticiones del PR #297 quedaron resueltas por el PO y el paquete se alinea.** `PLAN_TRABAJO` y `00_Storytelling_Scope` pasan a `approved`; `_index` a `active` ✅ · `P-01`/`P-02` se fusionan en una sola etiqueta: **nivel de atención** derivado de `indice_riesgo` (alta `>=0.50`, media `>=0.30`, baja `<0.30`), con prohibición explícita de consumir `gold.recomendaciones.prioridad` ✅ · `P-03`: sale "Watson", entra **Asistente FARO** en las cuatro superficies del paquete ✅ · `P-04`: se corrige un error propio — yo había escrito que `Manual_Usuario_Dashboards` pasaba a documentación histórica, y `DEC-023` mantiene Superset como evidencia y respaldo, así que **sigue vigente** ✅ · `P-06`: `UX_Guidelines.md` quedó `superseded` por su propio owner; este frente **no editó** ese archivo ✅ · Se añaden los no negociables de `ADR-011` §4, que el plan no tenía escritos —**WCAG 2.1 AA** incluido— y el régimen paralelo de `DEC-024`, que convierte el viernes en hito y no en compuerta ✅ · Se escribe el **criterio de cierre de `US-621`** (§14): cuatro entregables finales, 7 mockups y PDF, handoff aceptado por E5 y QA sobre la candidata ✅ · `vault_lint` limpio ✅ | **Dos avisos verificados que van a E5 antes de que implementen.** (1) **El nivel de atención y la columna Gold `prioridad` discrepan justo en las escuelas de la historia**: `src/modelos/publicar_gold.py:197` asigna `ALTA` con `>= ANCLA_SIGMOIDE` (0.60) y `MEDIA` con `>= RIESGO_ESTABLE` (0.30), así que coincide con el corte de presentación en media/baja y difiere **sólo entre 0.50 y 0.60** — donde vive el fenómeno, porque el máximo observado es `0.5717`. El front dirá *atención alta* y Gold dirá `MEDIA` para las mismas escuelas; DB-09 sigue expuesto por `DEC-023`, así que quien cruce las dos superficies ve una contradicción. Escrito en §3.quater; el glosario debe distinguirlos ⬜ · (2) **Las dos constantes viven en capas distintas**: `LINEA_DE_ALERTA` en `src/api/repositorio_gold.py:55` y `RIESGO_ESTABLE` en `src/modelos/riesgo.py:76`, crítico de Estefany Hernández. El front necesita ambas; teclear el `0.30` repite el patrón de `BUG-058` ⬜ · **Se conserva sin cambio** la regla de que el número de escuelas nunca va escrito a mano: `ADR-011` dejó la comprobación en el smoke continuo de QA, y un literal es justo lo que ese smoke no detecta | 🟢 paquete alineado y aprobado · avisos abiertos a E5 |


## Evidencia incremental — 2026-09-11 · Frontend E5 conectado al API real, revisión de Edgar en PR #302 (Diana Alvarez)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-005` | `US-641` · `ADR-012` | **`Home.jsx`, `LosSieteCasos.jsx` y `ExpedienteEscuela.jsx` (tabs Resumen/Drivers/Predicción/Recomendación) corren 100% contra el API real**, verificado en navegador (Network tab, respuestas reales) ✅ · **`BUG-064` encontrado y corregido**: `getEscuelasEnRiesgo()` no desenvolvía el sobre de paginación (`Page[EscuelaOut]`), "Los 7 casos" quedaba en blanco sin ningún error visible — corregido en `frontend/src/lib/api.js` ✅ · **Limpieza de lo que no es producto**: `frontend-angular-poc/` (PoC descartado), `ComparativaStacks.jsx`/`src/comparativa/` y sus assets, y 5 dependencias sin uso (`react-simple-maps`, `@observablehq/plot`, `prop-types`, `react-is`, `topojson-client`) fuera del repo y de `package.json` ✅ · `npm ci` **validado sin `--legacy-peer-deps`** tras esa limpieza: 0 vulnerabilidades, 0 conflictos de peers (era necesaria solo por las 4 dependencias ya retiradas) ✅ · **4 pantallas bloqueadas por contrato** (`MapaCasos`, `MatrizDrivers`, `ComparacionTerritorial`, `Comparativa`) muestran `EnConstruccion` con el gap puntual de cada una en vez de mock sin rotular ✅ · **`VistaGeneral` conectada parcialmente**: el bloque "El diferenciador" usa `getEscuela`+`getPrediccion` reales para el par `15DPR0920D`/`15DPR2254O` (el mismo que `Guion_Demo_US006` documenta, elegido por Marina García el 6-sep) ✅ · **Inter autohospedado** vía `@fontsource/inter` — el CSP de `nginx-unprivileged` en prod bloqueaba el `<link>` a Google Fonts (`style-src`/`font-src` solo `'self'`) ✅ · **`ADR-012` ratificado** (`status: accepted`, pedido explícito de Edgar en esta misma revisión) ✅ | **Dos pendientes que dependen de Luis Téllez (regla 7, `docker/**`/`vault/08_CICD_DevOps/**`).** (1) Aprobación explícita del PR sobre esas rutas críticas: se retira `--legacy-peer-deps` de `docker/frontend-react.Dockerfile` y se corrige el comentario de `frontend/.env.production`, ambos dentro de su alcance crítico. (2) Hallazgo propio, más allá de lo que pidió Edgar: `docker/.dockerignore` probablemente no se aplica — el contexto real del `docker build` es la raíz del repo (`.`), no `docker/`, así que Docker busca el `.dockerignore` ahí y no lo encuentra; pendiente de que Luis lo mueva/duplique en la raíz. **Un pendiente no bloqueante para este PR** (crítico para el viernes, per Edgar): el botón de login todavía no existe (`Topbar.jsx` sin estado de sesión) — el diseño de a dónde debe apuntar ya quedó documentado en `ADR-012` | 🟡 mayoría conectada y en revisión · 2 pendientes de Luis (regla 7) · login no bloqueante pendiente |

## Evidencia incremental — 2026-09-10 · revisión de Monserrat Miranda al paquete de UX (E3)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-006` | `US-621` · `ADR-011` · `US-611` | **Monserrat Miranda revisó el paquete antes de cerrar su documento y levantó cuatro puntos; tres se aplican.** (1) **Desfase corregido, y el defecto era del plan**: el §8 ponía sus ejemplos de visualización y los 7 mockups de Juan en el mismo corte del viernes 15:00 y a la vez decía que ella le entrega a él para integrarlos. Se separa: entrega intermedia hoy tras el gate, con el formato que defina Juan ✅ · (2) **`ejemplos_graficas/` dada de alta** en el paquete y registrada en su `_index` (regla 4); sus ejemplos no tenían ubicación en la estructura original ✅ · (3) Nueva **§10.bis** con dos límites del contrato que ella verificó: `/api/v1/escuelas` **no tiene filtro por `indice_riesgo`** —el corte en la línea de alerta se hace del lado del cliente— y **no existe endpoint de Top 3 agregado**. Se añade el matiz que abarata la implementación: el endpoint sí acepta `order_by=indice_riesgo` con `order=desc`, así que **una sola llamada con `size=100`** basta y se corta al bajar de `0.50`; no hay que recorrer las 45 276 ✅ · (4) Su primer punto tenía la premisa invertida —preguntaba si el nombre visible es *Agente FARO* «aunque el `ADR` diga otra cosa»—: el paquete dice **Asistente FARO** en sus 13 apariciones, igual que `ADR-011` §6, así que no hay desacuerdo ni nada que pedirle al PO ✅ | **Hallazgo real que sale de ese cuarto punto, y va al Equipo 5.** `src/frontend/pages/3_Chat.py:33` imprime hoy `st.title("Agente FARO")`, que es el nombre **técnico** del módulo y no el de producto que fija `ADR-011` §6. **La trampa no es esa línea sino las pruebas**: `tests/test_frontend_chat_streamlit.py` afirma el literal viejo en las líneas **77** y **114**, así que cambiar el título sin tocarlas **rompe CI**. Trabajo de E5 (dueño de `src/frontend/**`) o E2. Los nombres técnicos —`src/agente/**`, `/api/v1/agente/consulta`— **no se tocan** ⬜ · Quedan además 8 documentos del vault con el nombre viejo; son técnicos o históricos y no bloquean ⬜ · Se ratifica a Monserrat que `P-01` y `P-02` se cierran **sin campo nuevo**: la petición a C4/E5 para el 11-sep 12:00 queda **cancelada** ✅ | 🟢 aplicado · aviso del título abierto a E5 |

## Evidencia incremental — 2026-09-10 · la sección "Cómo funciona" de E1 entra al plan de UX (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-001` · `REQ-007` | `US-621` · `US-601` | **Nueva §5.bis del plan: la sección *Cómo funciona* del Equipo 1 entra a la experiencia.** Cubre el hallazgo del profesor *«faltó explicar cómo funciona el backend»*, que es de `US-601` y que E1 ya construyó: siete apartados (`arquitectura`, `modelo-datos`, `capas`, `cubos`, `stack`, `decisiones`, `modelos-ml`), dueño formal Héctor Morales, redactada e implementada por Manuel Serranía ✅ · **Decisión de diseño: superficie hermana, no octava pantalla.** Se alcanza desde la Pantalla 1 y desde el glosario, comparte identidad y no interrumpe el recorrido. Motivo narrativo, no de esfuerzo: la historia va de qué le pasa a las escuelas, no de cómo está hecho el sistema. Efecto práctico: los entregables siguen siendo **7 mockups** y el trabajo de Juan no se dispara a cuatro días del cierre ✅ · Contrato registrado en §10: `GET /api/v1/about/secciones` y `GET /api/v1/about/secciones/{id_seccion}`, sobre genérico `{id, titulo, fuente, advertencias, bloques}` con **7 tipos de bloque**, tres de ellos **D3** —justo lo que pidió el profesor—; un tipo desconocido se pinta con advertencia y **no tumba la página**, mismo espíritu de `SIN_DATO` aplicado a la estructura ✅ · Nuevo criterio de aceptación **27** ✅ · `vault_lint` limpio ✅ | **Dependencia declarada, va a E1.** Las dos rutas, la página y sus 54 pruebas viven en `dev/manuel-serrania` y **no están en `main`**. Si no aterrizan, la §5.bis se cae del alcance y se declara el recorte ⬜ · Por la misma razón `Bosquejo_Componentes_US601.md` se cita **sin wikilink**: enlazar a un archivo ausente de `main` reprueba `vault_lint`; se convierte en enlace al aterrizar ⬜ · **Aviso concreto para Juan (E3):** cada bloque D3 vive en su propio iframe y E1 ya se topó con el defecto de heredar `prefers-color-scheme: dark` —letras negras sobre fondo negro—, resuelto forzando `color-scheme: light` y fondo blanco. **Si la identidad nueva es oscura, esos bloques pelean con ella**; hay que decidirlo a propósito, no descubrirlo el sábado ⬜ · **Fuera de alcance por decisión de la líder de E3:** `ADR-012` (React, retiro de Streamlit, sesión por cookie) sigue `proposed` en `dev/diana-alvarez` y **no se incorpora**; el diseño no se ata a una decisión sin ratificar | 🟡 en el plan · condicionada al merge de E1 |

## Evidencia incremental — 2026-09-10 · leyenda de gráficas y el SQL del Asistente (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-006` | `US-621` · `US-611` | **Dos ajustes del checkpoint entran al plan.** (1) **Nueva §7.bis, leyenda de las gráficas**, al alcance de Monserrat: cada gráfica debe explicar qué se está viendo —ejes o series en lenguaje de negocio, unidad del valor, cómo se ve un `SIN_DATO` ahí dentro, ciclo y recorte—, no la leyenda de colores que trae cualquier librería. Va directo contra el hallazgo del profesor sobre visualizaciones que no comunicaron valor: una gráfica que hay que explicar en vivo no comunica sola. Se escribió en el plan y **no** en el documento de Monserrat, a propósito: su archivo es suyo y puede estar editándolo en local sin empujar (regla del §13) ✅ · (2) **Nueva §4.ter, el SQL del Asistente.** Se dudaba si era alcance de este frente y **lo es, en parte**: la §4 reparte lógica a E2 y comportamiento visual a E3, y qué se pinta es presentación ✅ · Nuevos criterios de aceptación **28** y **29** ✅ · `vault_lint` limpio ✅ | **Hallazgo verificado, va a E2/E5.** Hoy el SQL **sí se muestra**: `src/frontend/pages/3_Chat.py:85-87` pinta `respuesta.sql_generado` con `st.code(..., language="sql")` en cuanto viene, y el campo existe en el contrato como `AgenteRespuestaOut.sql_generado`. El profesor lo señaló el 9-sep (*«la explicación no debe reducirse a mostrar SQL»*, `REQ-006`, E2/`US-611`) ⬜ · **Decisión de UX: el campo se conserva en el contrato y la interfaz no lo muestra por defecto.** Se evitó pedir su eliminación: sirve para auditar, para QA y para que el evaluador compruebe que la respuesta sale de la base y no de un texto inventado. Queda tras una acción opcional del tipo *ver consulta*, cerrada de inicio. El profesor no pidió ocultarlo sino que la explicación no **se redujera** a mostrarlo; ocultarlo por defecto y dejarlo disponible responde a las dos cosas ⬜ · Implementa E2 o E5 según dónde viva el componente; este frente sólo especifica ⬜ · **Confirmado además que la §5.bis no agrega un mockup a Juan**: *Cómo funciona* es superficie hermana, el recorrido sigue siendo de 7 pantallas y el entregable de 7 mockups ✅ | 🟢 especificado · implementación abierta a E2/E5 |

## Evidencia incremental — 2026-09-10 · revisión del entregable de Oscar y corrección del Mockup 0 (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `US-641` | **`01_UX_Architecture.md` de Oscar Quiroz revisado contra el plan vigente: cumple los criterios 21 y 22** —el Equipo 5 podría implementar sin redefinir decisiones de UX, que era el objetivo del entregable ✅ · Nombre del Explorador cerrado (**Explorador de escuelas**, de tres opciones) ✅ · Tres aportes suyos por encima de lo pedido: el **401 tratado como sesión expirada** conservando la ruta para retomarla tras autenticar; `tiene_prediccion = false` **sin inventar recomendación genérica**; y enlace directo a P4/P6 con justificación de producto ✅ · **Corregido un error de este plan, no suyo:** la ficha del Mockup 0 decía *«los campos y acciones del login actual»* y es **falso** — `src/frontend/auth.py:194` muestra que el acceso es **un solo botón** `st.link_button("Iniciar sesión con Google", ...)` que redirige al consentimiento de Google; no hay usuario, contraseña ni formulario. Oscar heredó esa redacción y derivó de ella un estado de *«credenciales inválidas»* que no puede ocurrir, porque un fallo de OAuth vuelve por el callback. El costo de no corregirlo hoy era que **Juan dibujara un formulario inexistente** y se descubriera al integrar. Corregido en el plan y en el scope, con los estados reales —en reposo, redirigiendo a Google, error de vuelta del callback— y con lo que la pantalla **no** lleva ✅ | **Tres observaciones a Oscar (E3).** (1) Su walkthrough paso 3 promete *«pregúntale al Asistente sobre lo que estás viendo»*, pero el Asistente **no tiene contexto de pantalla ni de escuela** — él mismo lo escribe en su §6. Es una promesa que el producto no cumple; se corrige con el copy ⬜ · (2) Le faltan tres piezas del plan que aterrizaron después de que escribió, no atribuibles a él: el acceso a *Cómo funciona* (§5.bis) desde P1 y el glosario, la regla del SQL oculto por defecto (§4.ter) en su §6, y la existencia de la leyenda de la §7.bis ⬜ · (3) Menor: su tabla de filtros dice de P2 *«Limitados»* y en la misma celda *«ninguno de los 3 obligatorios»*; hay que elegir una ⬜ · **Pregunta de narrativa abierta para la líder**, no defecto: el logo lleva de P6 a P1, pero P1 no revela el número, así que quien ya vio la revelación vuelve a una entrada que finge no saber | 🟢 visto bueno condicionado a las tres observaciones |

## Evidencia incremental — 2026-09-10 · especificación de visualizaciones de la historia (E3, Monserrat Miranda)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `ADR-011` · `DEC-024` | **`02_Data_Visualization_Spec` en borrador formal**: qué dato y qué gráfica sostienen cada pantalla (P2 a P6), con endpoint y campos por fila ✅ · **Consulta a producción** (commit `457715a`, 10-sep 18:24): `/kpis.escuelas_en_riesgo` = 7 y el corte cliente sobre una sola página de `/escuelas` da las mismas 7 — la mecánica del conjunto en riesgo y del Top 3 queda probada en la superficie que consume Front ✅ · **Seis ejemplos PNG/SVG con datos reales** y script reproducible en `FARO_Storytelling_UX/ejemplos_graficas/`, con contraste WCAG 2.1 AA medido ✅ · **Cinco referencias de forma de Monserrat** en `ejemplos_graficas/referencias/`, evaluadas contra el contrato (§7.4), y libertad total de maquetación e identidad para Juan Macías, separada de las reglas de lectura del dato (§7.5) ✅ · `vault_lint` y `ruff` limpios ✅ · [[vault/_DevLog/2026-09-10-monserrat-miranda-us621-dataviz-spec]] | **Dato que condiciona la historia:** en las 7 escuelas sólo hay evidencia de D1, D2 y D4 (3 de 6); D3, D5 y D6 son `SIN_DATO` en todas, y D5 es `null` en todo el universo ⬜ · SHAP sin poblar en producción → Equipo 4 (`US-631`) ⬜ · `es_estimado_por_grupo` siempre `null` → Diana Alvarez / Héctor Morales ⬜ · alta de `ejemplos_graficas/` en el `_index` → Marina García ⬜ · formato de entrega → Juan Macías ⬜ · avisos de implementación de la §8.2 → Equipo 5 ⬜ | 🟡 borrador formal · final el viernes 11 a las 15:00 |

## Evidencia incremental — 2026-09-10 · correcciones de la §10 y el PR que nunca se abrió (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` · `REQ-003` | `US-621` · `BUG-053` | **Dos correcciones a la §10 del plan, ambas halladas por Monserrat Miranda al construir su especificación.** (1) **La explicación SHAP se prometía y no existe.** La tabla listaba `/predicciones/{cct}/explicacion` → `contribuciones` como evidencia disponible del driver dominante. `BUG-053` está `fixed` en el sentido correcto —el código lee `gold.recomendaciones.shap_d1…shap_d6`— pero esas columnas **no están pobladas en producción**: existen tras el `ALTER` de C5 y siguen en `NULL`. Medido el 10-sep: **0 de 42**. El endpoint responde 200 con las contribuciones vacías, que es la peor forma de fallar porque parece que funciona. Nueva §10.quater: el expediente muestra el driver dominante —que sí viene en `PrediccionOut`— y declara la explicación como `SIN_DATO`; si el Equipo 4 puebla las columnas, la pieza entra sin cambio de contrato ✅ (2) **`/kpis` no acepta `nivel`**, sólo `ciclo`, `cve_ent` y `cve_mun`. La §10 los listaba como si `/escuelas` y `/kpis` aceptaran los cuatro. Consecuencia real: el filtro de nivel educativo actúa sobre la lista pero **nunca sobre los indicadores**, así que sin decirlo el Equipo 5 pasa un parámetro que la API ignora y los números de arriba dejan de corresponder con la lista de abajo, sin error visible ✅ · Se corrigen en el plan y no sólo en el documento de ella, porque era la §10 la que lo prometía y es la que leen Oscar, Juan y E5 ✅ | **Fallo de proceso de este frente, registrado y no disimulado.** Se comunicó al equipo que las secciones nuevas estaban en `main` y **era falso**: se empujó a `dev/marina-garcia` y **nunca se abrió el PR**, así que `main` se quedó en `e775ff4` y §4.ter, §5.bis, §7.bis, §10.bis, §10.ter y el alta de `ejemplos_graficas/` en el `_index` del paquete no llegaron a nadie. Lo detectó Monserrat revisando `main` (`108be11`) y tenía razón en los dos puntos que reclamó ⬜ · **El costo ya se cobró tres veces:** Oscar escribió contra un plan sin esas secciones, Monserrat también, y Juan dibujó en `00_Login.html` un formulario de usuario y contraseña que no existe —el acceso real es un único botón de Google— porque la corrección del Mockup 0 vivía en un commit sin mergear ⬜ · **Acción:** abrir el PR de `dev/marina-garcia` hoy; es lo único que desbloquea a los tres ⬜ | 🔴 corregido en la rama · bloqueado hasta abrir el PR |

## Evidencia incremental — 2026-09-11 · los 7 mockups corregidos y liberados para Equipo 5 (E3, Juan Macías)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` | **Los 7 mockups de escritorio quedan listos y liberados para Equipo 5**, sobre el `00_Login.html` corregido por Marina (`abeb393`): un único botón OAuth de Google, sin formulario. **Reescrito el sistema de color de datos** que el PR #310 entregó mal: salen el semáforo rojo/ámbar/verde y los 6 colores por driver, entra una rampa secuencial de un tono (`#EEF2F7→#0F172A`) + acento ámbar `#B45309` exclusivo del dominante + gris de contexto `#94A3B8` + textura rayada para `SIN_DATO` — alineado con `ejemplos_graficas/referencias/LEER_PRIMERO.md` de Monserrat Miranda y con `ADR-011` §4 (nada codificado solo por color) ✅ · Nivel de atención sin color propio, solo icono ▲■● + texto, igual que en los ejemplos reales de producción de Monserrat ✅ · Matriz de la Pantalla 2 con solo las 6 columnas de drivers (se quitaron "Índice y atención" y "Evidencia disponible", que no correspondían a esa tabla) ✅ · Pantalla 6 (Explorador) reescrita: la primera versión afirmaba causalidad explícita ("vínculo causal dominante", una cifra de absentismo inventada) y usaba D5 —que no tiene fuente de datos integrada— como dominante con valor; ahora usa el catálogo real de recomendaciones y declara explícitamente "sin inferencia causal directa", conforme al criterio de aceptación 9 del plan ✅ · Tagline de marca unificado en las 8 pantallas ✅ · Corregido en integración (no ameritaba otra vuelta a Stitch): una frase residual en la Pantalla 2 que llamaba a D2 "principal detonante de deserción" — también causal, cambiada a "el driver dominante más frecuente en el corte actual" ✅ · `vault_lint` limpio ✅ | **Queda abierto:** auditoría formal de contraste WCAG 2.1 AA sobre la rampa/acento (§6 de `03_Visual_Identity.md`), `FARO_UX_UI_Guide.pdf`, y chrome decorativo menor sin corregir (algunas menciones de "crítico" y dos frases sueltas — "DICTAMEN_LEGISLATIVO", "CADENA DE CUSTODIA DIGITAL VERIFICADA" — que no son datos incorrectos, solo estética de panel de operaciones) → Juan Macías, sin bloquear a Equipo 5 · La superficie *Cómo funciona* de Manuel Serranía sigue sin sus dos rutas de API en `main` (`dev/manuel-serrania`) → Equipo 1 | 🟢 liberado para Equipo 5 · dos pendientes menores sin bloqueo |

## Evidencia incremental — 2026-09-11 · retiro del PDF y alcance de la leyenda (E3, Marina García)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `ADR-011` · `DEC-024` | **`FARO_UX_UI_Guide.pdf` retirado del alcance y del criterio de cierre**, a propuesta de Juan Macías. Nueva **§7.ter** del plan; limpiado de §7, §8, §14 punto 2 y del `_index` del paquete ✅ · Razones registradas: el `.md` está versionado y sincronizado con los mockups, E5 copia valores exactos en vez de transcribirlos, y un PDF exige sincronización manual — más dos que pesan por encima del mantenimiento: **el PDF no responde a ninguno de los hallazgos del profesor** (experiencia, gráficas, storytelling, componentes, chat y ML; ninguno documental) y `DEC-024` ya fijó que las únicas compuertas son rama personal, PR, CI, una aprobación humana y QA sobre la candidata ✅ · **No se aceptó** que `mockups/Design_Tokens_Stitch.md` sustituyera al PDF: su frontmatter es el YAML crudo de Stitch sin `id`/`owner`/`status` —incumple la regla 2 y `Definition_of_Filed`—, su encabezado lo declara *«no es un artefacto canónico del vault»*, y por esa misma nota **el sistema de color de datos no vive ahí** sino en `03_Visual_Identity.md` §3. Un anexo no puede ser la guía; la guía ya existía y tiene `id`, `owner`, `status` y trazas ✅ · **Leyenda (§7.bis), dos precisiones que faltaban:** se escribe **dentro de** `02_Data_Visualization_Spec.md` y no en un documento aparte (regla 1), y queda como ítem explícito de la entrega del viernes de Monserrat, que la §8 no listaba; y su alcance es **por gráfica, no por pantalla** — P2, P3, P4, P5 y P6, porque la primera lectura del equipo la acotaba a dos y dejaba tres sin cubrir ✅ | **A Juan Macías (E3):** frontmatter propio con `id`/`owner`/`status` para el anexo de tokens; crear `mockups/_index.md`, que no existe y donde viven los 7 mockups (regla 4); y limpiar las **tres menciones al PDF que conserva `03_Visual_Identity.md`** — no se editaron a propósito, la §13 prohíbe tocar el entregable de otro y lo está trabajando ahora ⬜ · **A Monserrat Miranda (E3):** escribir la §7.bis dentro de su spec, con el alcance de cinco pantallas ⬜ · **Al PO:** es un cambio a un criterio de cierre de `US-621` y el *go/no-go* es suyo; se registra en vez de desaparecer en silencio ⬜ | 🟢 plan actualizado · pendientes repartidos |

## Evidencia incremental — 2026-09-11 · leyenda de las gráficas (§7.bis) y revisión de Cómo funciona (E3, Monserrat Miranda)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `ADR-011` · `US-601` | **§7.bis escrita dentro de `02_Data_Visualization_Spec.md`** (no en archivo aparte, regla 1), con el alcance **por gráfica y no por pantalla** que precisó Marina: qué se está viendo, unidad, cómo se ve un `SIN_DATO` ahí y de qué ciclo/recorte habla, resuelto para las cinco gráficas de la historia (P2–P6) más el panel de explicación del modelo — cierra el criterio de aceptación **28** ✅ · **§8.4 nueva:** revisión de "Cómo funciona" (§5.bis, Equipo 1) contra las reglas de forma de este documento, cinco puntos, ninguno defecto de ellos — el punto 4 (fondo forzado de los bloques D3 contra una identidad oscura) queda resuelto por el hallazgo posterior de Juan Macías: la identidad nunca es oscura, solo lleva acento, así que esos bloques no necesitan tratamiento especial ✅ · **§8.3 corregida:** ya no dice "pendiente, aún no llega a `main`" de piezas que llevaban un día mergeadas (PR #312) ✅ · **Los seis ejemplos regenerados con la leyenda integrada** como bloque fijo y visible, nunca tooltip, midiendo el texto ya renderizado en vez de una posición adivinada — el primer intento chocaba en cuatro de las seis pantallas ✅ · `ruff` y `vault_lint` limpios ✅ · [[vault/_DevLog/2026-09-11-monserrat-miranda-us621-leyenda-7bis]] | **Dos hallazgos para Marina, sin resolver todavía.** (1) **El criterio 28 no distingue superficies**: leído literal, los tres bloques D3 de "Cómo funciona" también necesitarían su leyenda, pero el texto es del Equipo 1, que sabe qué mide cada uno — o se acota el criterio 28 a las gráficas de la historia, o el Equipo 1 escribe sus cuatro declaraciones; dejarlo ambiguo lo descubre QA el domingo ⬜ · (2) **Cifras fijas dentro de los bloques D3**: si alguno lleva un número escrito (cuántas escuelas, cuántos municipios), es el mismo patrón que la regla de `N` existe para evitar — el día que cambie el ciclo, esa cifra queda vieja y nada lo detecta; recomendación de que salgan del conteo vivo o no aparezcan ⬜ | 🟢 §7.bis y §8.4 entregadas · dos hallazgos abiertos a Marina |

| `REQ-002` | `US-621` · `ADR-011` · `DEC-024` | **Los tres pendientes de Juan Macías (E3) de la fila anterior, cerrados.** `mockups/Design_Tokens_Stitch.md` recibe frontmatter propio (`DOC-FARO-UX-TOKENS`, owner, status, trazas); el YAML crudo de Stitch se mueve a un bloque de código dentro del cuerpo, ya no ocupa el frontmatter real del archivo ✅ · `mockups/_index.md` creado (regla 4) con los 7 HTML + 7 PNG + material de soporte, enlazado desde el `_index` del paquete ✅ · Las tres menciones al PDF en `03_Visual_Identity.md` (encabezado, estado, §8) corregidas para reflejar el retiro del §7.ter ✅ · **Auditoría propia del frente, aparte de lo pedido:** `01_UX_Architecture.md` de Oscar Quiroz **no está en `main`** — vive en `dev/oscar-quiroz` (commits `fa2214a`/`e53c723`/`1b4c69f`) sin PR abierto, mismo patrón que le pasó antes a Marina. Lo que Equipo 5 ve hoy en `main` es el template vacío original, sin llenar ✅ verificado con `git log`/`gh pr list`, no reportado en silencio · **Datos inventados corregidos** en `02_Panorama_Escuelas_Riesgo.html`, `03_Seleccion_Caso.html`, `05_Conclusion_Top3.html` y `06_Explorador.html`, siguiendo el patrón de `LEER_PRIMERO.md` (número en vivo nunca se teclea): "7 escuelas"/"N=7"/"5 de 4,112" → `N`/`k`/`[entidad]`; se quitó "Cluster Prioritario: Edomex Norte-Valle Central" (nombre de agrupación que ML-03 no produce); un hallazgo propio no listado por Marina —una fórmula del pie de Panorama con `N=7` tecleado— también corregido ✅ · **Decisión de convivencia claro/oscuro documentada** en `03_Visual_Identity.md`: el lienzo de esta identidad nunca es oscuro (el slate `#0F172A` es solo acento de componentes), así que los 3 bloques D3 de "Cómo funciona" (forzados a claro) no necesitan tratamiento especial ✅ · Sincronía: el commit de retiro del PDF de Marina (`974d16e`) tampoco tenía PR — se fusionó directo desde su rama, en sync 1:1 con `main`; conflicto menor en el `_index` del paquete resuelto a favor de su versión ✅ · `vault_lint` limpio | **Bloqueante real, no de este frente:** `01_UX_Architecture.md` de Oscar sin PR — nadie más puede escribirlo ni abrirlo por él; bloquea el criterio de cierre #1 de `US-621` ⬜ · Auditoría formal de contraste WCAG 2.1 AA (§6 de `03_Visual_Identity.md`) ⬜ · Leyenda de las 5 gráficas (§7.bis): la escribe Monserrat dentro de su spec, Juan le da tratamiento visual cuando llegue ⬜ | 🟡 Equipo 3 al día salvo Oscar · bloqueado por PR ajeno |
## Evidencia incremental — 2026-09-11 · Fase 2 de identidad visual y corrección de rama (Diana Alvarez)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `US-641` | **`driverColors` (color fijo por driver) y el semáforo de `nivelRiesgo()` — ambos marcados "RECHAZADO, NO USAR" en `03_Visual_Identity.md` §3 — reemplazados en todo el frontend.** Nuevo `frontend/src/lib/riskRamp.js`: rampa secuencial de un solo tono (`#EEF2F7→#0F172A`, 5 paradas, `d3.scaleLinear().clamp(true)`) para cualquier magnitud 0→1, acento ámbar `#B45309` **solo de contorno** para el driver dominante, `SIN_DATO` explícito ✅ · `nivelRiesgo()` cambia de `{label, color}` a `{label, icon}` (▲/■/●) ✅ · 6 usos corregidos: `ExpedienteEscuela.jsx`, `DifferentiatorChart.jsx`, `MapaRiesgo.jsx`, `LosSieteCasos.jsx`, `Hallazgos.jsx` — el badge de "Driver dominante" pasa de relleno de color a contorno ámbar; el nivel de riesgo se pinta por icono + texto, nunca color; se agregó el icono/etiqueta de nivel a la leyenda de "Los 7 casos", que antes codificaba el nivel solo por color sin texto en ningún lado (misma violación que el documento prohíbe, no pedida explícitamente) ✅ · Verificado en navegador por Diana (expediente, "Los 7 casos", Vista general) ✅ · `vault_lint` limpio ✅ | **Desvío propio de `Branching_Strategy.md` encontrado y corregido antes de tocar `main`**: el trabajo se hizo primero en una rama nueva por historia (`dev/diana-alvarez-identidad-visual`) para no seguir empujando a `dev/diana-alvarez` mientras Edgar revisaba el PR #302 — es el antipatrón explícito del documento ("una rama por persona, permanente, nunca por historia"). Corregido con `git cherry-pick` del commit de vuelta a `dev/diana-alvarez` (única rama fija) una vez mergeado el #302, sin conflictos ⬜ | 🟢 aplicado y verificado · PR pendiente de abrir |

## Evidencia incremental — 2026-09-11 · `01_UX_Architecture.md` en versión final, handoff a Equipo 5 (E3, Oscar Quiroz)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` · `US-641` | **Cierra el bloqueo que señaló Monserrat en la fila anterior:** la PR de este documento estaba sin abrir; ya es la **[#319](https://github.com/edgarcoroneln/escuela-concausa-bi/pull/319)**, con las 8 secciones cerradas: mapa de navegación (con entrada directa por URL a P4/P6 y la superficie hermana *Cómo funciona*, §5.bis), ficha por pantalla P0–P6, filtros, walkthrough, pop-up de exploración, comportamiento del Asistente FARO, nombre final de la exploración (**Explorador de escuelas**, elegido por Marina) y estados vacíos/error/`SIN_DATO` ✅ · **Dos rondas de corrección aplicadas** tras la revisión de Marina y los hallazgos de Monserrat sobre el PR #308: Mockup 0 reescrito (login de un solo botón OAuth, sin "credenciales inválidas"), copy del walkthrough sin prometer contexto de pantalla al Asistente, filtros de P2/P6 cerrados palabra por palabra contra `02_Data_Visualization_Spec.md` (`nivel` no existe en `/kpis`), espacio de leyenda obligatoria (§7.bis) reservado en **las cinco pantallas con gráfica** (P2, P3, P4, P5, P6, no solo dos), acceso a *Cómo funciona* desde cualquier pantalla posterior al login, y tres estados nuevos en §8 (carga fila por fila en P2, ciclo distinto al más reciente en el expediente, evidencia SHAP sin poblar) ✅ · **Contrastado contra el texto oficial** del plan ya mergeado (PR #315): se corrigió la atribución del SQL oculto del Asistente (§4.ter), que puede implementarlo Equipo 2 o Equipo 5 según dónde viva el componente, no solo Equipo 2 como se había escrito antes de que el PR aterrizara ✅ · Decisión del logo P6→P1 ratificada como cerrada: P1 no revela el número en visita recurrente pero deja de fingir que la investigación empieza de cero ✅ · Verificado por Edgar Coronel contra el código real (`auth.py:194`, contrato de `/kpis`), no solo contra la descripción ✅ · `vault_lint` limpio ✅ · [[vault/_DevLog/2026-09-10-oscar-quiroz-borrador-arquitectura-ux-us621]] · [[vault/_DevLog/2026-09-10-oscar-quiroz-reconciliacion-arquitectura-ux-marina-monse]] · [[vault/_DevLog/2026-09-11-oscar-quiroz-cierre-arquitectura-ux-y-push]] | **Entrada directa por URL a P4/P6** es una decisión propia de este documento, no algo que el plan ya definiera — queda documentada como tal para que el Equipo 5 sepa que es interpretación de este frente, no un requisito de arriba ⬜ · Handoff formal: falta que **Diana Alvarez (Equipo 5)** confirme si ya reconcilió sus rutas construidas contra las 7 pantallas de este documento, ahora que la versión final está en PR ⬜ | 🟢 versión final · PR #319 abierta · pendiente de aceptación de Equipo 5 |
## Evidencia incremental — 2026-09-11 · botón de login (US-405, ADR-012) y hallazgo en `docker-compose.yml` (Diana Alvarez)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-405` · `ADR-012` | **Botón de login implementado tal como lo especifica `ADR-012` §6**, comentario que había quedado en `api.js` desde la revisión del PR #302 y que no se había construido: `getAuthLoginUrl()` apunta directo al origen del API (`VITE_API_ORIGIN`, deliberadamente separada de `VITE_API_BASE_URL`, que sigue relativa/proxiada) porque la cookie `faro_oauth_state` debe fijarse en el origen del API y no en el del frontend ✅ · `postAuthExchange()` nuevo, `POST /auth/exchange?sesion=cookie` por ruta relativa (proxiada, mismo origen), nunca expone el JWT al frontend ✅ · `SessionProvider` nuevo (`frontend/src/lib/session.jsx`): al montar, canjea `?code_faro=` si viene en la URL, limpia la URL con `history.replaceState` y resuelve `getAuthMe()` — expone `useSession()` con `status: loading/anonimo/autenticado` ✅ · `Topbar.jsx` reescrito para consumirlo: botón "Iniciar sesión" en anónimo, avatar con iniciales reales + "Cerrar sesión" en autenticado ✅ · Verificado en vivo por Diana: el flujo completo llega hasta la pantalla real de consentimiento de `accounts.google.com` ✅ · Además, los 4 bloques de `VistaGeneral.jsx` que Edgar señaló en su revisión del PR #321 (KPIs con `escuelas_en_riesgo`, mapa, matrícula por ciclo, distribución por nivel) ya llevan `DemoBadge`, mismo patrón que "El diferenciador" — enviado y mergeado antes de este commit ✅ | **Hallazgo de infraestructura, no de código de aplicación — probando el flujo en vivo.** El bloque `environment:` del servicio `api` en `docker-compose.yml` nunca pasaba `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `FRONTEND_REDIRECT_URIS` ni `JWT_SECRET_KEY` al contenedor — y el `.env` de la raíz tampoco se copia ni se monta dentro de él (`docker/api.Dockerfile` sólo hace `COPY src/`), así que ningún valor local de auth llegaba nunca al proceso, sin importar cuántas veces se reiniciara el contenedor (`docker restart` reutiliza el entorno con el que se creó; hace falta `docker compose up -d api` para que tome un `environment:` nuevo). **Corregido y mergeado en `main`** (PR #322, 11-sep) agregando las 6 variables faltantes a `docker-compose.yml`, con los mismos defaults que ya usaba el resto del archivo — `docker-compose.yml` pasó a `comunes` en `ownership.yml` vía `DEC-025` (autorización explícita del PM ante la ausencia de Luis en S7), así que se aprobó y mergeó sin bloqueo de revisión técnica de Luis ✅ · **El mismo hueco existe hoy en producción:** `deploy-cloud-run.sh` pasa `GOOGLE_CLIENT_ID`, `GOOGLE_REDIRECT_URI` y `ANALISTA_EMAILS` por `--set-env-vars`, y `JWT_SECRET_KEY`/`POSTGRES_PASSWORD`/`GOOGLE_CLIENT_SECRET` por `--set-secrets`, pero **`FRONTEND_REDIRECT_URIS` no aparece en ninguna de las dos** — no se tocó (fuera de alcance de E5, mismo dueño de `docker/**`), reportado a Christian/Luis como urgente de cara al viernes ⬜ · **Bug secundario encontrado y reportado, no corregido:** `src/api/app.py`, el diccionario `_ERROR_POR_STATUS` no tiene entrada para `400`, así que un `HTTPException(400, ...)` real (p. ej. `redirect` fuera del allowlist) se etiqueta `"internal_error"` genérico en vez de su causa — confunde el diagnóstico del lado del cliente; también fuera de alcance de E5 ⬜ | ✅ US-405 mergeado en `main` (PR #322, 11-sep) · gap de producción (`deploy-cloud-run.sh`) y bug de `app.py` siguen reportados y pendientes, fuera de alcance de E5 |

## Evidencia incremental — 2026-09-11 · reconciliación de rutas contra `01_UX_Architecture.md` (Diana Alvarez)

| REQ | Historias | Evidencia entregada | Pendiente / a quién | Estado |
|---|---|---|---|---|
| `REQ-002` | `US-621` | **Confirmación del pendiente de handoff que dejó Oscar Quiroz en el PR #319:** se comparó `frontend/src/main.jsx` contra las 7 pantallas de `01_UX_Architecture.md` (versión final). **No está reconciliado.** El documento define una sola P6 ("Explorador de escuelas", `/explorador`, 3 filtros obligatorios ciclo/entidad/nivel, reutilizando las gráficas del expediente) y P4 en `/escuela/{cct}`; el frontend tiene `/casos/:cct` y **4 rutas separadas** (`/mapa`, `/drivers`, `/comparacion-territorial`, `/comparativa`) donde el plan pide una sola, sin ningún `/explorador` ✅ (confirmación honesta del estado, no la reconciliación en sí) · Verificado que no es un descuido: las 4 páginas ya estaban documentadas como bloqueadas por el gap de contrato del API en evidencia anterior de Fase 2 — la estructura de rutas predata la versión final del documento de Oscar ✅ | **Plan de reconciliación, en dos pasos:** (1) renombrar `/casos/:cct` → `/escuela/:cct` — sin dependencia de API, ya ejecutado en `frontend/src/main.jsx` y `LosSieteCasos.jsx`; PR nuevo abierto para mergear a `main` (el commit no alcanzó a entrar en el PR #322 por una diferencia de minutos) ✅ · (2) consolidar las 4 rutas en una sola `/explorador` con los 3 filtros obligatorios, una vez exista el endpoint agregado que sostenga la pantalla única — bloqueado por el mismo gap de API que ya bloqueaba estas 4 páginas por separado, no se resuelve solo consolidándolas ⬜ | 🟡 paso 1 ejecutado (código listo, PR pendiente de aprobación del PM) · paso 2 sigue bloqueado por gap de API |
